# Browser SFX Mirrors

Mirror browser-ready special effects here when the video builder needs to load them.

Source files should live under:

```text
audio/sfx/
```

Browser-ready files should use stable lowercase names and paths that can be referenced from generated data or builder configuration.
