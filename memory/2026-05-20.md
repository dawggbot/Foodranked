# 2026-05-20

## Display Builder Layout Recovery

- James reported the display builder sprite placements/layout were wrong after today's builder work and asked to return to how it was at the start of the day.
- The attempted food-aware shared layout propagation was wrong for the current sprite placements. It was reverted in commit `1d7797e` and should not be treated as active behavior.
- `docs/app/index.html` was restored to the start-of-day builder baseline in commit `548f075`, after James said the previous revert still did not visibly restore the layout.
- The remaining "no change" reports were traced to browser/GitHub Pages cache and persisted builder `localStorage`, not just repo code. The old stored layout keys were:
  - `foodranked-display-builder-v4`
  - `foodranked-display-builder-sprite-layouts-v1`
- A storage-key bump was pushed in commit `0691a4d`, moving the builder to `foodranked-display-builder-v5` so browsers stop loading corrupted old `v4` layout state.
- A standalone reset page was pushed in commit `7a51c0b`: `https://dawggbot.github.io/Foodranked/app/reset-layout.html`. It clears the old saved builder layout keys and redirects back to the builder.
- Earlier in the day, a laptop-scaling fix (`f556cfa`) and a food-aware layout attempt (`e1b416e`) happened, but the user ultimately prioritized restoring the original sprite layout. Be careful not to reintroduce the reverted food-aware placement behavior without checking the actual visual layout first.

## Display Builder Follow-Up Corrections

- The v5 storage-key bump and reset page were later reverted because the correct Bacon layout was likely browser-saved under `foodranked-display-builder-v4`, not committed in git. The builder is back on the original `foodranked-display-builder-v4` key after commit `7a2e99a`; do not assume `v5` is active.
- The exact old Bacon sprite placement layout was not found in git. If it is not present in a browser/profile/device `localStorage` value for `foodranked-display-builder-v4`, it must be rebuilt from assets and committed as a repo baseline.
- James accidentally deleted the last section indicator. Commit `5b42921` added missing-indicator recovery by expected id, but that created duplicate indicator rows on top of the original saved layout.
- James clarified the desired behavior: keep the original indicator row/layout, remove duplicate added layers, and only append one missing final indicator sprite at the end.
- Commit `0d226ff` changed recovery to remove duplicate section-indicator layers, preserve the original/manual row, and append only a single missing final indicator.
- James then reported the final indicator was still missing and asked for the section 1/intro header layout to apply to other sections.
- Commit `aec5b37` fixed the stuck case where 8 real indicators plus 1 overlapping duplicate incorrectly counted as complete, then synced the intro/section 1 header sprite and text layout across all other sections.
- Latest verified state from the previous turn: `aec5b37` was pushed, Playwright tests passed for indicator recovery and header layout sync, and the FoodRanked schema Google Doc was updated with the correction.
