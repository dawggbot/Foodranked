# 2026-05-21

## FoodRanked Builder UI

- James liked the updated fats submacro arrow/color layout and asked for the same layout behavior across the other macro sections.
- Recent pushed commits on `main` in `docs/app/index.html`:
  - `dc8a2d7` (`Color submacro values by arrow band`): submacro numeric values inherit the same red/green meaning as their arrow band; arrows are polarity-aware, so lower-is-better rows can show green down arrows and red up arrows.
  - `02e1e5d` (`Apply macro layout sync to carbs and protein`): bumped the builder layout migration to `macroSectionLayoutFromFatsV5`, syncing the current fats layout into carbs and protein for existing saved layouts; also fixed legacy `↑`/`↓` band handling so value colors follow displayed arrow polarity.
  - `d47f37a` (`Show protein fallback arrows in builder`): added support for using `protein_g_fallback` as the protein-total row band, but this was immediately superseded by James clarifying that flat macro totals should be ignored for now because he plans fillable animated bars for fats/carbs/protein.
  - `ee31b58` (`Use collagen protein submacro in builder`): protein submacro rows are now `COLLAGEN`, `EAA`, `N-EAA`, and `BIOAVAIL.`; the flat `PROTEIN` row was removed from the submacro list. Protein rows use episode display bands first, then fall back to batch score breakdown data for submetric arrows/colors when needed.
- Current design direction: flat macro totals (`fats`, `carbs`, `protein`) should not be treated as submacro arrow rows. Protein submacro display should use collagen, essential amino acids, non-essential amino acids, and bioavailability with score-driven colors and arrow indicators.
- Verification used across these changes: inline script parse checks, Playwright render checks for bacon/oats/anchovies, migration simulation from older saved layouts, and `git diff --check`.

## Builder Micro Bars And Video Builder

- James liked the micro bar display and clarified that sprites/text are currently positioned correctly only for bacon; other foods will need independent per-food layout configuration later.
- Pushed builder display commits on `main` after the earlier macro work:
  - `b26c64e` (`Step vitamin bars by DV percentage`): EAA now displays as `/9`, N-EAA as `/11`; vitamin bars use 10% DV steps via floor-style display, capped at 10 bars; percent text is placed over the highest displayed vitamin bar; B12 matching respects the current visual order instead of assuming data order.
  - `45ae9a6` (`Apply stepped bars to minerals`): minerals use the same stepped percentage bar behavior as vitamins; any numeric value under 10% initially lit the first 10% bar, with `N/A` empty.
  - `e4f0045` (`Treat zero micronutrient percentages as unavailable`): final rule is that `0%` vitamin/mineral values display as `N/A` and show no bars; real low values from `1%` through `9%` still light the first 10% bar.
- Micro bar validation covered synthetic `0%`, `1%`, `8/9%`, `N/A`, mid values such as `42%`/`67%`, and `100%+`, plus browser checks.
- Added the separate animation surface in `d71d308` (`Add video animation builder`):
  - New page: `docs/video-builder/index.html`.
  - Linked from `docs/index.html`.
  - Purpose: separate video builder for reveal animations, caption editing, and sprite motion, without mixing animation tooling into the existing display builder.
  - It reads the display builder's saved layout when available and uses the repo default as fallback.
  - Current feature set: 9-scene timeline, reveal style controls, caption editing, sprite motion controls, playback/scrubbing, and copyable video manifest.
  - Verified in browser at desktop and mobile preview sizes.
