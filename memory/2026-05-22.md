# 2026-05-22

## Runtime / Repo State

- As of 2026-05-22 17:54 UTC, `main` is clean and matches `origin/main`.
- Local video builder preview was running at `http://127.0.0.1:4173/video-builder/` during the preview-polish work.

## Durable Project Updates

- Food sprite upload automation uses `production/inbox/food-image-uploads/<food-id>.png` as the GitHub trigger folder. Recent pushed commit: `50fe861` (`Limit sprite trigger workflow to uploads`).
- Voice settings live in `config/elevenlabs-voice-settings.v1.json`. ElevenLabs API key must stay in local `.env.local` or GitHub Actions `ELEVENLABS_API_KEY`, never in committed files.
- Video builder caption/text polish was pushed in `81135eb` (`Polish video builder captions and text sizing`):
  - Video preview text layers now match display-builder text rendering instead of using fixed sprite-style text heights or forced scale-down behavior.
  - Header food type, per-100g label, and score value were aligned with display-builder apparent sizing.
  - Subtitles render as centered word spans, with the active spoken word highlighted yellow.
- Caption centering and word sync tuning was pushed in `a819c37` (`Center visible video captions`):
  - Captions now center against the visible phone canvas/shell crop, not the oversized full author grid.
  - Caption safe margins are calculated from the actual crop for desktop and mobile.
  - Yellow word highlighting uses weighted word timing plus a small visual lookahead.
- Section reveal timing was pushed in `1c5b06d` (`Sync video reveals to narration beats`):
  - Section reveal beats now derive from spoken caption sentences/episode timing instead of generic row cascades.
  - Sprite rows/cards reveal when their matching metric or pro/con line is spoken where possible.
  - Final score waits until the tier sentence instead of appearing at the start of the verdict scene.
  - The manifest includes per-scene `revealBeats` so timing is inspectable/exportable.

## Verification Notes From Recent Work

- Ran `node --check docs/video-builder/video-builder.js` and `git diff --check` for the video builder changes.
- Playwright checked desktop and mobile preview states for text sizing, visible-canvas caption centering, yellow active-word captions, pros row reveal timing, and verdict score reveal timing.
