# 2026-05-23

- James requested subtitle/audio split fixes for generated FoodRanked scripts. Final locked behavior from this work: subtitles and on-screen/display text keep compact units like `3g`, `24mg`, and `100kcal`; actual narration/audio-ready text expands them to spoken words like `3 grams`, `24 milligrams`, and `100 calories`.
- Subtitle cues should be generated/rendered as a maximum of 2 lines. The latest fix also capped cues at 26 characters per line to avoid downstream wrapping into a third line.
- Implementation was corrected and pushed in commit `010d2ed` (`Fix narration subtitle unit split`). Earlier commit `fb0ac71` attempted the change but had the split wrong; treat `010d2ed` as the relevant final state.
- Final implementation notes: `script.sections[].narration` is audio-ready expanded text; `script.sections[].subtitleText`, `subtitles.json`, and website subtitle data keep abbreviated units; episode manifests now include subtitle cues so downstream tools do not need to use full scene text as subtitles.
- Verification reported after the fix: `scripts/run-foodranked-test-pack.sh` passed; subtitle line-count check had 0 violations; subtitle width check had 0 violations over 26 chars; narration fields/files had 0 compact unit abbreviations; subtitle cues had 0 expanded `grams` units. No `score.json` files were changed.
