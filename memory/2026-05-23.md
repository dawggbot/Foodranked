# 2026-05-23

- James requested subtitle/audio split fixes for generated FoodRanked scripts. Final locked behavior from this work: subtitles and on-screen/display text keep compact units like `3g`, `24mg`, and `100kcal`; actual narration/audio-ready text expands them to spoken words like `3 grams`, `24 milligrams`, and `100 calories`.
- Subtitle cues should be generated/rendered as a maximum of 2 lines. The latest fix also capped cues at 26 characters per line to avoid downstream wrapping into a third line.
- Implementation was corrected and pushed in commit `010d2ed` (`Fix narration subtitle unit split`). Earlier commit `fb0ac71` attempted the change but had the split wrong; treat `010d2ed` as the relevant final state.
- Final implementation notes: `script.sections[].narration` is audio-ready expanded text; `script.sections[].subtitleText`, `subtitles.json`, and website subtitle data keep abbreviated units; episode manifests now include subtitle cues so downstream tools do not need to use full scene text as subtitles.
- Verification reported after the fix: `scripts/run-foodranked-test-pack.sh` passed; subtitle line-count check had 0 violations; subtitle width check had 0 violations over 26 chars; narration fields/files had 0 compact unit abbreviations; subtitle cues had 0 expanded `grams` units. No `score.json` files were changed.

## 12:29 UTC follow-up

- Later on 2026-05-23, James reported the prior subtitle/audio split was still not sufficient: some subtitles wrapped into 3 lines, narration could still say compact `G`/unit forms, subtitles could still say `grams`, and macro sections named submacro pressure points without displaying the numeric submacro value.
- Current pushed fix supersedes the earlier `010d2ed` memory entry. Treat `4d65f4e` (`Fix narration unit split and subtitle wrapping`) as the current final implementation for this issue, followed by `1b3ebcc` to remove the local memory file from git tracking after the push helper accidentally committed it.
- Locked current behavior from `4d65f4e`: subtitles/display text keep compact units (`12.6g`, `100g`), narration/audio-ready text expands those units (`12.6 grams`, `100 grams`), and subtitle cue wrapping now targets max 2 lines with a stricter 20-character line target.
- Macro section narration/subtitles now include at least one numeric submacro value for fats, carbs, and proteins, e.g. bacon shows `Saturated fat is 12.6g` in subtitle/display text and speaks `12.6 grams`.
- Existing narration/subtitle artifacts and `docs/data/foods-index.*` were regenerated to match the generator. Score snapshots were protected: final verification confirmed no `outputs/episodes/**/score.json` files changed.
- Verification after the current fix: `node scripts/verify-narration-subtitles.js`, `scripts/run-foodranked-test-pack.sh`, and `git diff --check` all passed.
- `memory/2026-05-23.md` is intentionally local/untracked after cleanup; do not re-add it during normal FoodRanked project commits.

## 12:48 UTC follow-up

- James reported after `4d65f4e` that subtitles still visibly said `grams` and the narration had not changed. The true final pushed fix for this issue is now `f19e29e` (`Fix subtitle units and refresh bacon narration`), which supersedes the earlier `4d65f4e` note for subtitle/audio unit behavior.
- The verifier now rejects full unit words in subtitle cue text, not only numeric expanded-unit patterns. Generated outputs and site data were checked with 0 subtitle unit-word hits.
- Bacon's actual published narration asset was regenerated, not just `narration.txt`: `voice-v2.mp3` was refreshed from the current audio text, and the audio metadata hash matches the current bacon narration in both production and docs.
- Current locked behavior remains: subtitles/display text use compact units such as `12.6g` and `100g`; narration/audio text uses spoken units such as `12.6 grams` and `100 grams`; macro sections include at least one displayed numeric submacro value.
- Verification after `f19e29e`: `node scripts/verify-narration-subtitles.js`, audio metadata hash checks, generated/site subtitle unit-word scans, `scripts/run-foodranked-test-pack.sh`, and `git diff --check` all passed.

## 13:31 UTC subtitle-builder follow-up

- After `f19e29e`, James still saw bad subtitles in the browser video builder. The real leak was the video builder rendering captions from `narrationBlocks` (audio-ready text with words like `grams`) instead of generated subtitle cue data, then re-wrapping captions independently in the browser.
- Pushed `34f2f8d` (`Render video captions from subtitle cues`) to make the video builder render generated subtitle cues directly. Visible captions now preserve abbreviated units, use explicit `.caption-line` rows capped at 2 lines, and use subtitle-cue timing (`subtitle-cues-v3`) with tighter word lookahead.
- James then asked to tighten subtitles from all angles and make the conclusion summary full-width before a centered tier reveal. Pushed `18a920f` (`Tighten subtitles and center tier reveal`) as the latest subtitle/tier-reveal implementation.
- Current locked subtitle presentation behavior from `18a920f`: default cue wrapping targets 18 chars per line; closing summary cues use `summary-full` placement across the page; final tier cue is last and uses `tier-center` placement centered on screen, as a placeholder until James makes a tier-reveal sprite.
- `18a920f` also weights highlight timing by cue content instead of splitting chunks evenly. Generated subtitle cues/manifests and `docs/data/foods-index.*` were refreshed across episodes, while score/script/narration/audio files were intentionally left untouched.
- Verification after `18a920f`: `node scripts/verify-narration-subtitles.js`, full test pack, and browser render checks passed. Browser checks showed max rendered subtitle lines = 2, zero rendered `grams` hits, and bacon final-scene placements `summary-full`, `summary-full`, then `tier-center`.

## 17:00 UTC pre-compaction flush

- After `18a920f`, pushed `3e00664` (`Fix protein submetrics and tighten summary captions`). This fixed compact episode protein sections so generated narration/display no longer uses `protein_g_fallback` as the displayed protein submacro. Use real protein submetrics from the food record instead, such as essential amino acids and bioavailability, rather than repeating protein grams.
- `3e00664` also tightened closing summary captions: `summary-full` cues are capped at 28 chars per line and inset further inside the canvas. Bacon preview audio was regenerated as `voice-v3`; Salmon and Rice Cakes had regenerated text/subtitle outputs only.
- James then reported that EAA narration said `89` where speech should say `8 out of 9`, subtitles should remain `8/9`, and the conclusion was still off center. Latest pushed fix is `7ae8ba5` (`Tighten subtitles and speak EAA ratios`), which supersedes earlier EAA/conclusion placement behavior.
- Current locked EAA ratio behavior from `7ae8ba5`: audio/narration text speaks ratios as `8 out of 9`; subtitles/display keep compact ratios such as `8/9`.
- Current locked closing-caption behavior from `7ae8ba5`: final conclusion summary uses full-width centered placement, then the final tier reveal is centered separately; compact final timing no longer counts the unused CTA.
- `7ae8ba5` regenerated Bacon as `voice-v4`, updated docs preview data to point to it, refreshed generated subtitle cues/manifests, and kept subtitle cues capped at 2 rendered lines with compact units.
- Verification after `7ae8ba5`: `node scripts/verify-narration-subtitles.js`, `scripts/run-foodranked-test-pack.sh`, and Bacon browser/Playwright checks passed. Browser checks showed max subtitle lines `2`, no subtitle `grams`, summary center delta `0`, tier center delta `0`, and no overflow. Confirmed no `outputs/episodes/**/score.json` files changed.
- `memory/2026-05-23.md` remains local/untracked and should not be included in normal FoodRanked commits.

## 17:56 UTC pre-compaction flush

- James liked the current Bacon script/narration after `7ae8ba5` and asked for a focused tightening pass only: major subtitle/narration sync work, restore sprites to match display-builder layout, and keep the conclusion inside the canvas.
- Latest pushed state is `1e7e93f` on `main`. This supersedes the previous preview/sync behavior from `7ae8ba5` while preserving the liked Bacon script/narration content.
- Important preservation note: regenerating Bacon from the current episode script generator would remove the Omega 3 narration line James liked. The fix intentionally preserved the committed Bacon narration/script and rebuilt subtitle cues/timings around that text instead.
- Bacon subtitles are now scaled to the measured `voice-v4` audio duration: `73.885896s` (about `73.89s`). The video builder preview also calibrates its scene/timeline duration from loaded audio metadata instead of assuming the nominal generated timeline.
- Subtitle tokenization now preserves decimal nutrient values as single subtitle tokens, so values like `37.1g`, `12.6g`, and `13.7g` do not split into broken cues such as `37. 1g`.
- Locked display/audio split still holds: subtitles and display use compact values such as `g`, `mg`, and `8/9`; narration/audio uses spoken forms such as `grams`, `milligrams`, and `8 out of 9`.
- Conclusion captions now use safer centered `summary-full` lines, followed by centered `tier-center` for `D tier.`. Playwright checks reported no canvas overflow and centered final captions on desktop and mobile.
- Video builder sprite rendering now stops animation transforms from shifting authored sprite positions, restoring parity with the display-builder layout for content sprites.
- Verification after `1e7e93f`: `node --check docs/video-builder/video-builder.js`, `node --check scripts/foodranked-generate-episode.js`, `node scripts/verify-narration-subtitles.js`, `./scripts/run-foodranked-test-pack.sh`, and Playwright desktop/mobile preview checks passed.
- During this pass, the push helper briefly auto-committed `memory/2026-05-23.md`; a cleanup commit immediately removed it from `main`. Current GitHub `main` does not track the memory file, and the local memory file remains untracked.
