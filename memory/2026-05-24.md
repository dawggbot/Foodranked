# 2026-05-24

- James confirmed subtitles and narration are synced; current video-builder work is focused on visual placement parity with the display builder.
- Pushed `0ce2868` for the first outro layout fix: final-scene subtitle placements normalize into centered outro positions, tier reveals use `tier-center`, old final placements such as `subtitle-floor`/`verdict-payoff` render as `summary-full`, and video-builder chrome prefers active-section display-builder layers before falling back to intro chrome.
- Pushed `3bbb3ac` to tighten outro alignment: final subtitle placement now normalizes inside the timing model too, legacy tiny/header-position `outro_score_value` layers migrate to the large centered verdict slot (`x=48`, `y=94`, `width=40`, `fontSize=22`), and the repo default layout uses the same centered verdict position.
- Pushed `424232e` after James identified the remaining misses as arrow indicator placement, saturated-fat numeric placement, and outro captions being centered against the wrong canvas. This commit makes video builder mirror display-builder macro-arrow logic from episode/ruleset data, places submacro numeric values left of the arrow group, preserves decimals such as bacon `SAT FAT` as `12.6g` instead of `13g`, and centers outro subtitles against the visible phone canvas crop rather than the hidden stage.
- Useful verified coordinates after `424232e`: bacon `SAT FAT` rendered as `12.6g` at grid `x=51`, `y=76`, with arrows starting at `x=76`; outro summary and `D tier.` cues probed at `0px` x/y delta against the visible canvas crop.
- Verification used during this layout pass included `node --check docs/video-builder/video-builder.js`, `node --check docs/app/default-layout.js`, `node scripts/verify-narration-subtitles.js`, and Playwright/browser probes for Fats macro layout plus Verdict subtitle centering.
- Pushed `a576dcb` after James asked to restore the outro score number, enlarge outro captions, fix protein submacro value colors, and unlock display-builder sprite dragging. The outro score value is back in the score card at `x=64`, `y=24`; outro summary captions are `22px`; final tier caption is `44px`; protein submacro values are color-coded in both builders; sprite moves save with `manualPosition` so sync no longer snaps them back.
- Pushed `2f86111` after James reported missing protein arrows and a locked starch value. Protein arrow indicators now render in both display and video builders using a presentation fallback for protein submetrics without changing scoring rules. The starch numeric layer `carbs_submacro_value_3` can be manually moved and keeps `manualPosition: true` through normalization instead of snapping back over the arrow slot.
- Verification for `a576dcb` and `2f86111` included JS syntax checks for `docs/video-builder/video-builder.js` and `docs/app/default-layout.js`, `node scripts/verify-narration-subtitles.js`, `git diff --check`, and browser probes for outro score placement, caption sizes, protein colors/arrows, sprite dragging, and starch value persistence.
- Pushed `a311ee1` to make video-builder sprite/text placement match display-builder saved layouts more closely. Video builder now respects `manualPosition` for macro numerical values, display builder preserves manually moved micronutrient value text instead of snapping it back to DV bars, and video builder auto-rehydrates when a display-builder layout is saved in another tab.
- Verification for `a311ee1`: simulated saved display-builder edits kept starch at `63,113` and a vitamin value at `91,72` in video builder; a cross-tab save moved starch to `66,114` and video updated to the same coordinate; default layout comparison across fats, carbs, protein, vitamins, minerals, pros, cons, and outro had `0` common-layer placement diffs. Checks passed: `node --check docs/video-builder/video-builder.js`, `node --check docs/app/default-layout.js`, `node scripts/verify-narration-subtitles.js`, and `git diff --check`.
- Pushed `74b4dfe` after James flagged the middle protein submacro arrows in video builder. Video builder now loads `docs/data/batch-results.json` and uses scorer metric bands for protein arrow counts/colors/directions, matching display builder; Bacon protein rows verified as Collagen `3` red down arrows, EAA `2` green up arrows, N-EAA `2` green up arrows, and Bioavailability `2` green up arrows. Protein reveal order is now physical row order: row 0, row 1, row 2, row 3.
- Pushed `c3ccbe9` after James requested a 3-second viewing window between sections. Video builder now adds a 3-second hold after fats, carbs, protein, vitamins, minerals, pros, and cons; intro/outro do not get the extra hold. During holds, section visuals stay on screen, subtitles hide, and preview narration pauses/resumes in sync. Export manifests now include narration duration, total hold seconds, per-scene hold start/end, and hold-aware subtitle cue timings.
- Verification for `74b4dfe` and `c3ccbe9` included JS syntax checks, `git diff --check`, `node scripts/verify-narration-subtitles.js`, and browser timing/manifest/caption probes. The preview server was stopped after verification. As of the memory flush at 2026-05-24 14:00 UTC, `main` matched `origin/main`; the only local change was the untracked `memory/2026-05-24.md`.
- `04fc330` briefly changed video section holds to a visual-only model, but this was superseded. Do not reintroduce that timing approach: James reported it made audio choppy and rushed/changed the animations.
- Bacon Fats narration/detail should highlight polyunsaturated fat rather than omega 3.
- Pushed `b6deeb7` to `main` to restore real post-section dwell timing with 2-second holds after each of the 7 main sections. The current model keeps reveal/animation timing uncompressed, hides subtitles during each dwell, pauses preview audio once during a dwell, resumes once after it, and avoids frame-by-frame audio seeking.
- `b6deeb7` was rebased over James's uploaded intro/outro sprite files from GitHub, preserving those uploaded sprite changes.
- Verification for `b6deeb7` included `node --check docs/video-builder/video-builder.js`, `git diff --check`, `node scripts/verify-narration-subtitles.js`, and browser playback probes. The browser probe showed roughly 87.89s total video duration, 73.89s narration duration, 14s total dwell, 7 held sections, no bad subtitle cue spillover, and only intentional audio seeks around dwell boundaries.
- Pushed `912c2e1` to `main` for James's slower bacon narration and ranked-hook sprite request. The saved ElevenLabs voice speed is now `1.10x`; bacon uses `voice-v5` (`docs/audio/episodes/bacon/voice-v5.mp3` plus matching alignment JSON), and refreshed builder/site data points bacon to that take.
- The 1.10x voice-speed preference is now recorded in `config/elevenlabs-voice-settings.v1.json`, `production/ELEVENLABS-COPY-PACK.md`, `production/LAUNCH-5-NARRATION-QUEUE.md`, `production/NARRATION-OPS.md`, and `production/episodes/bacon/voice/voice-notes.md`. Keep future bacon narration regen aligned with that setting unless James changes it.
- Intro hook behavior after `912c2e1`: on spoken "Bacon", the food sprite appears; on spoken "ranked", the new ranked sprite appears. The food sprite sits in the ranked sprite's middle space above the word `ranked` and below the four stars. The ranked sprite has shiny/sparkly CSS treatment and is registered in the display-builder sprite library.
- Verification for `912c2e1` included JS syntax checks, `git diff --check`, `node scripts/verify-narration-subtitles.js`, config JSON parsing, and a browser probe confirming `voice-v5`, 2s section holds, intro word-timed reveals, and loaded bacon/ranked sprites.
- As of the memory flush at 2026-05-24 16:10 UTC, `main` and `origin/main` were at `912c2e1`; the only local git status item before this append was the untracked `memory/2026-05-24.md`.
