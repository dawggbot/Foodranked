# 2026-05-28

- James is leaving tomorrow and needs the FoodRanked display builder and video builder laptop-compatible without breaking PC behavior.
- Recent pushed fixes on `main` for builder laptop alignment/sprites:
  - `0843336 Fix laptop preview canvas scaling`
  - `85ac628 Stabilize builder layout on laptops`
  - `6f7d099 Avoid missing food sprite errors`
  - `66c0155 Fix laptop sprite placement math`
- Diagnosed likely laptop sprite issue: display builder drag/drop math read `--pixel-unit` from CSS, but on display builder it was a `calc(...)` string. `parseFloat` effectively kept PC fallback `4` while laptop pixel unit was about `3.2`, so authored sprite/text positions could be wrong on laptop. Last fix measures actual canvas width and writes a numeric `--pixel-unit` after responsive scaling.
- Other builder fixes from this session: compact/cropped phone-frame model now applies for laptop height-triggered compact mode; Tiny5 font is local at `docs/assets/fonts/tiny5-v3.ttf` and Google Fonts dependency was removed from builder pages; display/video builders avoid requesting missing food-image PNGs and use fallbacks; legacy saved sprite paths are normalized to canonical paths.
- Verification reported after latest fix: Chromium checks covered display and video builders on desktop, laptop, and narrow laptop; multiple foods and major sections/scenes; no console errors, no 404s, no legacy sprite paths rendered, no visible off-grid sprites. If James still reports laptop mismatch, continue from `66c0155` and compare real laptop screenshots against PC, keeping PC versions intact.
- Unrelated untracked file noted during work: `memory/2026-05-24.md`; it was intentionally left untouched.
# 2026-05-28

- James is leaving tomorrow and needs the FoodRanked display builder and video builder laptop-compatible without breaking PC behavior.
- Recent pushed fixes on `main` for builder laptop alignment/sprites:
  - `0843336 Fix laptop preview canvas scaling`
  - `85ac628 Stabilize builder layout on laptops`
  - `6f7d099 Avoid missing food sprite errors`
  - `66c0155 Fix laptop sprite placement math`
- Diagnosed likely laptop sprite issue: display builder drag/drop math read `--pixel-unit` from CSS, but on display builder it was a `calc(...)` string. `parseFloat` effectively kept PC fallback `4` while laptop pixel unit was about `3.2`, so authored sprite/text positions could be wrong on laptop. Last fix measures actual canvas width and writes a numeric `--pixel-unit` after responsive scaling.
- Other builder fixes from this session: compact/cropped phone-frame model now applies for laptop height-triggered compact mode; Tiny5 font is local at `docs/assets/fonts/tiny5-v3.ttf` and Google Fonts dependency was removed from builder pages; display/video builders avoid requesting missing food-image PNGs and use fallbacks; legacy saved sprite paths are normalized to canonical paths.
- Verification reported after latest fix: Chromium checks covered display and video builders on desktop, laptop, and narrow laptop; multiple foods and major sections/scenes; no console errors, no 404s, no legacy sprite paths rendered, no visible off-grid sprites. If James still reports laptop mismatch, continue from `66c0155` and compare real laptop screenshots against PC, keeping PC versions intact.
- Unrelated untracked file noted during work: `memory/2026-05-24.md`; it was intentionally left untouched.

- Later pushed cache/diagnostic commits on `main` for the same laptop sprite investigation:
  - `c5d9055 Bust builder asset cache`
  - `5b86917 Show fallback selected food sprites`
  - `faf1e50 Add builder sprite diagnostics`
  - `1cf34ef Report builder layout source`
- Public GitHub Pages was confirmed serving build marker `20260528-sprite-fallback-v2`, then the user copied diagnostics from both `https://dawggbot.github.io/Foodranked/app/index.html` and `https://dawggbot.github.io/Foodranked/video-builder/index.html` showing build `20260528-sprite-report-v3`, `remembered failures: 0`, and `currently broken images: 0`.
- Current durable diagnosis: when using the GitHub Pages URLs, James's laptop does not need local sprite files; it loads committed GitHub assets. The repo/live site appeared to have only one committed custom food image, `bacon.png`; other foods use generic food-type fallback plates. If James's PC shows extra actual food sprites, they are likely PC-local and not committed/pushed, or the PC has a localStorage-saved display/video layout that the laptop lacks.
- Next useful step if conversation resumes: ask James to hard refresh and copy the new `20260528-layout-report-v4` sprite report. It should include layout source and whether the selected food has a committed custom image, which distinguishes missing committed assets from PC-only localStorage/layout differences.
