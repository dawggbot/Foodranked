
## 2026-05-29 15:51 UTC - Display Builder And Video Timing

- Google Doc `FoodRanked schema` display JSON was accessible/exportable and parsed successfully. The exported default display layout had `canvas: 135x240`, `selectedFoodId: bacon`, `selectedSectionId: protein`, `sections: 9`, and `layers: 459`.
- Synced the Google Doc `display json` into `docs/app/default-layout.js`; committed and pushed `03968f7` (`Sync display builder default layout`). Checks included parsing the JS assignment, confirming 9 sections/459 layers, verifying decoded sprite references exist, and `git diff --check`.
- Fixed display builder falling background sprites after the layout sync. Real committed food sprites now win over generic fallback plates; fallback plates are only used when no real food sprites exist. Commit pushed: `681046a` (`Restore falling food background sprites`). Verified 24 falling sprites using `./sprites/header/food_images/bacon.png` and 0 broken images.
- Synced laptop/PC/other-device builder behavior by adding repo layout versioning instead of trusting stale per-browser localStorage. Display builder stamps layouts with `20260529-layout-sync-v1`; video builder ignores stale display-builder localStorage and falls back to the repo default. Commit pushed: `5ce9d77` (`Sync builder layouts by repo version`). Verified stale desktop/laptop storage ignored, clean display/video builder loads OK, and falling sprites still render.
- Changed macro section animation to a two-beat reveal: macro icon appears first, then all submacro cards/text/arrows appear together. Applies to `fats`, `carbs`, and `protein`; video builder cache was bumped to `20260529-macro-reveal-v1`. Commit pushed: `7e7dc29` (`Set macro sections to two-beat reveal`).
- Updated timing so section dwell is `1s` instead of `2s`, and submacros reveal only after narration finishes the main macro sentence (for example after `37.1g of fat.`). Cache bumped to `20260529-macro-timing-v1`. Commit pushed: `1535038` (`Anchor submacro reveal after macro narration`). Verified total dwell is now 7s across the 7 scored sections and fats/carbs/protein each use `holdSeconds: 1`.
- Throughout these builder fixes, unrelated untracked memory files such as `memory/2026-05-24.md` and `memory/2026-05-28.md` were intentionally left untouched.
