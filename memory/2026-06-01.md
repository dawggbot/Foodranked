# 2026-06-01 Memory

- Bacon narration was upgraded into the preferred educational/informative script style. Current reference bacon compact script uses strongest visible indicator/value standouts rather than only weighted scores: fat section calls out polyunsaturated fat 5.8g as the positive standout and saturated fat 12.6g as the downside; protein section calls out essential amino acid score 8/9 with 90% bioavailability and low collagen; vitamins call out B12 21% DV and vitamin D 1% DV; minerals call out zinc 10% DV and iron 2% DV.
- James approved the new style direction: educational and informative, with brief explanations of why positive standouts matter and short category-based relevance notes for weak points. For submacros, explain the positive nutrient in plain language, e.g. polyunsaturated fats help with cell structure and healthy signalling, but do not copy James's examples verbatim. For weak vitamin/mineral notes, keep the category relevance short and refer to food type/category such as meats, vegetables, grains, not the exact food.
- Updated generator/rules/docs and outputs in recent commits: `bfd90b9 Regenerate bacon narration with outstanding nutrient notes`, `ab2a07a Refine bacon protein narration`, and `99f2258 Generate aligned bacon narration take`.
- Bacon narration was fully regenerated using ElevenLabs `voice-v6`, forced alignment was run, compact subtitles/manifest were updated with word timings, `docs/data/foods-index.json` and `docs/data/foods-index.js` now point at `voice-v6`, and `NARRATION-STYLE.md` includes bacon as the reference example. Aligned duration was about 90.529s with 58 subtitle cues.
- A subtitle alignment issue was fixed so `DV` maps correctly to spoken `daily value`.

- James corrected that FoodRanked videos now count intro and outro as sections: the video shell and builder progress indicators should be 9 sections (`intro`, `fats`, `carbs`, `protein`, `vitamins`, `minerals`, `pros`, `cons`, `outro`), while scoring still uses the 7 content sections. Do not revert the builder to 7 indicator dots.
- The vitamins/minerals progress indicator gap in the video builder was fixed while preserving the 9-section model. Post-section dwell was removed for the longer videos; generated manifests should report `totalHoldSeconds: 0`.
- Recent commit for that correction: `2d3bea2 Restore nine video sections and remove dwell`. Earlier commit `954269e Fix video builder progress indicator spacing` was superseded by this 9-section correction.

- James approved the narrated submacro highlight direction in the video builder: when narration reaches a specific submacro, highlight the matching row for viewer engagement using the same green/red nutrition color as the value/arrow indicator, but do not change the row text color.
- Latest intended submacro highlight behavior: stronger glow, fade in, hold while that submacro is being discussed, and only begin fading out when the next submacro starts fading in so the rows briefly crossfade. Current implementation was pushed in commit `6821d69 Strengthen submacro highlight crossfade`; earlier support came from `1243297 Highlight narrated submacros in video builder` and `ad04e79 Preserve indicator layout and fade submacro highlights`.
- Section indicator layout source of truth: preserve the display-builder/layout-provided progress indicator positions in the video builder. Do not manually force indicator spacing or add overlap; James said the original start-of-day indicators were correct. Commit `ad04e79 Preserve indicator layout and fade submacro highlights` removed the forced positioning after `fd13d13` overlapped too much.

- James said the macros section is now done, except he still plans to make the main macro animated bar sprite later. Preserve the current macro/submacro behavior unless he asks for more polish.
- Latest submacro/arrow behavior in the video builder: submacro card sprite, label, value, and arrow indicators reveal in sync; the separate delayed/slot-machine arrow roll was scrapped. Arrow indicators keep a permanent red/green glow after reveal, and the last highlighted submacro fades out when the section finishes. Recent relevant commits: `31a5ad0`, `b5e6fbe`, `c02dfab`, `28bb806`, and final polish in `26bdf30 Polish micron reveals and arrow glow`.
- Macro sprite GIF regression was fixed in `26bdf30` by reusing existing image nodes so GIF animation continues instead of recreating image nodes every frame.
- Latest vitamins/minerals reveal behavior: everything except the bars appears first with a subtle stamp-like reveal, then bar levels reveal together across all fields from 10% up to 100%, one level at a time. Recent relevant commits: `0b9f112 Sequence micron bars from bottom` and `26bdf30 Polish micron reveals and arrow glow`.

- Recent video-builder polish pushed after macro section completion: `391f177 Tighten narration and micro reveal timing`, `7bc637c Sync micro highlights and icon labels`, `8bde41b Rank micro highlights and tighten timing`, `96e4f79 Clean video canvas and tighten macro transitions`, `2a3a8bc Sync pros cons reveal highlights`, and `a1f314d Add major pros cons highlight effects`.
- Current intended micro-section behavior: micro narration highlights use the same red/green outstanding-rank logic as macros, with the highest narrated micro in that section green, the lowest red, and middle values neutral when there are more than two. Micro icons, icon labels, and graph/bar reveals are synced; micro icon text placement should follow display-builder placement rather than video-builder overrides. Micro bars reveal slightly slower than before.
- Video builder presentation notes: the canvas/stage border shadow was removed (`.video-stage` and `.stage-vignette` no longer add the previous shadow/inset look). Preserve this clean edge unless James asks for a frame.
- Narration timing remains an open polish area. James still feels pauses need a rework, especially around some section transitions; fats-to-carbs was tightened once in `96e4f79`, but further pause tuning is likely still needed.
- Pros/cons current behavior: bullet, impact label, and item text reveal together per row and receive narration highlight glow while being read. Pros glow green; cons glow red. Major pros add a sparkle/pulse effect while highlighted, and major cons add a red warning/flicker-style negative effect. Latest commit: `a1f314d Add major pros cons highlight effects`.

- Pros/cons polish advanced beyond `a1f314d`: commits `4bdc983 Tighten pros cons highlight focus`, `f28743c Tune pros cons highlight timing`, `1ca10ce Refine pros cons resting highlights`, `4b14896 Latch pros cons highlight effects`, `5fcfe2b Sync pros cons pulse and text glow`, and latest `f5ba09b Add global pulse and con sparks` were pushed.
- Current intended pros/cons behavior after `f5ba09b`: each pro/con row fades its highlighted state in when narration reaches it, then stays highlighted through the section; text and sprite/point marker are treated as one unit with matched highlight strength; highlighted text glow was boosted, with extra strength for pros/cons; major pro effects apply to text as well as sprites; major con flashing was made smaller and paired with solid red spark particles.
- Last-pro timing was fixed by making the narration matcher handle split/hyphenated words such as `comfort-food`, so highlights start on the leading phrase instead of late in the explanation.
- James asked to apply the subtle synced pulse motion to every sprite and text layer across all sections and the persistent header to see how it looks. This is an experiment, not necessarily final approval. Older unsynced per-sprite pulse scaling was removed so the motion stays unified.

- James rejected the global sprite/text pulse experiment. The video builder should no longer have sprite motion as a feature or UI control, and the old synced pulse/bob/drift/sprite-motion transform paths were removed. Relevant commit: `66a3d33 Remove sprite motion feature`.
- Current intended major con behavior: treat one pro/con point as a single unit made of the sprite, impact text, and item text. The major con red pulse treatment should apply to both the sprite and all text in the point with shared row timing.
- Major con sparks were replaced by an ember effect. Embers should sit behind the matching sprite/text layers, stay within/clipped to the element or the sprite padded ember field, and be frequent enough to remain visible throughout the highlighted bullet point. Sprite-side embers were boosted to be denser, slightly larger, and brighter. Recent commits: `aee4a8d Sync major con sparks across point`, `8de2ddd Replace major con sparks with embers`, `33ef4bf Place major con embers behind layers`, `927b9ca Increase major con ember frequency`, and `2f81bd6 Boost major con sprite embers`.

- Major pro/con effects were simplified after several messy iterations. Final current intent: no major-pro pulse/twinkle, no major-con pulse/flicker, no separate major-effect overlay/layer, and no broad major-specific text/sprite effects. Pros and cons should stay plain and simple with the shared narration highlight glow.
- Latest major-con request from James: "just add an ember effect to the glow of the major cons". This was implemented as a simple ember addition inside the existing major-con highlight glow, without restoring the separate major-effect system. Latest pushed commit: `be5a28c Add major con ember glow`; immediately before it, `1a8e571 Remove pro con major effects` removed the messy major effects.
- For future pro/con work, treat one bullet point as one unit: bullet sprite, major/minor label, and right-hand info text share the same highlight treatment. Avoid splitting sprite/text into independent effect rules unless James explicitly asks.

- Shared calibrated tier thresholds were implemented and pushed in commit `d2b05e0 Calibrate shared tier thresholds`. FoodRanked now uses one shared display tier map across all food types: `S 80-100`, `A 60-79.9999`, `B 40-59.9999`, `C 20-39.9999`, `D 0-19.9999`. Each ruleset has `scoreCalibration` anchors that map its raw category score onto that shared final scale, and the scorer outputs both `overallScore` and `baseOverallScore` for audit.
- James asked whether tier allocations stayed the same and fair after the shared threshold change. Direct verification showed all `275` foods kept the same allocated benchmark tier (`0` tier allocation mismatches). The calibration matrix still verified every category at `25/25`, with no mismatches. Important framing: score numbers changed, but intended tier placements stayed stable.
- The shared tier calibration work regenerated the calibration matrix, rulesets, episode outputs, leaderboards, batch results, and website data. Verification included `node --check` on touched scripts, `node scripts/verify-narration-subtitles.js`, full `foodranked-score-all`, calibration matrix verification, manifest/scorer alignment with `0` mismatches across `158` episode manifests, and `git diff --check`.
- Outro final score graded glow was implemented and pushed in commit `8c88afd Add graded outro score glow`. The final score reveal now gets a highlighted glow based on grade: D red, C/B blend through amber toward green, A green, and S uses the `#00bfa5` teal from the 100% micron bar. Checks passed: `node --check docs/video-builder/video-builder.js`, inline app script syntax check, `node scripts/verify-narration-subtitles.js`, and `git diff --check`.
