# 2026-06-06

- Recent video/display builder D-tier outro work was committed and pushed through several fixes. Latest intended state is commit `99d1b47 Fit builder canvas inside display size`, which supersedes the earlier `b1dbadd Match video builder shell to full canvas` direction.
- D-tier outro sprite behavior: the uploaded `D tier.png` appears only on the final tier reveal after the outro summary, is centered on the visible phone display, and the spoken/final `D tier` subtitle is suppressed on that reveal beat.
- Centering lesson for the builders: James said the 7/9 preview/display size is the correct visual size; the problem was the authored 135x240 canvas rendering too large compared with Aseprite. Future centering/layout fixes should fit the 135x240 stage into the capped display instead of expanding the phone shell to the full oversized canvas.
- Verification already performed for latest builder state: video builder and display builder matched shell content with 0px width/height mismatch across large desktop, normal desktop, and laptop-ish viewports; subtitle verifier passed.
# 2026-06-06

- Recent video/display builder D-tier outro work was committed and pushed through several fixes. Latest intended state is commit `99d1b47 Fit builder canvas inside display size`, which supersedes the earlier `b1dbadd Match video builder shell to full canvas` direction.
- D-tier outro sprite behavior: the uploaded `D tier.png` appears only on the final tier reveal after the outro summary, is centered on the visible phone display, and the spoken/final `D tier` subtitle is suppressed on that reveal beat.
- Centering lesson for the builders: James said the 7/9 preview/display size is the correct visual size; the problem was the authored 135x240 canvas rendering too large compared with Aseprite. Future centering/layout fixes should fit the 135x240 stage into the capped display instead of expanding the phone shell to the full oversized canvas.
- Verification already performed for latest builder state: video builder and display builder matched shell content with 0px width/height mismatch across large desktop, normal desktop, and laptop-ish viewports; subtitle verifier passed.

- Later builder sizing correction: after the `99d1b47` direction, James said the fix was getting worse and asked for the original display look. Commit `f7cb4e8 Restore original builder display sizing` restored the pre-experiment display sizing while keeping D-tier reveal/centering work.
- Latest intended builder state is now commit `cd30f81 Match builder canvas to display frame`, pushed after James asked to match the canvas size to the display size. It keeps the original display frame sizing, separates frame scale from canvas scale, and makes the border a visual overlay so the authored 135x240 canvas matches the visible display frame instead of sitting larger behind it.
- Live GitHub Pages verification for `cd30f81`: video builder and display builder both reported 0px delta for canvas/display width, height, and center; D-tier sprite still appears on the final reveal centered with no D-tier subtitle; no browser errors; `node --check docs/video-builder/video-builder.js`, `node scripts/verify-narration-subtitles.js`, and `git diff --check` passed.

- Later builder sizing correction after `cd30f81`: James clarified he wanted the display restored as the crop/target size, not the display enlarged to the canvas. Commit `5360d62 Restore canvas crop display behavior` reversed the frame-scale experiment, then `73ce40b Set builder canvas to display size` set the canvas/stage to the visible display size of `415.125 x 738`.
- Latest intended builder state is commit `7b030a7` after `ce5b04a Keep sprite scale with resized canvas`: canvas/display remain `415.125 x 738`, sprite scale stays at the previous larger visual size (`pixelUnit` about `3.953571`, not the smaller canvas unit), all sprite layers are centered against the resized canvas, D-tier stamp remains centered on final reveal, and D-tier subtitle stays hidden.
- Verification for latest pushed state: JS syntax check, `scripts/verify-narration-subtitles.js`, `git diff --check`, and live GitHub Pages measurements passed after centering the sprites.
