
## 2026-06-07 10:16 UTC - Video builder stamp animation state

Recent pushed work changed `docs/video-builder` stamp/sparkle behavior through commits `c666e51`, `a12e2b9`, `eb6cd49`, `d32d279`, `a2b818a`, `239a140`, and `3a3ae0d` on `main`.

Durable current behavior:
- Intro/outro stamp animations are heavier and more prominent, with entry-only pulse and no ongoing main stamp animation after settling.
- Screen shake happens after the stamped sprite has landed, not before. Intro `RANKED`, outro tier stamp, and the bacon/food sprite all get post-land shake.
- Bacon/food sprite stamp appears quicker; final known reveal window was shortened to about `0.22s`.
- `RANKED` sprite sparkle effect uses exactly 5 glimmer marks with extra sparkly glow.
- Verified during the work with `node --check docs/video-builder/video-builder.js` and Playwright render/style checks.

## 2026-06-07 14:52 UTC

- Implemented split narration workflow for FoodRanked: `scripts/foodranked-generate-voice.js` supports `--split-blocks`; `scripts/foodranked-align-subtitles.js` detects `voice-v7-blocks.json`, aligns blocks separately, and stitches word timings into one timeline.
- Generated and committed bacon `voice-v7` as 11 separate narration MP3 blocks, with updated subtitles/episode timing and video builder preference for `voice-v7 split`; `voice-v6` remains the single-audio fallback.
- Added video timing behavior: audio defaults on in the builder, old saved `audioEnabled:false` state is cleaned up, and each section gets a `0.5s` dwell except the final outro. Bacon currently reports `84.8s + 4.0s dwell`.
- Fixed follow-up audio default issue by bumping GitHub Pages cache-busters, preserving audio through split-audio source swaps, and only switching audio off for genuine browser permission blocks. Browser probe confirmed audio stayed on through every section.
- Added top-level reusable audio source folders: `audio/sfx/stamps`, `audio/sfx/sparkles`, `audio/sfx/transitions`, `audio/sfx/ui`, `audio/music`, and `audio/narration`. Added `docs/audio/sfx` mirror for browser-loadable SFX. Generated narration still lives under `production/episodes/<food>/voice` and browser episode audio under `docs/audio/episodes/<food>`.
- Schema/docs were updated to capture narration blocks, audio manifests, forced alignment, dashboard data, stamp/glimmer effects, current video format behavior, audio-on default, section dwell rule, and audio asset locations.
- Recent pushed commits from the work: `8c3a5fe` split narration workflow, `29b6cfc` split narration schema and bacon take, `6cba751` default audio on and section dwell, `2fd9abe` builder audio persistence and audio asset folders.
