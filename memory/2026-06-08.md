
## 2026-06-08 - Video Builder Sound State

- Stamp SFX in `docs/video-builder/video-builder.js` was tuned through several pushed commits and is intentionally very soft under narration. Current target: base volume `0.18`, randomized volume roughly `0.155-0.205`, playbackRate/pitch randomized `0.93-1.07`, `preservesPitch` disabled, and randomized sample start offset up to `45ms`. Latest stamp-related commit pushed: `54df03d Make stamp SFX extra soft`.
- User added a UI sound on GitHub for the highlighted glow. It was pulled/mirrored into `docs/audio/sfx/ui/` and wired into the video builder as a quiet looping glow bed that follows the strongest active narration-highlight glow and ramps volume smoothly in/out with the glow. Current glow loop cap verified at `0.08`; latest commit pushed: `75f4fe8 Add glow highlight loop SFX`.
- Verification for these audio changes included `node --check docs/video-builder/video-builder.js` and Playwright browser probes confirming stamp randomization plus the glow loop loading, looping, and ramping.
