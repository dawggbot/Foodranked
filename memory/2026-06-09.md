## 2026-06-09 03:23 UTC - Pre-compaction flush

Recent FoodRanked production state:

- Glow/highlight SFX work is in good shape and James explicitly said it now looks and sounds good. Relevant pushed commits:
  - `294837a Raise glow highlight SFX volume`: raised glow loop cap from `0.08` to `0.18`.
  - `92af7c2 Vary glow highlight SFX profiles`: raised cap to `0.28`, added per-highlight playback-rate variation, required meaningful delta between adjacent highlights, and disabled pitch preservation so transitions sound different.
  - `e10de67 Tune glow SFX by highlight tone`: raised cap to `0.36`; green/good highlights use higher warmer playback rates around `1.16x-1.42x`; red/bad highlights use deeper less friendly rates around `0.58x-0.82x`; transitions still retune the loop.
- Sprite placement follow-up: commit `80b2bcb Tighten video sprite placement sync` was too broad for highlighted section indicators; James clarified those indicators were fine and wanted protein arrow indicators plus title score plate fixed instead.
- Latest pushed fix is `29fb7ff Align protein arrows and title score plate`:
  - Reversed highlighted section-indicator render behavior back to the prior `x-1,y-1` placement.
  - Locked macro row reveal placement so protein arrow indicators do not drift down during the reveal.
  - Made the video builder use intro/title header chrome for score plate placement across scenes.
- Files involved in recent video-builder placement/audio work included `docs/video-builder/video-builder.js` and `docs/video-builder/index.html`.
- Verification reported after the latest fix: `node --check`, `git diff --check`, Playwright coordinate checks, and a protein screenshot pass. Repo was clean except existing untracked `memory/*.md` files.
## 2026-06-09 03:23 UTC - Pre-compaction flush

Recent FoodRanked production state:

- Glow/highlight SFX work is in good shape and James explicitly said it now looks and sounds good. Relevant pushed commits:
  - `294837a Raise glow highlight SFX volume`: raised glow loop cap from `0.08` to `0.18`.
  - `92af7c2 Vary glow highlight SFX profiles`: raised cap to `0.28`, added per-highlight playback-rate variation, required meaningful delta between adjacent highlights, and disabled pitch preservation so transitions sound different.
  - `e10de67 Tune glow SFX by highlight tone`: raised cap to `0.36`; green/good highlights use higher warmer playback rates around `1.16x-1.42x`; red/bad highlights use deeper less friendly rates around `0.58x-0.82x`; transitions still retune the loop.
- Sprite placement follow-up: commit `80b2bcb Tighten video sprite placement sync` was too broad for highlighted section indicators; James clarified those indicators were fine and wanted protein arrow indicators plus title score plate fixed instead.
- Latest pushed fix is `29fb7ff Align protein arrows and title score plate`:
  - Reversed highlighted section-indicator render behavior back to the prior `x-1,y-1` placement.
  - Locked macro row reveal placement so protein arrow indicators do not drift down during the reveal.
  - Made the video builder use intro/title header chrome for score plate placement across scenes.
- Files involved in recent video-builder placement/audio work included `docs/video-builder/video-builder.js` and `docs/video-builder/index.html`.
- Verification reported after the latest fix: `node --check`, `git diff --check`, Playwright coordinate checks, and a protein screenshot pass. Repo was clean except existing untracked `memory/*.md` files.

## 2026-06-09 05:58 UTC - Pre-compaction flush

Recent FoodRanked video-builder state since the prior flush:

- Pushed `a3c263f Restore video builder glow while using display placements` after James clarified only sprite placement needed changing. This reversed broad sprite-lock behavior, restored macro arrow/highlight glow behavior, kept display-builder placement sync, and bumped the video-builder cache key.
- James then saved the display layout locally and updated the schema JSON. A follow-up check found no code changes needed: saved `foodranked-display-builder-v4` layout stamped `20260529-layout-sync-v1` synced correctly to repo version `20260609-display-builder-placement-v2`; Protein scene rendered cleanly with 47 layers, display-builder sprite placement, arrow glow filters, and no console/page errors.
- Pushed `56bc5be Step pros cons glow between highlighted rows` to make Pros/Cons glow move row-to-row. James then asked to keep the audio change but revert visuals.
- Latest pushed Pros/Cons fix is `caa1864 Keep pros cons glow visuals while stepping audio`: Pros/Cons visuals are back to old cumulative behavior where earlier rows stay lit as later rows light, while audio stepping remains separated via `cueStrength` so the glow cue can still retune per active pro/con.
- James asked to use an uploaded transition AFX as a section transition sound. Multiple searches did not find any new audio attachment in the repo, `/home/idk/.openclaw`, `/root/.openclaw`, `/root/.openclaw/media`, `/tmp`, `/var/tmp`, or OpenClaw session metadata/history. No code changes were made for this yet. Suggested target path if the file appears: `audio/sfx/transitions/section-transition-afx.mp3`, then mirror into `docs/audio/sfx/transitions/`, wire section-boundary playback, verify, commit, and push.
