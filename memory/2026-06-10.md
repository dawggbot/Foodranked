# 2026-06-10

- Video builder section transition SFX is wired from `audio/sfx/transitions/oxidvideos-transition-sfx-2-409073.mp3`, mirrored for docs at `docs/audio/sfx/transitions/oxidvideos-transition-sfx-2-409073.mp3`, and plays on section boundaries at volume `0.22`. Pushed in commit `58ed859`.
- James uploaded macro bar placement sprites for the display builder: `macro_bar_frame.png` and `fat_macro_bar_fill.gif`. They are for display-builder placement only for now; no video-builder macro bar logic has been added yet. Display-builder sprite-library sizing should match existing sprite behavior: source `104x17`, newly added layers save around `78x13` via the normal `0.75x` drop scale. Pushed in commits `75f51a7` and `5a60875`.
- Display builder laptop UI was improved so the right inspector panel scrolls instead of overlapping controls. Selected-layer z-order controls (`Forward` / `Backward`), z-index, and visibility sit near the top. The selected-layer delete button was removed; layer deletion is mapped to keyboard `Delete`, with `Backspace` also accepted. Verified at `1180x760` with Playwright and pushed in commit `6cb38da`.
# 2026-06-10

- Video builder section transition SFX is wired from `audio/sfx/transitions/oxidvideos-transition-sfx-2-409073.mp3`, mirrored for docs at `docs/audio/sfx/transitions/oxidvideos-transition-sfx-2-409073.mp3`, and plays on section boundaries at volume `0.22`. Pushed in commit `58ed859`.
- James uploaded macro bar placement sprites for the display builder: `macro_bar_frame.png` and `fat_macro_bar_fill.gif`. They are for display-builder placement only for now; no video-builder macro bar logic has been added yet. Display-builder sprite-library sizing should match existing sprite behavior: source `104x17`, newly added layers save around `78x13` via the normal `0.75x` drop scale. Pushed in commits `75f51a7` and `5a60875`.
- Display builder laptop UI was improved so the right inspector panel scrolls instead of overlapping controls. Selected-layer z-order controls (`Forward` / `Backward`), z-index, and visibility sit near the top. The selected-layer delete button was removed; layer deletion is mapped to keyboard `Delete`, with `Backspace` also accepted. Verified at `1180x760` with Playwright and pushed in commit `6cb38da`.
- Display/header update pushed in commit `af4be25`: locked food-name header now renders uppercase in both the display builder and video builder. Repo default display JSON was aligned so bacon displays as `BACON`. Added a `Display JSON` tab in the display builder, and a `Display JSON` tab in Studio food pages that shows saved local display layout or repo default.
- Fats macro reveal update pushed in commit `25d8a76`: in the video builder, the fats icon plus macro bar frame/fill reveal together first; the fill animates using the food type's min/max range ratio; submacros reveal only after the fill animation finishes. Repo default display layout now includes the fats bar frame/fill, while saved placement JSON can still override positions. Verified with Playwright timing: icon/frame/fill start `0.08s`, first submacro start `0.94s`, fill gate `0.72s`.

- Fats reveal follow-up pushed in commit `89dbf23`: the video builder revealed the fat amount with the submacros after the macro bar fill, and temporarily used a data-derived meat fats max of `44g` from pepperoni. This was superseded by the schema-range fix in `64c7c33`.
- Display macro ranges are now sourced from shared schema code, not food data maxima. Commit `64c7c33` added `docs/app/display-schema.js`; the video builder, display builder, and Studio rules page read the same display macro min/max ranges from it. Schema version verified as `20260610-display-schema-v1`.
- Current schema display range for meat fats is `2 to 35g`. Bacon at `37.1g` should fill the fats bar to `100%` but must not redefine the scale. Continue treating the schema page/shared display schema as the source of truth for food-type min/max macro bar ranges.
- Current fats animation contract: fats icon plus bar frame/fill reveal together first; the fill animation must finish before submacros appear; the fat amount (for example `37.1g`) reveals with the submacros after the bar fill.
