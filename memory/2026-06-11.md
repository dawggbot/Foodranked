
## 2026-06-11 01:44 UTC

- James corrected the fats macro bar animation direction: use the supplied `fat_macro_bar_fill.gif` sprite only; do not invent or draw a custom fill asset for this bar.
- Current macro bar fill implementation was pushed in commit `1cede94` (`Stabilize macro bar GIF fill animation`): the video builder decodes frames from `fat_macro_bar_fill.gif` and draws them onto one stable canvas layer, avoiding `clip-path`, custom fill art, and flickery per-frame `img.src` swapping.
- Current reveal timing intent for fats: icon, bar, and `fats` label reveal first; the bar fill completes before submacros reveal; the numerical fat value reveals together with the submacros. The fill is about `1.35s` and should feel faster at the start, easing into the destination.
- Latest browser verification after `1cede94`: bar progressed smoothly to full width and no console errors were seen.
