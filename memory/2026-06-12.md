# 2026-06-12

- Display builder/video builder protein macro fill: use James's uploaded GIF asset at `sprites/macros_section/section_3_protein/protein_macro_bar_fill.gif`, with browser-served copy at `docs/app/sprites/macros_section/section_3_protein/protein_macro_bar_fill.gif`. Do not use the old placeholder `docs/app/sprites/macros/protein/protein_bar_fill.svg`; it was deleted. Stale SVG/GIF placeholder paths are migrated to the uploaded GIF.
- Display builder selected-layer `Delete` is intended to remove the currently selected sprite/text box, not just saved presets. Deleted generated layers must stay deleted after render and reload.
- Recent pushed fix: `f902d46 Use uploaded protein fill gif and persist layer deletes` verified in Playwright for GIF rendering, stale path migration, sprite deletion, and text-box deletion persistence.

- Pushed `5a1413f Allow dragging protein bar fill after placeholder delete`: library-dropped protein bar fill layers keep their unique `lib_...` ids so they are not filtered by deleted generated-layer ids. Verified drag/drop and click-add both create editable layers using `./sprites/macros_section/section_3_protein/protein_macro_bar_fill.gif`.
- Pushed `9039839 Fix macro arrows and frame reveal`: display builder macro arrow row detection should ignore macro icons, bar fills, and bar frames so arrows bind to the real submacro rows; video builder macro bar frames now use the same reveal class/timing behavior as macro icons. Cache tag was bumped to `20260612-arrow-frame-reveal-v1`; syntax checks, browser submacro arrow checks, and video sprite diagnostics passed.
