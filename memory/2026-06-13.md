
## 2026-06-13 07:40 UTC - Video Builder Macro Bar Fill Audio/Timing

- Recent macro bar-fill tuning work was completed and pushed through commit `602f460 Slow final quarter of macro bar fill` after earlier commits including `0c01c02 Make macro bar fill SFX proportional`.
- Current intended behavior in `docs/video-builder/video-builder.js`: each macro bar dwells on the first frame for about `0.5s`, then the visual fill and bar-fill SFX start together.
- Full-bar timing is anchored to bacon fats: a 100% bar uses the full canonical fill path/SFX duration, currently `1.55s` after the dwell. Partial bars use the same path but stop early in proportion to the filled amount.
- James specifically wants all macro bar-fill SFX to sound the same across fill amounts; a 50% and 80% fill should share the same beginning of the audio, with the shorter one simply cut off earlier. Do not choose unique source offsets or unique playback rates per food/macro for this effect unless he asks for that again.
- The visual bar fill should not speed up at the start. Only the final quarter of the actual target bar should progressively slow down, reaching slowest at the end. For example: a 100% bar slows from 75%-100%, an 80% bar slows from 60%-80%, and a 50% bar slows from 37.5%-50%.
- During that final-quarter visual slowdown, the audio clip should not be time-stretched or pitch-shifted to fit; instead, play more of the same SFX clip at normal rate to cover the extra visual duration.
- James's current taste notes for this SFX: keep it softer, smooth/seamless at the start and end, aligned with the fill motion after the dwell, and avoid the previous "alien" pitch-sweep sound.

## 2026-06-13 10:11 UTC - Video Builder Audio/Reveal Follow-Ups

- Later macro bar-fill work after the earlier timing note was completed and pushed through commit `3ec8eb7`.
- James liked the smoother macro bar-fill SFX after commit `45e5f35`: lower gain, lower low-pass cutoff around `3600Hz`, reduced filter sharpness, and slightly rounded fades. Keep future tweaks in that softer/smoother direction unless he asks for sharper audio.
- The actual bar-fill SFX asset was trimmed from `15.048s` to `9.408s` so the file now contains only the useful full-bar slice that the builder uses. The corresponding builder duration constant was updated.
- Macro label/title, macro icon, bar frame, and bar fill should reveal as one composited group. The final preferred approach is the grouped macro-head reveal from commit `f417a75`, with the icon drawn on top inside the group so lower bar pixels do not show through during fade-in.
- Avoid reintroducing the broad macro-icon footprint crop/clip from commit `42fe2a5`; James said it cut off too much. The desired effect is only normal sprite layering/occlusion where the actual sprites overlap.
- Confirm tap audio was added to docs audio assets and wired to vitamin/mineral DV bar graph reveals. It should play one tap per revealed graph level, with pitch progressively rising from `10%` as the lowest-sounding tap to `100%` as the highest-sounding tap.
- Checks used for this latest set of changes: `node --check docs/video-builder/video-builder.js` and `git diff --check -- docs/video-builder/video-builder.js docs/video-builder/index.html`.

## 2026-06-13 14:01 UTC - Video Builder Micron Bar Confirm/100% Firework SFX

- Vitamin/mineral confirm tap behavior was updated and pushed in commit `6974130`: each vitamins/minerals section now plays a rising tap sequence from `10%` up to the highest visible graph value, instead of only one detected tap. A highest value of `60%` produces 6 taps; `100%` produces all 10.
- The confirm tap SFX was renamed and pushed in commit `aac0a9b`: current path is `audio/sfx/ui/micron-bar-confirm-tap.mp3`, mirrored in `docs/audio/sfx/ui/micron-bar-confirm-tap.mp3`. The video builder loads this filename.
- A pixel firework VFX for `100%` vitamin/mineral DV graph bars was iterated through commits `9697321`, `072c103`, `7dc7ebc`, `613354c`, and `99abfed`. Final preferred behavior: only actual visible `100%` micron bar sprites trigger it; it should be clearly visible but without rectangular glowy halo boxes. The center pop should stay small/faint, with the surrounding square sparks carrying the effect.
- Commit `7dc7ebc` switched firework targeting away from flaky micron reveal classification and onto the actual visible `100%` bar sprites. Keep that direct-bar targeting if future timing/visibility tweaks are needed.
- The new firework sparkle SFX was wired and pushed in commit `c9b64d5 Add micron firework SFX`. Current builder constants: `MICRON_100_FIREWORK_SFX_PATH = 'audio/sfx/sparkles/freesound_community-firework-cluster-90480.mp3'`, volume `0.28`, pool size `2`, build id `20260613-micron-100-firework-sfx-v1`.
- James liked the final visible 100% effect after the halo removal and fainter center pop, and then asked for the sparkle-folder SFX to play with it. Avoid bringing back the glowy box/halo look.
