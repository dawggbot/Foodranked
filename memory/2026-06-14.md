# 2026-06-14 Memory

## Major pro sparkle VFX work

James iterated on a new twinkly/sparkly VFX for major pro reveals in the video builder. Current intended behavior:

- Sparkles fire for `contextItems.pros[*].impactLevel === "major"` only.
- Sparkles play at the same time as the highlighted glow state on the major pro row.
- Sparkles cover the whole pro row area, including bullet/sprite, impact label, and main pro text.
- The earlier glowy box/core overlay was removed and should stay gone.
- Sparkles render behind the main pro text so the text stays readable.
- James repeatedly asked for them to be more prominent; latest state is intentionally dense/bright: 14 particles, larger size, stronger glow, peak opacity near full strength.
- Verified during the work with `node --check docs/video-builder/video-builder.js` and Playwright scrub checks on Orange's Pros scene.

Relevant pushed commits in order:

- `b2a5499 Add sparkle VFX for major pro reveals`
- `e5decbe Sync major pro sparkles with highlight glow`
- `9b9dc6b Soften major pro sparkle VFX`
- `8052a64 Boost major pro sparkle visibility`
- `820603e Layer major pro sparkles behind text`
- `0cffaee Increase major pro sparkle prominence`
- `7bb0261 Make major pro sparkles denser`

Files involved:

- `docs/video-builder/video-builder.js`
- `docs/video-builder/video-builder.css`
- `docs/video-builder/index.html`

If James asks for further tuning, preserve these constraints unless he says otherwise: no glowy box, behind text, synced with highlight, across the whole major pro row, major pros only.

## Major con siren VFX and SFX work

James iterated on a new emergency-siren style VFX for major con reveals in the video builder. Current intended behavior:

- Siren VFX fires for major cons only, following the same major-only row logic and timing as the major pro sparkle VFX.
- It appears during the active highlight glow on the major con row and renders behind the con text so the text stays readable.
- The visual should be warm red only: no blue.
- The circle/pip particles were removed and should stay gone unless James asks to reintroduce them.
- Current coverage is intentionally tight, not wide: beam-only effect with 3 beams per active row, smaller row spread, smaller beam height, and less horizontal scale than the widened version.
- Edges should remain smooth/blurred with gradient softness, with no sharp bars.

Major con siren audio state:

- James uploaded `floraphonic-buzzer-4-183895.mp3` to the GitHub sparkles folder.
- It was pulled, renamed to `major-con-siren-buzzer.mp3`, mirrored into `docs/audio/sfx/sparkles/`, and wired into the major-con siren reveal with its own SFX pool and playback-rate variation, like the major-pro sparkle SFX.
- Playwright preview confirmed `major-con-siren-buzzer.mp3` is requested during Bacon's major-con reveal.

Relevant pushed commits in order:

- `533cf3d Add siren VFX for major cons`
- `81aa9af Soften major con siren VFX`
- `ecc4d70 Make major con siren beam-only`
- `767b78f Tighten major con siren coverage`
- `0429843 Add SFX for major con siren`

Related major pro audio work from the same iteration:

- `benkirb-shine-6-268910.mp3` was renamed to `major-pro-sparkle-shine.mp3`.
- `freesound_community-firework-cluster-90480.mp3` was renamed to `micron-100-firework-cluster.mp3`.
- `major-pro-sparkle-shine.mp3` is mirrored in `docs/audio/sfx/sparkles/` and synced to the same narration-highlight start as the major pro sparkle VFX.
- Relevant commit: `dfb2a3f Add SFX for major pro sparkles`.

## Major section SFX current state

After James said the major one-shot SFX were too loud, the loudness bump from `042f9c3` was reverted/tuned in pushed commit `1b7f1f8 Lower major SFX volume and vary pitch`.

Current intended audio behavior:

- Major pro sparkle one-shot volume: `0.24`.
- Major con siren one-shot volume: `0.20`.
- Both major SFX use the same playback-rate modifier ranges as the current highlight glow logic.
- Both disable pitch preservation before randomized playback-rate changes, so the pitch/rate variation is more audible.
- Browser verification used Butter and confirmed 3 pro + 3 con plays at the quieter volumes, randomized rates in the expected highlight ranges, and `preservesPitch: false`.

If James asks for more SFX tuning, preserve the quieter major-SFX volume baseline unless he explicitly wants another volume lift.

## Audio folder organization

Section-specific SFX were reorganized and pushed in commit `445cd3f Organize section SFX folders`.

Current folder layout for section SFX:

- `audio/sfx/sections/pros/`
- `audio/sfx/sections/cons/`
- `audio/sfx/sections/microns/`
- `audio/sfx/sections/macros/`

The same structure is mirrored under `docs/audio/sfx/sections/`. Builder paths, cache tags, and audio docs were updated. The old `sparkles/` grouping for section-specific SFX should be considered obsolete; future section SFX should go into the specific section folder.

Verification included `node --check`, `git diff --check`, mirror comparisons, stale-path searches, and a browser smoke test confirming the new section paths load and playback requests use them.

## Finalisation sample food batch

James said FoodRanked was moving into finalisation and asked for sample foods covering at least one of each food type, like Bacon. This was completed and pushed in commit `fd2f682 Add finalisation sample food batch`.

The batch covers all 11 food types:

- Kale
- Raspberries
- Oats
- Black Beans
- Sweet Potato
- Almonds
- Chia Seeds
- Bacon
- Greek Yogurt
- Extra Virgin Olive Oil
- Regular Cola

Key files added/updated:

- `config/finalisation-sample-foods.v1.json`
- `production/FINALISATION-SAMPLE-FOODS.md`
- `outputs/batches/finalisation-sample-foods-v1/README.md`
- `production/foods/<food-id>/voice/final-narration.txt` drafts for the new sample foods, following the Bacon-style production narration workflow.

Existing Bacon, Extra Virgin Olive Oil, and Regular Cola narration files were left in place. Checks passed: finalisation batch generation reported `11 succeeded, 0 failed`; `node scripts/verify-narration-subtitles.js`; and the final coverage check reported `11/11 food types`.

## Protein scoring and sample data cleanup

Latest pushed commit from the finalisation cleanup: `24fe8a8`.

Durable scoring rule change:

- Protein amino-acid / protein-quality points only count when the food has a useful protein amount.
- Low-protein foods should fall back to protein-amount messaging instead of earning credit just because trace amino acids are present.

Durable pro/con data direction:

- Avoid generic cons like "not a complete food" unless James explicitly asks for that style again.
- Keep the locked exactly 3 pros + 3 cons format.
- Make pros/cons food-specific and source-aligned.
- Beetroot should carry the nitrate / nitric-oxide angle.

Updated/generated surfaces from that work included the finalisation sample batch, production narration drafts, site data, and builder display logic. Verification passed with the scorer, script checks, narration/subtitle checks, and `git diff --check`. Working tree was clean except existing untracked `memory/*.md` files, which were intentionally left alone.
