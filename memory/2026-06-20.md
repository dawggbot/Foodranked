# 2026-06-20

- Pushed `c1f8775` (`Enforce arrows for numeric submacros`): every numeric submacro must resolve to an arrow band; only true `N/A` values stay neutral/no-arrow. Updated all 11 food-type rulesets, re-scored all 274 foods, regenerated dashboard/finalisation sample data, and regenerated the tracked episode outputs. Kale now shows arrows for saturated fat, omega 3, polyunsaturated fat, starch, fibre, sugar, glycemic index, and protein amount.
- Pushed `39369dc` (`Tighten protein gate and layout sync`): amino acids now count toward protein quality only when the food has a useful protein amount for its type; otherwise protein scoring falls back to plain protein amount. This prevents low-protein foods/grains from inflating to high amino-acid scores just because amino acids are present.
- Pros/cons generation was tightened: generic filler like `not a complete food on its own` is blocked going forward; beetroot uses nitrate/nitric-oxide-specific benefit language, with a guard against incorrect `nitrous oxide` wording.
- Food type text placement from the first section now syncs across all sections in both the display builder and video builder. Vegetable display mapping remains `VEG`.
- Verification after `39369dc`: data quality audit had 0 errors, subtitle verification passed, JS syntax passed, `git diff --check` passed, and strict arrow/protein gate check found 0 missing arrows and 0 ineligible protein-quality rows counted.
# 2026-06-20

- Pushed `c1f8775` (`Enforce arrows for numeric submacros`): every numeric submacro must resolve to an arrow band; only true `N/A` values stay neutral/no-arrow. Updated all 11 food-type rulesets, re-scored all 274 foods, regenerated dashboard/finalisation sample data, and regenerated the tracked episode outputs. Kale now shows arrows for saturated fat, omega 3, polyunsaturated fat, starch, fibre, sugar, glycemic index, and protein amount.
- Pushed `39369dc` (`Tighten protein gate and layout sync`): amino acids now count toward protein quality only when the food has a useful protein amount for its type; otherwise protein scoring falls back to plain protein amount. This prevents low-protein foods/grains from inflating to high amino-acid scores just because amino acids are present.
- Pros/cons generation was tightened: generic filler like `not a complete food on its own` is blocked going forward; beetroot uses nitrate/nitric-oxide-specific benefit language, with a guard against incorrect `nitrous oxide` wording.
- Food type text placement from the first section now syncs across all sections in both the display builder and video builder. Vegetable display mapping remains `VEG`.
- Verification after `39369dc`: data quality audit had 0 errors, subtitle verification passed, JS syntax passed, `git diff --check` passed, and strict arrow/protein gate check found 0 missing arrows and 0 ineligible protein-quality rows counted.
- Pushed `ef43bec` (`Fix display builder protein submetrics`): display builder protein submetrics now use `collagen`, `EAA`, `NEAA`, and `bioavailability` instead of total protein leaking into collagen; stale/manual protein values refresh from food data. Kale shows protein `4.3g`, collagen `N/A`, EAA `0/9`, NEAA `0/10`, and bioavailability `N/A` because USDA `FDC 168421` supports amino acids but not a defensible exact bioavailability percent.
- Kale food image was implemented for the display builder: uploaded `kale.png` from remote was mirrored into `docs/app/sprites/header/food_images/`, the food index was regenerated, and Playwright verified the Kale protein values plus image render correctly. Verification also included data audit 0 errors, score-all `274/274` ok, narration/subtitle check ok, and no video-builder browser errors.

- Pushed `2847be8` (`Preserve display builder layout placements`): restored layout compatibility to `20260529-layout-sync-v1` so saved sprite/text placements would stop being treated as stale; did not intentionally change sprite/text x/y placements. Protein bioavailability display now falls back to `0%` when protein grams are present and bioavailability is missing.
- Pushed `c980f72` (`Restore builder layout placements`): forced bad old saved browser placement data to be ignored so bar fill/frame and other defaults return to committed layout; removed automatic food-type text placement sync across sections because it risked moving text boxes. Standing preference from James: never add, move, resize, edit, or place canvas sprites unless he directly asks for sprite/canvas placement work.
- Pushed `d9d8c33` (`Wire reuploaded D tier sprite`): mirrored James's reuploaded root D-tier asset to `docs/app/sprites/ui/intro_&_outro/D_tier.png`, pointed the video builder at `D_tier.png`, and verified a D-tier Bacon outro loads it at `50x50`.
- Current display/scoring notes after `d9d8c33`: macro bar fill/frame repo defaults were verified unchanged at `x:31, y:48, width:88, height:14`; Bio-A `0%` displays three red-arrow indicators in clean display-builder state and video-builder reveal layers; the `0%` fallback is display-only and does not affect food performance scoring. Kale's score drop came from tightened amino-acid/protein gating, not layout or display changes: Kale moved from about `82.1 / S` with protein score `80` to about `65.4 / A` with proteins `0` under the tightened rules.
# 2026-06-20

- Pushed `c1f8775` (`Enforce arrows for numeric submacros`): every numeric submacro must resolve to an arrow band; only true `N/A` values stay neutral/no-arrow. Updated all 11 food-type rulesets, re-scored all 274 foods, regenerated dashboard/finalisation sample data, and regenerated the tracked episode outputs. Kale now shows arrows for saturated fat, omega 3, polyunsaturated fat, starch, fibre, sugar, glycemic index, and protein amount.
- Pushed `39369dc` (`Tighten protein gate and layout sync`): amino acids now count toward protein quality only when the food has a useful protein amount for its type; otherwise protein scoring falls back to plain protein amount. This prevents low-protein foods/grains from inflating to high amino-acid scores just because amino acids are present.
- Pros/cons generation was tightened: generic filler like `not a complete food on its own` is blocked going forward; beetroot uses nitrate/nitric-oxide-specific benefit language, with a guard against incorrect `nitrous oxide` wording.
- Food type text placement from the first section now syncs across all sections in both the display builder and video builder. Vegetable display mapping remains `VEG`.
- Verification after `39369dc`: data quality audit had 0 errors, subtitle verification passed, JS syntax passed, `git diff --check` passed, and strict arrow/protein gate check found 0 missing arrows and 0 ineligible protein-quality rows counted.
# 2026-06-20

- Pushed `c1f8775` (`Enforce arrows for numeric submacros`): every numeric submacro must resolve to an arrow band; only true `N/A` values stay neutral/no-arrow. Updated all 11 food-type rulesets, re-scored all 274 foods, regenerated dashboard/finalisation sample data, and regenerated the tracked episode outputs. Kale now shows arrows for saturated fat, omega 3, polyunsaturated fat, starch, fibre, sugar, glycemic index, and protein amount.
- Pushed `39369dc` (`Tighten protein gate and layout sync`): amino acids now count toward protein quality only when the food has a useful protein amount for its type; otherwise protein scoring falls back to plain protein amount. This prevents low-protein foods/grains from inflating to high amino-acid scores just because amino acids are present.
- Pros/cons generation was tightened: generic filler like `not a complete food on its own` is blocked going forward; beetroot uses nitrate/nitric-oxide-specific benefit language, with a guard against incorrect `nitrous oxide` wording.
- Food type text placement from the first section now syncs across all sections in both the display builder and video builder. Vegetable display mapping remains `VEG`.
- Verification after `39369dc`: data quality audit had 0 errors, subtitle verification passed, JS syntax passed, `git diff --check` passed, and strict arrow/protein gate check found 0 missing arrows and 0 ineligible protein-quality rows counted.
- Pushed `ef43bec` (`Fix display builder protein submetrics`): display builder protein submetrics now use `collagen`, `EAA`, `NEAA`, and `bioavailability` instead of total protein leaking into collagen; stale/manual protein values refresh from food data. Kale shows protein `4.3g`, collagen `N/A`, EAA `0/9`, NEAA `0/10`, and bioavailability `N/A` because USDA `FDC 168421` supports amino acids but not a defensible exact bioavailability percent.
- Kale food image was implemented for the display builder: uploaded `kale.png` from remote was mirrored into `docs/app/sprites/header/food_images/`, the food index was regenerated, and Playwright verified the Kale protein values plus image render correctly. Verification also included data audit 0 errors, score-all `274/274` ok, narration/subtitle check ok, and no video-builder browser errors.

- Pushed `2847be8` (`Preserve display builder layout placements`): restored layout compatibility to `20260529-layout-sync-v1` so saved sprite/text placements would stop being treated as stale; did not intentionally change sprite/text x/y placements. Protein bioavailability display now falls back to `0%` when protein grams are present and bioavailability is missing.
- Pushed `c980f72` (`Restore builder layout placements`): forced bad old saved browser placement data to be ignored so bar fill/frame and other defaults return to committed layout; removed automatic food-type text placement sync across sections because it risked moving text boxes. Standing preference from James: never add, move, resize, edit, or place canvas sprites unless he directly asks for sprite/canvas placement work.
- Pushed `d9d8c33` (`Wire reuploaded D tier sprite`): mirrored James's reuploaded root D-tier asset to `docs/app/sprites/ui/intro_&_outro/D_tier.png`, pointed the video builder at `D_tier.png`, and verified a D-tier Bacon outro loads it at `50x50`.
- Current display/scoring notes after `d9d8c33`: macro bar fill/frame repo defaults were verified unchanged at `x:31, y:48, width:88, height:14`; Bio-A `0%` displays three red-arrow indicators in clean display-builder state and video-builder reveal layers; the `0%` fallback is display-only and does not affect food performance scoring. Kale's score drop came from tightened amino-acid/protein gating, not layout or display changes: Kale moved from about `82.1 / S` with protein score `80` to about `65.4 / A` with proteins `0` under the tightened rules.
- Pushed `2fe4666` (`Fix protein fallback and macro bar placement sync`): fixed the amino-acid fairness bug where meat `collagen_g` rows could block the plain protein fallback when amino data was missing. Turkey and tuna now score protein `80`; salmon still uses its real amino profile; Kale remains about `65.4 / A` with protein section `0` because its source-backed amino acids clear `0/9` EAA and `0/10` NEAA.
- Builder placement rule after `2fe4666`: Bacon fats saved bar fill/frame placement propagates only to existing macro bar fill/frame layers across fats, carbs, and protein. The automatic broad fats-to-carbs/protein layout sync was removed so text boxes and unrelated sprites do not get moved by that path.
- James reiterated a hard workflow preference: do not touch sprite or textbox placements, and do not add anything to or remove anything from the canvas, unless he directly asks for that exact canvas/sprite/textbox placement work.
- Verification after `2fe4666`: test pack, syntax checks, and Playwright browser checks passed for macro bar placement and Kale image wiring.
