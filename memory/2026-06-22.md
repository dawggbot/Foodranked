# 2026-06-22

- Layout/display builder controls incident: James reported sprites could not reliably be added to the canvas or moved with arrow keys. Initial fix commit `c705b7b` changed `docs/app/index.html` and layout-builder controls but caused section indicator sprites to be messed up; final pushed head after cleanup was `e18038d`.
- James asked to reverse that work and copy the drag/drop and arrow nudge behavior from the display builder. Final resolution pushed as commit `d734470`: reversed the previous `docs/app/index.html` control/classification changes, restored section indicator behavior, and changed `docs/layout-builder/layout-builder.js` so the layout builder no longer binds its custom drag/drop shim or custom arrow-key nudge handler. It now relies on the embedded display builder's native sprite drag/drop and arrow nudge controls.
- Verification for `d734470`: Playwright passed for layout-builder native click-add, drag-add, arrow nudge, and intact 9 section indicators; direct display-builder click-add and arrow nudge still worked; `git diff --check` was clean.

- James clarified the display builder and layout builder must remain separate. Do not link their layout storage or make display builder depend on layout-builder state; layout builder is only a test surface for now, and display builder should remain the fallback/stable builder.
- Follow-up control/layout fix pushed as commit `2eaae82`: display builder and layout builder use separate storage keys, section indicators were restored as the canonical 9-dot strip on both builders, section indicator sprites were removed as draggable library items so they cannot corrupt the managed strip, shared app controls handle sprite drag/drop in both surfaces, arrow nudge still works, and layout builder storage no longer mutates display builder storage.
- Section indicator/schema fix pushed as commit `b81b334`: section indicators now use the machine schema geometry from `templates/visual-template.v1.json`; the stale schema value of 7 dots was corrected to locked 9 dots; schema placement converts to builder grid `x 42.875 -> 88.875`, `y 213.75`, size `3.25`; display builder, layout builder default layout, video builder, studio cache-busts, and visual spec wording were updated.
- Arrow indicator drag/drop fix in `b81b334`: manually dragged green/red arrow sprites stay manual, visible, and retain their color instead of being hijacked by macro scoring sync. In layout-builder mode only, hidden scoring arrows render as faint editable ghosts so their positions can be dragged even when the selected food hides them.
- James confirmed the local environment already has ElevenLabs and USDA API keys; do not assume those keys are missing unless a concrete command/API check fails.
- OpenClaw memory search/indexing was observed failing with OpenAI embeddings error `billing_not_active`; once API billing is enabled, rebuild with `openclaw memory index --force --agent foodranked`.

- Indicator size follow-up pushed as commit `3a5d6ca` (`Restore indicator and arrow sprite sizes`): arrow indicator sprites now stay at their original `7x14` size when loaded, synced, or dragged from the sprite library. Section indicators are visible again at `10x10`, with highlighted indicators at `12x12`.
- The `3a5d6ca` fix also prevents saved deletion metadata from removing required section indicator slots; display builder and layout builder remain separate with no layout sharing. Video builder previews were updated separately to use the restored indicator sizes.
- Verification for `3a5d6ca`: `node --check` on changed JS files, `git diff --check`, structured default-layout size checks, and Playwright checks for display builder, layout builder, arrow drag/drop, deleted-indicator recovery, and video preview indicators.
