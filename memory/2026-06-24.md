# 2026-06-24

- Arrow sprite sizing work: final pushed commit `4a98b1c` (`Use default sprite scaling for arrows`) is the intended state. Arrows must not be a special native-size exception; they should use the same default library sprite sizing/drop logic as other sprites (`dropScale: 0.75`). Saved oversized arrow layouts from the earlier `7x14` attempt are migrated down to `5x11`, and builders use cache key `20260624-arrow-default-scale-v1`.
- Relevant prior arrow commits `965affe` and `cace54d` tried keeping arrows at native `7x14`; those were superseded because James reported the arrows were still way too big and asked to use the same sizing logic as the other sprites.

- Section indicator restoration: final pushed state after James rejected canvas/preview sizing changes is commit `007031f` (`Restore canvas sizing for indicators`). Do not change canvas/preview crop or scale to make indicators visible. Instead, keep the managed section-indicator row inside the existing visible 135x240 layout area and keep recovery logic so old saved layouts/deletedLayerIds cannot permanently remove recovered indicators.
- Superseded section-indicator commit `38e1da5` changed preview/canvas visibility behavior and should not be treated as the desired approach. The active cache key after the no-canvas fix is `20260624-section-indicator-no-canvas-v1`.

- Layout builder sizing final state: commits after `007031f` adjusted the layout builder only. `15dbcb6` was the first canvas/display sizing attempt, but James clarified the direction; the intended sequence is `1a9f4be` (`Set layout builder canvas to display box`), `195cfe4` (`Balance layout builder canvas margins`), `e294cdf` (`Set layout builder canvas ratio from width`), and `ddc5ebd` (`Fit layout builder display to canvas`). Final visible layout-builder canvas/display is `304.5 x 541.34`, 9:16 derived from unchanged x-axis width, with square display edges and no display border/shadow/padding.
- Important layout-builder constraint from James: changes are layout-builder only. Do not make the display builder take layout from the layout builder, and do not connect layout-builder state/storage to display-builder state/storage in any way. Display builder should remain unchanged/separate unless James explicitly asks otherwise.
# 2026-06-24

- Arrow sprite sizing work: final pushed commit `4a98b1c` (`Use default sprite scaling for arrows`) is the intended state. Arrows must not be a special native-size exception; they should use the same default library sprite sizing/drop logic as other sprites (`dropScale: 0.75`). Saved oversized arrow layouts from the earlier `7x14` attempt are migrated down to `5x11`, and builders use cache key `20260624-arrow-default-scale-v1`.
- Relevant prior arrow commits `965affe` and `cace54d` tried keeping arrows at native `7x14`; those were superseded because James reported the arrows were still way too big and asked to use the same sizing logic as the other sprites.

- Section indicator restoration: final pushed state after James rejected canvas/preview sizing changes is commit `007031f` (`Restore canvas sizing for indicators`). Do not change canvas/preview crop or scale to make indicators visible. Instead, keep the managed section-indicator row inside the existing visible 135x240 layout area and keep recovery logic so old saved layouts/deletedLayerIds cannot permanently remove recovered indicators.
- Superseded section-indicator commit `38e1da5` changed preview/canvas visibility behavior and should not be treated as the desired approach. The active cache key after the no-canvas fix is `20260624-section-indicator-no-canvas-v1`.

- Layout builder sizing final state: commits after `007031f` adjusted the layout builder only. `15dbcb6` was the first canvas/display sizing attempt, but James clarified the direction; the intended sequence is `1a9f4be` (`Set layout builder canvas to display box`), `195cfe4` (`Balance layout builder canvas margins`), `e294cdf` (`Set layout builder canvas ratio from width`), and `ddc5ebd` (`Fit layout builder display to canvas`). Final visible layout-builder canvas/display is `304.5 x 541.34`, 9:16 derived from unchanged x-axis width, with square display edges and no display border/shadow/padding.
- Important layout-builder constraint from James: changes are layout-builder only. Do not make the display builder take layout from the layout builder, and do not connect layout-builder state/storage to display-builder state/storage in any way. Display builder should remain unchanged/separate unless James explicitly asks otherwise.

- Layout builder zoom/view final state: `b87f415` and `d488abb` zoom/lock attempts were both reverted by `8e8550d` and `1c78ba9` because they changed the canvas or scaled the wrong wrapper. The desired current zoom is the later canvas-section zoom sequence ending at `8293b94` (`Maximize layout builder canvas view`), kept by `e7f3180`: visually enlarge the layout-builder canvas section while leaving authored canvas/display dimensions locked at about `304.5 x 541.3`.
- Treat the layout-builder display/canvas dimensions and square-edged display as locked. If James asks for more/less view size, only adjust the surrounding layout-builder presentation/zoom area; do not change the actual canvas, display shell, canvas coordinate system, exported layout dimensions, or display-builder behavior.
- Section indicators: `50a5c49` disabled the managed auto-render/recovery section-indicator system. They should no longer be automatically created in layout/display/video builders. James wants to rebuild them manually.
- Current final commit `e7f3180` (`Enable layout builder text editing`) is the desired state for editing: only the layout builder has directly editable canvas text layers, including multiline persistence and the visible “Text boxes in this section” panel. Display builder remains unaffected and must not gain editable canvas text.
- Current final section-indicator library state from `e7f3180`: section indicator and highlighted section indicator sprites are available in the layout builder sprite library only, so James can manually add them. Display builder should not gain section-indicator library items from this change.

- Layout builder text editing follow-up: commits after `e7f3180` are part of the current intended state. `6765bde` clamps layout-builder text boxes so edited text stays within fixed boxes, and section indicator library sprites use the same default sprite sizing logic as other sprites (`dropScale: 0.75`; verified dropped section indicator from `10x10` source to `8x8`). `88d48ea` adds a small 2px layout-builder-only text clip bleed so pixel text/strokes are not visually cut too early.
- Deleted default text boxes recovery: `b7df3a0` recovers accidentally deleted default layout-builder text boxes from saved layouts and changes keyboard deletion so `Backspace` does not delete selected layers; only `Delete` intentionally deletes layers.
- Header editing and text fit final state: `5a2e5cf` attempted to preserve layout-builder header edits, but the current final intended commit is `fc44df0` (`Constrain layout builder text editing`). Layout builder only: header text is directly editable and persists, normal text boxes block typed/pasted input once it would exceed the existing box instead of wrapping to another row, and display builder remains non-editable/separate. Cache key from this work: `20260624-layout-text-fit-v1`.
