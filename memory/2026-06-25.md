# 2026-06-25

- Layout builder state James approved: the display/canvas dimensions and maximized canvas view should be treated as locked. Do not change the layout-builder authored canvas/display size or the approved zoom/framing unless James explicitly asks. The display shell is square-edged and fits the canvas.
- Keep layout builder and display builder separated. James repeatedly asked that layout-builder layout/state changes not feed display builder, and layout-builder-only requests should not change display builder behavior.
- Managed/generated section indicators are disabled/removed; do not auto-recover them. Section indicator sprites are available in the layout-builder sprite library for James to rebuild manually.
- Layout-builder text behavior: all textboxes and the header are editable; header edits persist as manual layout-builder edits. Text entry is constrained to the existing textbox, does not wrap to a new line, and blocks typed/pasted overflow. A small clip bleed exists so pixel text is not visibly chopped. Backspace must not delete selected layers; only Delete should.
- Layout-builder default textboxes should recover if accidentally deleted from saved layout state.
- Fats macro layout is the template James wanted applied to carbs and protein in layout builder only, with appropriate sprite/text swaps. Fats itself should remain unchanged. The fats header layout was copied to the other sections.
- Layout builder disables submacro score color coding; display builder keeps the color-scoring logic.
- Arrow indicator sprite library state: green/red 1-arrow, 2-arrow, and 3-arrow sprites exist in layout builder. Latest fix restored the 2-arrow PNG to unsquashed 13x14 and normalizes stale 12x14 saved layers. Expected drops: 1-arrow 5x11, 2-arrow 10x11, 3-arrow 14x11. Arrow positions snap to whole grid units while dragging. Latest relevant commits: `f119c2d` for arrow grid/sizing and `acf3b29` for removing accidentally staged OpenClaw state files. Cache key: `20260624-layout-arrow-grid-v1`.
# 2026-06-25

- Layout builder state James approved: the display/canvas dimensions and maximized canvas view should be treated as locked. Do not change the layout-builder authored canvas/display size or the approved zoom/framing unless James explicitly asks. The display shell is square-edged and fits the canvas.
- Keep layout builder and display builder separated. James repeatedly asked that layout-builder layout/state changes not feed display builder, and layout-builder-only requests should not change display builder behavior.
- Managed/generated section indicators are disabled/removed; do not auto-recover them. Section indicator sprites are available in the layout-builder sprite library for James to rebuild manually.
- Layout-builder text behavior: all textboxes and the header are editable; header edits persist as manual layout-builder edits. Text entry is constrained to the existing textbox, does not wrap to a new line, and blocks typed/pasted overflow. A small clip bleed exists so pixel text is not visibly chopped. Backspace must not delete selected layers; only Delete should.
- Layout-builder default textboxes should recover if accidentally deleted from saved layout state.
- Fats macro layout is the template James wanted applied to carbs and protein in layout builder only, with appropriate sprite/text swaps. Fats itself should remain unchanged. The fats header layout was copied to the other sections.
- Layout builder disables submacro score color coding; display builder keeps the color-scoring logic.
- Arrow indicator sprite library state: green/red 1-arrow, 2-arrow, and 3-arrow sprites exist in layout builder. Latest fix restored the 2-arrow PNG to unsquashed 13x14 and normalizes stale 12x14 saved layers. Expected drops: 1-arrow 5x11, 2-arrow 10x11, 3-arrow 14x11. Arrow positions snap to whole grid units while dragging. Latest relevant commits: `f119c2d` for arrow grid/sizing and `acf3b29` for removing accidentally staged OpenClaw state files. Cache key: `20260624-layout-arrow-grid-v1`.

## 09:45 UTC flush

- Layout-builder-only work since the earlier arrow-grid memory: do not change display builder for these items unless James explicitly asks.
- Arrow indicator state after James's latest feedback: keep the 2-arrow layer's grid box at integer `10x11` so placement matches the `5x11` and `14x11` arrows, but render the grouped two-arrow sprite with an internal `0.25` grid inset on each side for an effective visual width of `9.5x11`. Latest commit: `7acb3c5` (`Keep two-arrow layout placement on grid`). Prior tuning commits were `a7a515c` and `065e251`.
- All 9 layout-builder sections now share the macro-derived canvas/display rules: `125 x 222.222` grid units for intro, fats, carbs, protein, vitamins, minerals, pros, cons, and outro. Latest commit: `e47debe` (`Share layout builder canvas width across sections`).
- Recent verification pattern: Playwright checked green/red arrow rendering and all 9 layout-builder sections with no browser/page errors; local preview servers were stopped after verification.

- Memory-file housekeeping: during the 09:45 UTC flush, the append-only writer duplicated the earlier top section before adding the new 09:45 entries. Treat the `## 09:45 UTC flush` bullets as the new durable content from that flush; the repeated earlier bullets are duplicates, not new rule changes.

## 11:45 UTC flush

- Layout-builder-only arrow placement latest: 1-arrow indicator layers now use an even `6x11` grid box with `0.5` grid inset on each side, 2-arrow layers use `10x11` with `0.25` inset each side, and 3-arrow layers remain `14x11`. This keeps placement on integer grid boxes while preserving the visible sprite widths. Latest related commit: `4ca9a51` (`Center single-arrow layout placement`).
- Layout-builder macro sections latest: Carbs and Protein should receive the same actual arrow layers/positions as Fats when missing, while keeping food-driven arrow colour/direction/visibility separate. Latest related commit: `fed0cff` (`Copy missing macro arrows from fats`). This was intended layout-builder-only, not a production display-builder change.
- New isolated experimental macro display test builder exists at `docs/display-builder-test/` and live URL `https://dawggbot.github.io/Foodranked/display-builder-test/index.html`. Page title/label: `FoodRanked Macro Display Test` and `EXPERIMENTAL — MACROS ONLY`. Commit: `aa73306` (`Add isolated macro display test builder`), pushed to `main`.
- The experimental display builder is intentionally separate from `docs/app/`, `docs/layout-builder/`, and `docs/video-builder/`; those existing builder folders were not modified for the test harness. It should not be linked from production pages and should not write to production/layout-builder state.
- Experimental display builder storage contract: reads layout-builder keys `foodranked-layout-builder-v4`, `foodranked-layout-builder-sprite-layouts-v1`, and watches `foodranked-layout-builder-food-layouts-v1`; writes only `foodranked-display-builder-test-state-v1` for harmless test-page state.
- Experimental display builder scope: macros only (`fats`, `carbs`, `protein`), read-only canvas, layout selector, food library, section selector, background colour/motion controls, and programmer logic viewer. It deep-clones selected layout data before resolving food values/arrows so stored layouts are untouched.
- Experimental macro bindings created: fats total/saturated/polyunsaturated/omega-3/cholesterol; carbs total/fibre/sugar/starch/glycemic index; protein total/collagen/essential amino acid/non-essential amino acid/bioavailability. Arrow rows cover the same metric sets. Missing sprites and unbound placeholders were not found in the seeded test layout.
- Experimental display builder reuses generated food data, `docs/data/batch-results.json`, display schema/type normalization, macro fill ranges, episode display items, batch metric breakdowns, ruleset fallback logic, and existing green/red arrow sprite assets/90-degree rotation mapping. It should not introduce new nutrition thresholds, scoring rules, or production data changes.
- Acceptance testing performed for the experimental page: Bacon/meats, Oats/grains, Kale/vegetables, and Anchovies for N/A fibre; sections `fats`, `carbohydrates`, and `protein`; browser checks covered layout selector, food switching, stale-value prevention, arrow colours/rotations, background settings, storage isolation, mobile scroll, no console errors, no broken images, and comparison against production display builder for Bacon/fats and Oats/carbs. JS syntax checks and `git diff --cached --check` passed.

## 14:52 UTC flush

- Display Builder v2 exists at `docs/display-builder-v2/index.html` and live URL `https://dawggbot.github.io/Foodranked/display-builder-v2/index.html`. It is separate from the existing display builder, layout builder, and video builder. It is linked from `docs/index.html` as a GitHub Pages studio entry. Initial isolated v2 page commit: `906718a` (`Add isolated Display Builder v2`).
- Display Builder v2 was rebuilt into the macro-only experimental harness James requested, using the existing display builder as the foundation but keeping implementation isolated under `docs/display-builder-v2/`. Main harness commit: `afd9b74` (`Build macro display test into v2`), pushed to `main`.
- Current v2 behavior after James's latest request: match the layout builder's authored text and sprite behavior. V2 no longer rewrites layout text labels/header text; it only replaces exact `?` placeholder text layers. V2 does not run falling/background sprite animations; keep animation behavior for the video builder. Latest commit: `3d42aa7` (`Match v2 layout text and static sprite behavior`), pushed to `main`.
- V2 storage contract: reads layout-builder keys `foodranked-layout-builder-v4`, `foodranked-layout-builder-sprite-layouts-v1`, and `foodranked-layout-builder-food-layouts-v1`; writes only `foodranked-display-builder-v2-state-v1`. It should not write old display-builder, layout-builder, or video-builder keys.
- V2 protected-folder rule from recent work: `docs/app/`, `docs/layout-builder/`, and `docs/video-builder/` were intentionally left untouched. Continue preserving that separation unless James explicitly asks otherwise.
- V2 macro harness scope: fats, carbohydrates, and protein only; read-only canvas; layout selector from layout-builder saved/current layouts; foods library; section selector; prominent programmer logic panel; generated food data and existing scoring/arrow logic reused. Text/arrow bindings cover fats total/saturated/polyunsaturated/omega-3/cholesterol; carbs total/fibre/sugar/starch/glycemic index; protein total/collagen/EAA/NEAA/bioavailability.
- V2 testing already performed around the latest commits: Bacon, Oats, Kale, and Anchovies across macro sections; comparison to production display builder for Bacon/fats and Oats/carbs; N/A fibre behavior; no stale values; no broken sprite paths; no console errors; mobile scroll; `node --check` for v2 JS; `git diff --check`; no background animation sprites after `3d42aa7`.
