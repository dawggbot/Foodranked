# 2026-06-26

## Recent Builder Work

- Display builder v2 now has the submacro nutrition value colour grade system from the original display builder. The v2 renderer applies value text colours from the resolved arrow band and no longer suppresses those colours. Commit pushed: `2a38f31 Add v2 submacro value colour grades`.
- Display builder v2 preview was zoomed out by reducing the canvas scale from `1.52` to `1.36` and updating the CSS fallback width, making the display panel about 10% smaller without changing layout coordinates. Commit pushed: `18bdf1d Reduce display builder v2 canvas zoom`.
- Display builder v2 permanent title/status topbar was removed to free page space. Render/warning status now lives in the canvas meta line. Commit pushed: `2ad2cbb Tighten display builder v2 canvas view`.
- Display builder v2 main macro bars now copy the video builder fill timing/behaviour while leaving the video builder untouched: fill layers render as canvases and animate through the GIF-frame fill path instead of appearing instantly. Commit pushed: `2ad2cbb Tighten display builder v2 canvas view`.
- Layout builder section indicators were redone layout-builder-only. It reads the 9 indicator assets from the intro section, clones that row into all 9 sections, and swaps the corresponding slot to the highlighted indicator per section. Commit pushed: `70e839c Sync layout builder section indicators`.

## Display Builder V2 Section Expansion

- Display builder v2 now includes the Intro section and defaults to it. Section indicator sprites are resolved through the food-type helper so each food type gets matching normal/highlighted indicator sprites. Commit pushed: `dbbf9fd Add display builder v2 intro indicators`.
- Display builder v2 now enables all 9 layout-builder sections: Intro, Fats, Carbohydrates, Protein, Vitamins, Minerals, Pros, Cons, and Outro. The old "not configured" note was removed, and browser verification confirmed the highlighted section indicator moves through slots 1-9. Commit pushed: `4bc4cb8 Enable all display builder v2 sections`.
- Protein section deep dive found v2 was forcing foods into static protein quality rows, causing bogus `0g`, `0/9`, `0/11`, and `0%` rows. Protein rows now come from the generated protein scoring breakdown and only show active metrics: `AMOUNT`, `CLGN`, `EAAs`, `N-EAAs`, or `BIO-A`.
- Display builder v2 protein fallback now displays real protein amount, e.g. bacon shows `AMOUNT 13.7g`; unused protein score-card rows, bullets, and arrows are hidden, and the protein logic panel matches the visible rows. Verified with focused browser checks and a full browser sweep across all 274 foods. Commit pushed: `e9e6043 Fix display builder v2 protein rows`.

## Layout Builder Editability

- Layout builder was updated so non-macro sections are editable/changeable like macro sections. Manual edits on non-macro text layers are now preserved, the outro score no longer snaps back to generated placement, and section reset uses the complete default layout instead of the sparse fallback. Commit pushed: `1650605 Make all layout builder sections editable`.
- Verified with `git diff --check` and Playwright on `docs/layout-builder/` after clearing localStorage: Vitamins, Minerals, Pros, Cons, and Outro reset properly, restore deleted content layers, and keep manual text layout/style edits after `renderAll()`.
