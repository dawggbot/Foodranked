
## 2026-06-27 03:18 UTC - FoodRanked builder work

- Recent Display Builder v2 work was committed/pushed: submacro value colour grades from the original display builder were added to v2 (`2a38f31`); v2 canvas preview zoom was reduced (`18bdf1d`); the permanent title/status card was removed and main macro bar fill animation behavior copied from video builder into v2 only (`2ad2cbb`).
- Layout Builder section indicators were synced from the intro section across all 9 sections, with the active section using the highlighted slot (`70e839c`). Display Builder v2 later gained the intro section and food-type-specific normal/highlighted section indicator sprites (`dbbf9fd`), then all 9 sections were enabled in v2 (`4bc4cb8`).
- A Display Builder v2 protein-row rewrite (`e9e6043`) was reverted at James's request with `f52c3f7`. Do not assume that protein dynamic-row behavior remains active unless reimplemented intentionally.
- Layout Builder editability has been the active trouble area. Commits attempted to make all sections editable: preserve non-macro text edits/reset from full defaults (`1650605`), expose sprite controls for all sections (`b31d75c`), enable text layer dragging (`59b05e7`), force fresh iframe app load via cache-busted URL (`8ec05d8`), stabilize text selection by removing contenteditable canvas text and editing via inspector (`83482c7`), and finally fix internal select/unselect behavior by suppressing post-drag clicks, raising drag thresholds, updating layer selection in place, and avoiding full canvas rebuild on drag end (`4fbedc9`).
- Latest stated verification for `4fbedc9`: Playwright checks passed for clean click, shaky click, drag, double-click-to-edit, text editing across all 9 sections, sprite selection/dragging from layer list, no browser errors, `git diff --check` clean, local test server stopped.
