# 2026-07-17

- FoodRanked layout-builder micro bar workflow: `docs/app/index.html` now has layout-builder-only recovery/migration logic for vitamin and mineral micronutrient bar graphs. Vitamins use 6 columns x 10 DV bars; minerals must stay 5 columns x 10 DV bars. Generated micro bar textbox overlays use `??%` in layout builder, centered on the DV bar sprites, white fill, a bar-colour glow, stroke width `1.15`, width `11`, and font size `4.5`.
- Mineral micro bar cleanup must dedupe aggressively: keep one mineral bar line plus one DV bar per 10% slot across the 5 columns, remove exact/shifted duplicate mineral bar sprites, and preserve normal `minerals_percent_1..5` labels. Vitamin cleanup similarly removes duplicate generated `vitamins_bar_percent_c*_ *` overlays and legacy extra `vitamins_percent_*` overlay duplicates without touching labels.
- Display Builder v2 now incorporates vitamins and minerals from the layout-builder template, similar to macros: layout builder stores all possible sprite/text positions, and Display Builder v2 applies food data, replaces `??%` with actual DV text or `N/A`, shows DV bar sprites up to `floor(DV% / 10)` capped at 10, and hides unused micro bars/textboxes. This was pushed in commit `e4a744c` (`Apply micronutrient sections in display v2`).
- Recent relevant pushed commits for the micro-bar/display work include `3dffacc`, `c1921f8`, `f4d12b6`, `41a8e94`, `397af2c`, `eb187e1`, `1fb5828`, `d9c656f`, `978b20b`, `7ae0e2c`, and `e4a744c`. Verification pattern used: `node --check` on inline/extracted JS where applicable, `git diff --check`, and Playwright smoke tests against Display Builder v2 using Almonds to confirm vitamins/minerals render real values, no visible `??%`, expected bar counts, and glow styling.
- A reusable FoodRanked micro layout builder skill was proposed through Skill Workshop. Original proposal `foodranked-layout-builder-micros-20260717-853087858f` became quarantined and cannot be applied. Replacement proposal `foodranked-micro-layout-builder-20260717-273c02c209` was clean/pending, but apply attempts returned `Plugin approval required (gateway unavailable)` even after James approved. Do not rely on the quarantined proposal.
- Correction for the vitamin cleanup note above: generated vitamin micro textbox overlay ids follow `vitamins_bar_percent_c<column>_<percent>`.
