## 2026-07-19 Durable Memory

- Vitamin B lock completed and pushed in commit `1ce623a Lock Vitamin B scoring to B12`.
- FoodRanked's Vitamin B score/display slot is always Vitamin B12: data key `vitamin_b12_dv`, label `Vitamin B12`. Scripts, narration, subtitles, schema wording, and generated outputs should not say generic `Vitamin B` when referring to the score slot.
- B12 scoring uses FDA DV `2.4 mcg`; score remains `floor(DV% / 10)`, capped at `10`.
- Anchovies B12 was corrected after checking USDA FDC `174183`: `0.88 mcg/100g`, which is `37% DV`, not `150%`.
- B1/B3 were removed from score-bearing/output vitamin rules; repo search after the update showed no B1/B3 scoring/output references except schema/docs language that explicitly forbids using them for the Vitamin B slot.
- B12/nutrition revision verification from the completed work: `node scripts/foodranked-data-quality-audit.js` passed with `0` errors across `274` foods; affected subtitle verification passed.

- Pros/cons layout builder textbox expansion completed and pushed in commit `67eba5b Expand pros cons layout textboxes`.
- Pro/con item text layers are normalized to hold at least `3` lines. Checked-in defaults in `docs/app/default-layout.js` had all six pro/con boxes at `textBoxHeight: 18`.
- Textbox wrapping/clipping completed and pushed in commit `ede4a94 Wrap layout builder textboxes`.
- Layout-builder textboxes use normal textbox behavior: `white-space: pre-wrap`, `overflow-wrap: break-word`, and `overflow: hidden`, so text wraps inside the box and excess vertical text is clipped instead of spilling out.
- Pros/cons bite-size guard completed and pushed in commit `5001017 Constrain pros cons titles to fit`.
- Pro/con titles are capped at `64` characters. The scorer rejects overlong/dirty pro/con titles, the data-quality audit checks the same rule, and `scripts/verify-pros-cons-title-fit.js` verifies every source/docs title against the actual 3-line layout-builder textbox.
- During the bite-size pass, 22 long pro/con titles were shortened in both `foods/` and `docs/data/foods/`; `docs/data/foods-index.*` and `docs/data/batch-results.json` were regenerated.
- Verification for the pro/con guard: `scripts/run-foodranked-test-pack.sh` passed; `node scripts/foodranked-data-quality-audit.js --scope=all` passed with `0` errors; `node scripts/verify-pros-cons-title-fit.js` passed for `3,288` source + docs titles.

- Global layout-builder textbox rules completed and pushed in commit `22eccbb`.
- In `docs/app/index.html`, all `.layer-node.text` elements in layout-builder mode now use the same textbox behavior: `white-space: pre-wrap`, `overflow-wrap: break-word`, `overflow: hidden`, and `box-sizing: border-box`.
- The wrapper iframe cache marker in `docs/layout-builder/index.html` was bumped. Verification checked all 9 sections through Playwright: `225` direct app text layers and `225` wrapper text layers had no style failures, and `node scripts/verify-pros-cons-title-fit.js` still passed for `3,288` titles.

- Display Builder v2 pros/cons binding completed and pushed in commit `1c90bf4`.
- Display Builder v2 now swaps layout-builder Pros/Cons placeholders to food-specific data: `pros_item_1..3` / `cons_item_1..3` use each food's `contextItems` titles, and `pros_impact_1..3` / `cons_impact_1..3` use `MAJOR` / `MINOR`.
- Pro/con rows in Display Builder v2 use the same wrap/clip textbox behavior. Programmer Logic includes a `PROS/CONS DISPLAY` report plus missing-data/layer warnings. Verification included `node --check`, `git diff --check`, Playwright smoke tests for Bacon and Almonds across Pros and Cons, and `node scripts/verify-pros-cons-title-fit.js`.

- Pros/cons anti-recap rule completed and pushed in commit `c025f53 Prevent pros cons section recaps`.
- New durable content rule: Pros/cons should be bonus-context tidbits, not plain repeats of video sections the viewer already saw, especially fats/carbs/protein/vitamin/mineral score summaries. They may still build on a previous section when they add a real extra angle, e.g. `ALA omega-3 is the headline fat`.
- The rule was added to repo spec/schema/scoring docs and data-quality audit banned recap phrase checks. Current data was cleaned, including Avocado replacing `protein contribution is tiny` with `ripeness window is annoyingly narrow`, and Chia Seeds replacing `mineral density is genuinely strong` with `easy to boost meals invisibly`.
- The anti-recap pass cleaned 35 food entries, regenerated affected compact episodes, dashboard food index, and leaderboards. Verification passed: `node scripts/foodranked-data-quality-audit.js`, `node scripts/verify-pros-cons-title-fit.js`, and banned phrase search. Live Google Doc schema still needs Drive connector access for syncing.

- Tier-anchor display scoring completed and pushed in commit `d113ec2 Anchor tier display scores`.
- Public/display scores are now fixed by tier for every food: `D=20`, `C=40`, `B=60`, `A=80`, `S=100`.
- Fair ranking still uses internal `rankingScore` / `rankingScoreExact`, after calibration and any food-specific anomaly adjustment; display score is intentionally a simple tier anchor.
- Added `scoreAdjustments[]` support for anomaly foods and outliers that need context outside normal pros/cons, including examples like unusual macro profiles for a category and penalties for processed/fried/sweetened foods.
- Updated schemas/docs/rulesets, regenerated `274` food batch scores, website data, leaderboards, and `156` compact episode score/script payloads. Studio sorting now uses `rankingScore` while showing the tier-anchor display score.
- Verification for tier-anchor scoring: syntax checks passed; all `274` foods and `156` compact episodes matched tier anchors; data-quality audit passed with `0` errors and existing placeholder-source warnings; pros/cons title fit, narration/subtitle checks, and `git diff --check` passed.
