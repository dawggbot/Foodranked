## 2026-07-19 Durable Memory

- Vitamin B lock completed and pushed in commit `1ce623a Lock Vitamin B scoring to B12`.
- FoodRanked's Vitamin B score/display slot is always Vitamin B12: data key `vitamin_b12_dv`, label `Vitamin B12`. Scripts, narration, subtitles, schema wording, and generated outputs should not say generic `Vitamin B` when referring to the score slot.
- B12 scoring uses FDA DV `2.4 mcg`; score remains `floor(DV% / 10)`, capped at `10`.
- Anchovies B12 was corrected after checking USDA FDC `174183`: `0.88 mcg/100g`, which is `37% DV`, not `150%`.
- B1/B3 were removed from score-bearing/output vitamin rules; repo search after the update showed no B1/B3 scoring/output references except schema/docs language that explicitly forbids using them for the Vitamin B slot.
- B12/nutrition revision verification from the completed work: `node scripts/foodranked-data-quality-audit.js` passed with `0` errors across `274` foods; affected subtitle verification passed.

- Pros/cons layout builder textbox expansion completed and pushed in commit `67eba5b Expand pros cons layout textboxes`.
- Pro/con item text layers are normalized to hold at least `3` lines. Checked-in defaults in `docs/app/default-layout.js` had all six pro/con boxes at `textBoxHeight: 18`.
- Textbox wrapping/clipping completed and pushed in commit `ede4a94 Wrap layout builder textboxes`.
- Layout-builder textboxes use normal textbox behavior: `white-space: pre-wrap`, `overflow-wrap: break-word`, and `overflow: hidden`, so text wraps inside the box and excess vertical text is clipped instead of spilling out.
- Pros/cons bite-size guard completed and pushed in commit `5001017 Constrain pros cons titles to fit`.
- Pro/con titles are capped at `64` characters. The scorer rejects overlong/dirty pro/con titles, the data-quality audit checks the same rule, and `scripts/verify-pros-cons-title-fit.js` verifies every source/docs title against the actual 3-line layout-builder textbox.
- During the bite-size pass, 22 long pro/con titles were shortened in both `foods/` and `docs/data/foods/`; `docs/data/foods-index.*` and `docs/data/batch-results.json` were regenerated.
- Verification for the pro/con guard: `scripts/run-foodranked-test-pack.sh` passed; `node scripts/foodranked-data-quality-audit.js --scope=all` passed with `0` errors; `node scripts/verify-pros-cons-title-fit.js` passed for `3,288` source + docs titles.

- Global layout-builder textbox rules completed and pushed in commit `22eccbb`.
- In `docs/app/index.html`, all `.layer-node.text` elements in layout-builder mode now use the same textbox behavior: `white-space: pre-wrap`, `overflow-wrap: break-word`, `overflow: hidden`, and `box-sizing: border-box`.
- The wrapper iframe cache marker in `docs/layout-builder/index.html` was bumped. Verification checked all 9 sections through Playwright: `225` direct app text layers and `225` wrapper text layers had no style failures, and `node scripts/verify-pros-cons-title-fit.js` still passed for `3,288` titles.

- Display Builder v2 pros/cons binding completed and pushed in commit `1c90bf4`.
- Display Builder v2 now swaps layout-builder Pros/Cons placeholders to food-specific data: `pros_item_1..3` / `cons_item_1..3` use each food's `contextItems` titles, and `pros_impact_1..3` / `cons_impact_1..3` use `MAJOR` / `MINOR`.
- Pro/con rows in Display Builder v2 use the same wrap/clip textbox behavior. Programmer Logic includes a `PROS/CONS DISPLAY` report plus missing-data/layer warnings. Verification included `node --check`, `git diff --check`, Playwright smoke tests for Bacon and Almonds across Pros and Cons, and `node scripts/verify-pros-cons-title-fit.js`.

- Pros/cons anti-recap rule completed and pushed in commit `c025f53 Prevent pros cons section recaps`.
- New durable content rule: Pros/cons should be bonus-context tidbits, not plain repeats of video sections the viewer already saw, especially fats/carbs/protein/vitamin/mineral score summaries. They may still build on a previous section when they add a real extra angle, e.g. `ALA omega-3 is the headline fat`.
- The rule was added to repo spec/schema/scoring docs and data-quality audit banned recap phrase checks. Current data was cleaned, including Avocado replacing `protein contribution is tiny` with `ripeness window is annoyingly narrow`, and Chia Seeds replacing `mineral density is genuinely strong` with `easy to boost meals invisibly`.
- The anti-recap pass cleaned 35 food entries, regenerated affected compact episodes, dashboard food index, and leaderboards. Verification passed: `node scripts/foodranked-data-quality-audit.js`, `node scripts/verify-pros-cons-title-fit.js`, and banned phrase search. Live Google Doc schema still needs Drive connector access for syncing.

- Tier-anchor display scoring completed and pushed in commit `d113ec2 Anchor tier display scores`.
- Public/display scores are now fixed by tier for every food: `D=20`, `C=40`, `B=60`, `A=80`, `S=100`.
- Fair ranking still uses internal `rankingScore` / `rankingScoreExact`, after calibration and any food-specific anomaly adjustment; display score is intentionally a simple tier anchor.
- Added `scoreAdjustments[]` support for anomaly foods and outliers that need context outside normal pros/cons, including examples like unusual macro profiles for a category and penalties for processed/fried/sweetened foods.
- Updated schemas/docs/rulesets, regenerated `274` food batch scores, website data, leaderboards, and `156` compact episode score/script payloads. Studio sorting now uses `rankingScore` while showing the tier-anchor display score.
- Verification for tier-anchor scoring: syntax checks passed; all `274` foods and `156` compact episodes matched tier anchors; data-quality audit passed with `0` errors and existing placeholder-source warnings; pros/cons title fit, narration/subtitle checks, and `git diff --check` passed.

- Protein display fallback fix completed and pushed in commit `da81a6a Fix protein display fallbacks`.
- Root cause: the display generator was converting missing protein-quality values to `0` because `Number(null) === 0`, while builders were bound to fixed protein rows such as collagen/EAA/NEAA/bioavailability. Missing protein quality fields should display `N/A`, not fake zeroes.
- Protein fallback rows now use generated display items: examples verified were Bacon `13.7g`, `N/A`, `N/A`, `N/A` and Sweet Potato `1.6g`, `N/A`, `N/A`, `N/A`. Genuine scored zeroes remain visible, e.g. Kale amino-acid threshold score `0/9` when that score actually ran.
- Verification for the protein display fix included JS syntax checks, app inline script parse, targeted protein display audit across `312` scripts, narration/subtitle verification, data-quality audit with `0` errors, pros/cons title fit, and the FoodRanked test pack.
- Repo history note: push helper briefly made an extra auto-commit with pre-existing workspace files, then revert commit `2c0b13d` was pushed immediately. GitHub has the protein fix plus that revert; unrelated local workspace files remained uncommitted.

- Protein section display policy was locked and pushed in commit `61ee49c Lock protein section display policy`.
- Durable protein display contract: the visible Protein section rows are exactly `collagen_g`, `essential_amino_acids_score`, `nonessential_amino_acids_score`, and `bioavailability_percent`, in that order.
- `protein_g_fallback` is hidden scoring/narration support only and must not appear as a visible Protein section row.
- Missing, skipped, or non-source-backed protein-quality fields display `N/A`; genuine scored/source-backed zeroes may still display as `0`.
- The schema/docs, public data, indexes, and all existing compact episodes were regenerated to match this locked protein policy. Audit coverage now fails if visible protein row order drifts or fallback appears visibly; verification passed with `proteinDisplayScripts: 312` and data-quality audit `0` errors.

- Protein layout-builder to Display Builder v2 linking was completed in two commits: `918fdde Link protein layout section to display v2` and the corrective `b21e5c9`.
- Durable builder behavior: layouts saved with legacy section key `proteins` should hydrate/render as visible section key `protein` in Display Builder v2, including current working layout, food-specific layouts, and saved layout presets.
- The real linkage bug was stale `manualText` values on protein text layers (`CLGN`, `1g`, `8/9`, etc.). Those manual values froze the layout textboxes instead of binding to generated Display Builder v2 protein display data.
- The fix added a layout-builder migration that releases protein textboxes from manual mode and cleaned default protein textbox labels/values to match the schema-backed display. Verified seeded stale saved layout data now matches across Layout Builder canvas, Layout Builder textboxes, and Display Builder v2: `PROTEIN`, food protein amount such as `13.7g`, then `COLLAGEN / N/A`, `EAA / N/A`, `NEAA / N/A`, `BIOAVAIL. / N/A`.
- Related files changed for the protein linkage fix: `docs/app/index.html`, `docs/app/default-layout.js`, and `docs/layout-builder/index.html` cache marker.

- Correction/supersession for the protein builder work: James clarified the layout builder and default layout are correct and must not be changed unless he specifically and directly asks for layout changes.
- The earlier layout-builder/default-layout protein edits from `b21e5c9` were wrong and were reverted in commit `2563831`; later fixes should be Display Builder v2 only unless James explicitly asks to change the layout builder.
- Durable Display Builder v2 contract: use the layout-builder layout as the visual template, preserve its protein labels/positions/styles, replace question-mark/value placeholders with food-specific nutrition values, then apply the existing arrow indicator logic.
- Protein Display Builder v2 was corrected in commit `42f4f42`: it preserves layout labels `CLGN`, `EAAs`, `N-EAAs`, and `BIO-A`, fills values from generated protein `displayItems`/`displayPolicy`, applies arrows from displayed rows, and keeps `protein_g_fallback` hidden from visible protein submacro rows.
- Macro submacro `N/A` policy was corrected and pushed in commit `201bcf8 Fix macro submacro display defaults`. This supersedes the earlier protein-quality policy that allowed skipped/missing visible subrows to show `N/A`.
- Durable submacro rule: visible submacros are not allowed to display `N/A` unless their main macro displays `N/A`. If the main macro has a real value, submacro rows must be filled with appropriate display defaults and arrow logic.
- Current protein defaults for foods whose main Protein macro is present but detailed quality data is missing/skipped: `CLGN 0g`, `EAAs 0/9`, `N-EAAs 0/11`, and `BIO-A 0%`, with red down arrows where applicable. Foods whose main Protein macro is `N/A` may still show `N/A` in protein subrows.
- Verification after `201bcf8`: data-quality audit passed, narration/subtitle verification passed, Display Builder v2 browser smoke test passed, and `foods-index.json` had `0` remaining bad submacro `N/A` rows.

- Display Builder v2 protein fallback rows were fixed and pushed in commit `9b899e2 Fix v2 protein fallback rows`.
- Durable v2 protein fallback rule: foods without generated episode `displayItems` must still render the locked layout protein rows `CLGN`, `EAAs`, `N-EAAs`, and `BIO-A`, using schema/display-policy values rather than partial `batch-results` protein metrics.
- Verified examples after `9b899e2`: Amaranth renders `0g`, `0/9`, `0/11`, `0%`; Beef Sirloin renders `1.2g`, `0/9`, `0/11`, `0%`; Bacon and Kale remained correct; Protein-`N/A` entries still show `N/A`.
- All `274` foods passed the v2 protein row/order/value invariant after `9b899e2`. No layout builder or default-layout files were changed.

- Display Builder v2 macro fallback arrow bands were fixed and pushed in commit `a3d3aa9 Fix v2 macro fallback arrow bands`.
- All `274` food entries now properly display all visible submacros across the macro sections Fats, Carbs, and Protein: `822` macro-section checks and `3,288` submacro rows passed with zero failures.
- Durable macro submacro display status after `a3d3aa9`: row order, filled values, and arrow bands are valid for all foods; `N/A` remains only where the main macro itself is `N/A`.
- The final v2 edge case was unscripted/sample foods such as Hot Dog and Mayonnaise, which had fallback `0` submacro values but no arrow bands because their scoring rules were marked not-applicable. This was fixed in `docs/display-builder-v2/macro-logic-view.js` with a cache bump in `docs/display-builder-v2/index.html`. No layout builder files were changed.

- Protein display submacro fallbacks were filled and pushed in commit `b310027f Fill protein display submacro fallbacks`.
- Durable protein submacro display policy after `b310027f`: Display Builder v2 may use display-only protein estimates for visible protein rows when source-backed amino-acid/bioavailability details are missing, while keeping underlying source nutrition strict.
- Current v2 examples: Amaranth `CLGN 0g`, `EAAs 7/9`, `N-EAAs 9/11`, `BIO-A 62%`; Beef Sirloin `CLGN 1.2g`, `EAAs 9/9`, `N-EAAs 11/11`, `BIO-A 92%`; Chicken Breast `0.4g`, `9/9`, `11/11`, `92%`; Tofu `0g`, `7/9`, `9/11`, `78%`; Whey Isolate `0g`, `9/9`, `11/11`, `99%`.
- After `b310027f`, all `240` protein-positive foods in the `274`-food Playwright sweep returned filled protein submacro values with no `N/A`/null leaks. Remaining `0`s are intentional, such as collagen for non-meats or trace-protein EAA/NEAA cases.
- `b310027f` regenerated dashboard data, batch results, leaderboards, and all `156` compact episodes. No layout builder files were changed.

- Amino-acid display counting was gated and pushed in commit `9b16f740 Gate amino acid display counts`.
- Durable amino-acid display rule: essential and nonessential amino-acid submacro counts should only count amino acids present in a useful/material amount, not tiny trace values that would not make a practical difference.
- Current material floors after `9b16f740`: essential amino acids count only at `>=100mg/100g`; nonessential amino acids count only at `>=500mg/100g`.
- Display Builder v2 gates estimated EAA/NEAA rows behind a useful protein band and uses `floor()` so counts do not round up from borderline values. Arrow bands for EAA/NEAA are non-overlapping integer count bands aligned to the gated counts.
- Verified examples after `9b16f740`: Milk and White Rice show `EAAs 0/9` and `N-EAAs 0/11`; Amaranth, Tofu, and Greek Yogurt show `EAAs 7/9` and `N-EAAs 8/11`; Beef Sirloin shows `EAAs 9/9` and `N-EAAs 11/11`.
- `9b16f740` regenerated all `274` foods and `156` compact episodes. Display Builder v2 browser verification checked `240` protein-positive foods with filled protein rows and no bad `N/A` leaks. Data-quality audit passed with `0` errors and the same `274` placeholder-source warnings as before. No layout builder files were changed.

- Bacon amino-acid sourcing/estimate was completed and pushed in commit `3cf63f8d Source bacon amino acid estimate`.
- Durable bacon protein-quality note: raw bacon has `13.7g` protein per 100g in the FoodRanked source file, but the exact raw bacon source record has missing amino-acid rows. Do not interpret missing amino-acid rows as literal zero amino acids.
- For bacon, a USDA-backed cooked pan-fried bacon amino-acid profile was used via MyFoodData / FoodData Central Standard Release, then scaled back to the raw bacon entry by protein ratio. Keep this as an educated display estimate unless/until a better raw source amino-acid profile is found.
- Current bacon protein submacro display after `3cf63f8d`: `CLGN 0.4g`, `EAAs 9/9`, `N-EAAs 10/11`, and `BIO-A 84%`.
- `3cf63f8d` regenerated the relevant food/episode outputs and verified Display Builder v2 reads the updated bacon values. Data audit passed. No layout builder files were changed.
