## Video Builder v2 Display Builder v2 Placement Contract

- James wants `docs/video-builder-v2/` to copy sprite and textbox placement/content from Display Builder v2 only: nothing from the original display builder, generic layout builder, repo default layout, or `default-layout.js`.
- Display Builder v2 exports its rendered placement/content to browser localStorage key `foodranked-display-builder-v2-placement-layouts-v1`.
- Video Builder v2 should read that Display Builder v2 export only, with no fallback placement sources and no post-load rewrites for header, macro, micron, pros/cons, intro, outro, sprite, textbox, or z-index placement.
- Because this handoff is localStorage-based, the same browser/session must open/select the food in `display-builder-v2` first so the export exists; then `video-builder-v2` picks it up.
- Relevant pushed commits from the July 21 work: `31e1d7ef` added isolated Video Builder v2, `ce058ad7` moved it toward Display Builder v2 food layouts, `1a575e60` exported Display Builder v2 placement to Video Builder v2, and `7140f738` tightened Video Builder v2 to exact Display Builder v2 export only. A later cleanup made remote head `31787a14` while preserving unrelated local dirty files.
- Video Builder v2 also intentionally removed the Hook duration/reveal/caption controls block James pointed at, while keeping Manifest visible.

## 2026-07-21 Finalisation Sample Direction

- Latest Video Builder v2 decision from James: Video Builder v2 looks good now; do not touch anything about `docs/video-builder-v2/` unless James directly asks for it.
- Proposed first finalisation sample set should cover all 11 locked food types, include Bacon, cover every public tier at least once, and include one food with a 100% micro section.
- Current proposed sample set from live scorer output: Kale (vegetables, S), Raspberries (fruits, A), Buckwheat (grains, B), Black Beans (legumes, A), White Potato (tubers, C), Hazelnuts (nuts, S, vitamins 100), Chia Seeds (seeds, S), Bacon (meats, D), Greek Yogurt (dairy, A), Extra Virgin Olive Oil (oils-and-fats, S), Regular Cola / `cola-regular` (misc, D).
- This set covers tiers D, C, B, A, and S. Hazelnuts is the explicit 100% micro-section sample via vitamins = 100. Bacon remains the first food to perfect after the list is agreed.

## 2026-07-21 Bacon Voice v8 Production Pass

- Bacon was updated with a cleaner finalisation narration take and pushed in commit `2484448c` (`Produce bacon voice v8 aligned episode`).
- New canonical split narration take: `voice-v8`, generated with ElevenLabs split blocks under `production/episodes/bacon/voice/voice-v8-blocks/` and mirrored to `docs/audio/episodes/bacon/voice-v8-blocks/`.
- Forced alignment output: `outputs/episodes/bacon-compact/voice-v8-blocks-forced-alignment.json`; aligned subtitles remain 70 cues, max 2 lines, using compact display units while narration uses full spoken units.
- Section map added for the later reusable workflow/app: `voice-v8-section-map.json` groups the canonical blocks into the 9 production sections James requested: intro, fats, carbs, protein, vitamins, minerals, pros, cons, outro.
- Browser/data verification showed `voice-v8 split ready`, section timings synced to about 115.0s narration plus 4.0s dwell, and trigger-word highlight checks passed for polyunsaturated fat, saturated fat, essential amino acids, collagen, vitamin B12, zinc, the first pro, and the first con. A fresh headless Video Builder v2 browser still reports `DBv2 missing` without James's local Display Builder v2 layout export, but it reads the new `voice-v8` data correctly.
- This workflow is a good candidate for a future skill, but use Skill Workshop when James explicitly asks to create it.

## 2026-07-21 Video Builder v2 Current State Supersedes Earlier Notes

- James approved the rebuilt Video Builder v2 and asked not to touch `docs/video-builder-v2/` unless directly asked. Later fixes were directly requested for Bacon sync/glow/indicators.
- Current Video Builder v2 was rebuilt from the original Video Builder bar/structure while leaving `docs/video-builder/` untouched. The Hook controls are back: duration, reveal, caption size, caption, reset captions, copy manifest.
- Current Video Builder v2 still must use Display Builder v2 only for sprite/textbox placement and content. It should not load `default-layout.js`, the original Display Builder storage keys, the generic Layout Builder, or repo/default placement fallbacks.
- If Display Builder v2 has no food layout/export for the selected food, Video Builder v2 should clear the stage and show a clear `DBv2 missing` style message rather than stray text.
- Micron highlighted glow in Video Builder v2 is binary only: `<10% DV` glows red and `10% DV+` glows green. Do not use white/neutral glow for middle micron mentions; for Bacon specifically, potassium `4% DV` should glow red and zinc `10% DV` should glow green.
- Video Builder v2 section indicator sprites should render as a visible 9-dot food-type sprite row at the bottom of the visible phone/canvas bounds, not below the cropped desktop preview. Active section uses the highlighted food-type sprite/glow.
- Relevant pushed commits after the Bacon voice pass: `844f5e96` fixed binary micron glow and added bottom indicators; `f57dac66` anchored section indicator sprites to the visible Video Builder v2 phone/canvas bounds.

## 2026-07-21 Pro/Con Narration Rule

- New James-approved rule: every pro/con title shown on the display must be spoken word for word in the script/narration before any explanatory "after bulk" sentence. The after-bulk sentence can explain or expand naturally, but it comes after the exact displayed phrase.
- Bacon retake direction: replace the old con title `salty-fatty profile encourages portion creep` with `salty, fatty flavour makes portions easy to overdo`, then narrate that exact phrase before the explanation.
