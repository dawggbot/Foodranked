# 2026-07-22

## Video Builder Guardrail

- James explicitly ruled that the original video builder must not be touched: do not edit `docs/video-builder/` under any circumstances unless he gives an explicit one-time instruction. Future video builder work should target `docs/video-builder-v2/` only.
- The intended outro pacing changes were moved into Video Builder v2. Latest pushed v2 timing: `1s` breathing room after the final summary before the tier reveal, and `2s` dwell/window after the tier reveal audio cue finishes.
- Video Builder v2 split narration should let each section MP3 play fully before moving to the next section. Use per-section audio sync/metadata and tail guards rather than cutting section transitions tightly.
- The v2 ending tier reveal should feel less rushed: start on the final `X tier` phrase, reveal the tier stamp more slowly, and keep enough tail/post-audio hold so the final audio is not stopped early.

## Recent Commits

- `d5edcc88` moved the split-audio/outro pacing fixes into Video Builder v2 only.
- `a567b180` adjusted v2 outro timing to `1s` pre-tier breath and `2s` post-tier dwell.

## Skill Follow-Up

- James has been building toward saving the reusable FoodRanked workflow as a skill. When he says "save the skill", route the reusable `nutrition-content-studio`/FoodRanked production workflow into the skill creation path rather than continuing ad hoc video-builder edits.

## Video Builder v2 Tier Reveal Polish

- Further v2-only polish was completed after the guardrail: tier stamp animation is now triggered when the final tier reveal audio plays, matches the punchy intro `food image` / `ranked` stamp timing and SFX feel, and is not stretched by the outro scene duration.
- Subtitles now clear during the gap before the final tier stamp animation starts, and the tier caption stays suppressed during the reveal.
- Recent pushed commits: `6bed32b7` enabled the v2 tier stamp reveal, `fd5e3fa1` matched the v2 tier stamp to intro timing, and `acde44f8` cleared v2 subtitles before the tier stamp.

## Pending Sync Skill Proposal

- Created pending Skill Workshop proposal `nutrition-video-sync-integrator-20260722-9140366c2b`; it has not been applied/installed yet.
- The proposal captures the narration-to-video sync/integration workflow: split audio, section timing, subtitles, trigger-word reveals, intro and tier stamp animations, VFX/SFX coordination, v2-only builder safety, and validation checks.

## Video Builder v2 Section Indicators

- James wants Video Builder v2 section indicator sprites to match the Display/Layout Builder export exactly: keep DBv2 placement, size, z-index, and visibility; only swap normal/highlighted sprites for active state. Do not add extra active glow or rebuild/reposition them in v2.
- `efce84dd` matched v2 section indicators to DBv2. `c645f93a` guarded tiny sprite seams by rendering them as block/crisp pixel sprites and adding a small centered sub-pixel bleed to the indicator boxes without changing stored DBv2 layout width.

## Video Builder v2 Final Reveal CTA Stamps

- The final tier reveal in Video Builder v2 now includes `like`, `follow`, and `share` UI sprites stamped in with the tier sprite, placed in a row just below the tier sprite.
- James replaced the `follow` and `share` files and wanted those updated assets used. Current CTA stamp size is 50% smaller than the initial same-size intro/outro stamp attempt, verified around `11.7` grid units instead of `23.4`.
- During the last few seconds after the tier reveal lands, the `like`, `follow`, and `share` stamps do a left-to-right wave: each moves up slightly and scales up slightly in order, with a second late pulse before the video ends.
- Recent pushed commits: `f7ebbfcd` added v2 final reveal CTA stamps, `80c3318a` used the replaced/smaller CTA stamps, and `c415bbd3` added the v2 CTA stamp wave.

## Video Builder v2 Outro Score Display

- The v2 outro score display should show the food entry's final tally score, not a stale layout placeholder and not the tier anchor score.
- Latest behavior: use the `rankingScore` tally source and round to the nearest whole number with no decimal point. Examples verified: Bacon `16.2 -> 16`, Apple `13.4 -> 13`, Acorns `25.5 -> 26`, Almond Butter `81.2 -> 81`, Sweet Potato `80.5 -> 81`.
- Recent pushed commits: `4081230f` bound the v2 outro score display to food data, `07ec6981` switched it from tier-anchor score to tally score, and `916b7626` rounded the v2 outro tally score.

## First Sample Foods

- James clarified that the earlier Bacon/Rice Cakes/Cola/Olive Oil/Salmon launch-prototype set is no longer relevant for this next pass.
- The current first sample list from July 21 is: Kale, Raspberries, Buckwheat, Black Beans, White Potato, Hazelnuts, Chia Seeds, Bacon, Greek Yogurt, Extra Virgin Olive Oil, and Regular Cola / `cola-regular`.
- James plans to do Kale next because he has made a food image sprite for it.

## Food Image Plate Normalization

- Food image sprites in the food image plate should now use Bacon's reference scale and center consistently across foods.
- Latest reference placement: `x: 8`, `y: 3.5`, `width: 23`, `height: 23`.
- `870c9279` normalized food image sprite placement across the main display builder, Display Builder v2 export, and Video Builder v2. Bacon keeps its existing visual placement; Kale and future foods inherit the same constant plate placement.

## Layout Builder -> DBv2 -> VBv2 Workflow

- James clarified the intended production workflow: Layout Builder is the source of truth for all sprite/textbox placements and sizes; Display Builder v2 applies the correct sprites and text for each food entry into that layout; Video Builder v2 applies SFX, VFX, animations, narration, subtitles, and timing to the DBv2 food-entry display.
- This workflow may not be theoretically perfect, but it is the workflow that has worked so far and should be preserved unless James explicitly asks to redesign it.
- Original `docs/video-builder/` remains off-limits; video-builder work should continue in `docs/video-builder-v2/` only.

## Food Image Placement Follow-Up

- `1b01eb82` made the food image sprite movable in the Layout Builder and preserved manual drag/resize placement instead of overwriting it with auto food-image scaling.
- `a4a48fc4` started applying the Layout Builder food image placement in DBv2, but James reported no visible DBv2 change after moving the food image plate upward.
- `df2b1596` fixed DBv2 to treat the current saved Layout Builder layout as the active placement/size guide, including both the food image plate and food image sprite position. DBv2 should then apply food-specific sprites/text after layout placement, and VBv2 should inherit that DBv2 export.

## DBv2 Score Card

- DBv2 score cards should show each food entry's actual total score tally, rounded to the nearest whole number with no decimal point.
- `df2b1596` also fixed DBv2 score-card text to use the rounded tally rather than stale layout placeholders or tier-anchor values.
