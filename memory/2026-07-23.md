## 2026-07-23 09:09 UTC - Pre-compaction flush

- VBv2 is the only video builder James wants changed for current visual/layout work unless he explicitly says otherwise. Intro layering now has ranked glow as its own underlay below the food sprite, with the food sprite still below the ranked sprite: ranked glow `z: 53`, food sprite `z: 54`, ranked sprite `z: 55`.
- Narration voice correction: sourced nutrition facts and food-specific pros/cons can stay precise and somewhat technical when that is the clearest wording. The casual/day-to-day speech rule mainly applies to extra connective explanations, food-type summaries, category context, and use-case wrapping outside the actual nutrition data and pro/con items.
- Avoid odd abstract connective phrases in scripts, including `main pitch`, `low risk is good`, `reason to exist`, `nutrition load`, `lives or dies`, `automatic` in lines like `less automatic than softer greens`, and `universal` in lines like `not universal`. Prefer direct everyday context tied to the food type, such as `protein is a bonus, not something to rely on`, `the carbs are easy to work with`, `it helps fill out a meal without many calories`, and `some people just do not like the bitter leafy taste`.
- New narration rule: macro and micro sections should each end with a quick food-type summary explaining why the scores mentioned in that section matter for that food category.
- Kale script work: current regenerated Kale output is `S tier` with ranking score `82.5`; latest pushed narration refinements are in commit `fca4241c Add food-type section summaries`. The Kale pros/cons were restored to the more precise earlier style, except awkward casual phrasing was cleaned up.
- Pending skill update proposals noted during this work: `nutrition-content-strategist-20260723-30c6d49004` and `nutrition-workflow-automator-20260723-ee7b42c6c8`, both matching the approved narrower narration rule.

## 2026-07-23 09:50 UTC - Pre-compaction flush

- James approved the latest Kale script voice as "amazing"; preserve that style for future scripts. The saved script is `outputs/episodes/kale-compact/narration.txt`, and the latest related push was `2878df28 Save approved narration skill guidance` after `182550ac Record approved narration voice`.
- Durable narration style: keep exact nutrition values, technical scoring details, and precise pro/con wording when they are clearest. Make only the connective/context lines sound like normal everyday speech. Avoid odd abstract wording such as `main pitch`, `low risk is good`, `reason to exist`, `nutrition load`, `lives or dies`, `less automatic`, and `not universal`.
- Future macro and micro narration sections should end with a short food-type summary explaining why those section scores matter for that food category, using direct wording such as `For vegetables, protein is a bonus, not something to rely on`, `the carbs are easy to work with`, and `it helps fill out a meal without many calories`.
- Current Bacon narration voice source points to `voice-v9`. ElevenLabs voice/settings used: voice `Eryn - Informative, Neutral and Measured`, voice ID `RaFzMbMIfqBcIurH6XF9`, model `Eleven Multilingual v2`, output `mp3_44100_128`, stability `50%`, similarity boost `75%`, style `10%`, speaker boost enabled, speed `1.10x`. Repo sources: `production/episodes/bacon/voice/voice-v9-blocks.json` and `config/elevenlabs-voice-settings.v1.json`.

## 2026-07-23 10:10 UTC - ElevenLabs Voice Picker Direction

- James wants reusable skill proposals whenever they seem useful, rather than waiting for him to ask every time.
- Keep FoodRanked ElevenLabs generation settings constant across all audio generation: model `eleven_multilingual_v2`, output `mp3_44100_128`, stability `50%`, similarity boost `75%`, style `10%`, speaker boost enabled, speed `1.10x`.
- The voice is the one thing that should normally change per video. Use a random suitable ElevenLabs voice when generating new narration: clear, not silly sounding, relatively professional, English-capable, and okay with accents as long as they are not very strong.
- If the random voice picker cannot find a suitable voice, fall back to the known `Eryn - Informative, Neutral and Measured` profile rather than picking a weak or gimmicky voice.

## 2026-07-23 10:35 UTC - Kale Adam Voice Take

- James asked for the approved Kale script to be voiced by Adam and integrated into Video Builder v2.
- Kale `voice-v1` is the Adam take using ElevenLabs voice `Adam - Dominant, Firm`, voice ID `pNInz6obpgDQGcFmaJgB`, with the fixed FoodRanked settings from `config/elevenlabs-voice-settings.v1.json`.
- Generated split-audio blocks are under `production/episodes/kale/voice/voice-v1-blocks/` and mirrored to `docs/audio/episodes/kale/voice-v1-blocks/`.
- Forced alignment output is `outputs/episodes/kale-compact/voice-v1-blocks-forced-alignment.json`; Video Builder v2 picks it up through refreshed `docs/data/foods-index.js`.
- Browser probe showed Video Builder v2 reads Kale as `voice-v1 split ready` synced to about `162.3s + 4.0s dwell`, with 80 subtitle cues and the first Kale MP3 returning HTTP 200. A fresh browser still reports no Display Builder v2 placement for Kale unless James's local DBv2 layout export is present, so this pass intentionally did not invent canvas placement.

## 2026-07-23 10:55 UTC - Kale Adam Opener Pronunciation Fix

- James heard the first `voice-v1` Adam opener as closer to `kaleh` than clean `kale`.
- Current Kale Adam take is now `voice-v2`; only the opener food-name block was regenerated, using `Kale.` as the TTS text to soften the ending. The visible/script opener remains Kale.
- `voice-v2` forced alignment output is `outputs/episodes/kale-compact/voice-v2-blocks-forced-alignment.json`; refreshed `docs/data/foods-index.js` points Video Builder v2 at `voice-v2`.

## 2026-07-23 12:03 UTC - Kale S Tier Pronunciation Fix

- James heard Kale's Adam final reveal as closer to `steer` than `S tier`.
- Current Kale Adam take is now `voice-v3`; it keeps all `voice-v2` blocks except `11-final_reveal`, which was regenerated with TTS prompt `S. Tier!` while keeping the stored/display text as `S tier.`.
- For S-tier finals, use a pronunciation-safe TTS prompt that separates the letter from `tier`, but keep the approved final display/script text as `S tier.`.

## 2026-07-23 12:34 UTC - Kale Final Reveal Audio Tail Fix

- James still heard the Kale Adam `S tier` outro as slightly cut off after the pronunciation retake.
- The `voice-v3` final reveal MP3 is `0.882s`, while the forced-alignment speech window had timed the block as `0.629s`; VBv2 should time tiny final reveal clips from full media duration, not only from last-word alignment.
- Kale `voice-v3` now stores `mediaDurationSeconds: 0.882` for `11-final_reveal`, extends the final cue/episode end to `162.656s`, and VBv2 no longer applies the normal split-audio early-stop margin to `final_reveal` blocks.

## 2026-07-23 12:49 UTC - Kale Adam S Start Fix

- James heard the start of the Kale Adam `S tier` final reveal as still slightly cut off, specifically the `S`.
- Current Kale Adam take is now `voice-v4`; it keeps the previous blocks and regenerates only `11-final_reveal` using TTS prompt `Ess tier!`, while the stored/display text remains `S tier.`.
- For Adam S-tier finals, prefer `Ess tier!` over `S. Tier!` when the letter `S` needs a clearer start. VBv2 also gives `final_reveal` split audio a small start grace so the first fraction of the clip is not skipped if playback lands slightly late.

## 2026-07-23 13:02 UTC - Standing Final Reveal Audio Rule

- Treat the Kale final reveal lessons as standing FoodRanked rules for all present and future food entries, not a Kale-only patch.
- For any split-block `S tier.` final reveal, keep stored/display/subtitle text as `S tier.` but use pronunciation-safe TTS text `Ess tier!` and store that separately as `ttsText`.
- For final reveal split-audio timing, use the full MP3 duration when available, keep the normal early-stop margin disabled, and allow a small VBv2 start grace so the first sound is not clipped.
- Skill Workshop proposal `nutrition-workflow-automator-20260723-466908ddd3` captures the same workflow guidance, but applying it was blocked by an OpenClaw approval/gateway issue; repo code and memory now carry the rule.

## 2026-07-23 10:50 UTC - Glycemic Index Narration Wording

- When scripts mention glycemic index in spoken narration, say `glycemic index is 15`, not `glycemic index is 15 GI`. The phrase `glycemic index` already clarifies what GI means; the trailing `GI` only exists for compact nutrition display labels and should not be spoken.

## 2026-07-23 11:08 UTC - VBv2 Intro Ranked Gap

- VBv2 intro timing should leave a short beat between the first food sprite stamp and the second `RANKED` sprite stamp/audio across all foods, so the `ranked` voice block does not feel clipped or rushed.

## 2026-07-23 11:14 UTC - VBv2 Micron Value Reveal

- In VBv2 vitamin/mineral sections, the micron `%` value text should stay hidden until that column's bar reveal animation has finished. The value reveal is the payoff and should not spoil the bar surprise.
- Display Builder v2 can export active per-bar micron textboxes like `vitamins_bar_percent_c1_50` / `minerals_bar_percent_c1_50`, not only legacy `vitamins_percent_1` / `minerals_percent_1` layers. VBv2 reveal rules must treat both shapes as micron value layers.

## 2026-07-23 11:32 UTC - VBv2 Micron Reveal Follow-Up

- James reported the first micron value reveal fix showed no visible change. The real live VBv2 issue was the DBv2 per-bar percent textbox naming; keep this in mind when debugging layer reveal bugs, and verify against the exported active layer ids rather than assuming older helper names.
- Correct fix was pushed as `0167f78f Fix VBv2 micron textbox reveal timing`; it keeps active per-bar micron `%` text hidden through the bar fill and reveals it only after that bar column finishes.
- Follow-up correction: micron bar/value reveal timing in VBv2 must be food-entry specific. Treat active `%` textbox layers like their visible micro bar sprite: use the textbox/bar percent from the DBv2 food-entry layout, and cap timing from the actual visible bar sprites rather than from the default nutrient-column order.

## 2026-07-23 13:29 UTC - Uploaded SFX Naming Rule

- Reusable uploaded SFX assets in `audio/sfx/` and the public `docs/audio/sfx/` mirror should use snake_case filenames, matching the sprite/upload naming style James prefers. Commit `27845783 Rename reusable SFX assets to snake case` applied this to the reusable SFX set and updated VBv2 paths/docs.
- Do not rename generated episode narration blocks just to match sprite naming; those follow the episode/voice/block naming system.
- For current builder work, VBv2 remains the target unless James explicitly asks to update the old original builder. The SFX rename intentionally left the old original builder alone.
- Pending skill proposal from the SFX naming cleanup: `nutrition-video-sync-integrator-20260723-e8e895a739`.

## 2026-07-23 13:44 UTC - Food Entry SFX Profiles

- Food entries can carry `episode.sfxProfile` version 1 with `stampImpact.path`, `sectionTransition.path`, and `highlightGlow.path`. VBv2 should use those paths for the selected food and fall back to the default constants when a profile is missing.
- `scripts/foodranked-generate-episode.js` now assigns future non-explicit entries a stable-random SFX profile from the reusable stamp, transition, and UI/glow pools, so each food can vary while remaining repeatable on regeneration.
- Kale is the manual test entry for the newly used alternate SFX: `audio/sfx/stamps/traditional_stamp_hit.mp3`, `audio/sfx/transitions/freesound_community_retro_spell_sfx_85574.mp3`, and `audio/sfx/ui/freesound_community_magical_background_6892.mp3`.
- James could not hear the Kale alternate transition SFX at the default `0.22` transition volume. VBv2 now supports `sectionTransition.volume`; values above `1` use a Web Audio gain boost, and Kale's retro spell transition is set to `1.5` for review.

## 2026-07-23 15:53 UTC - VBv2 Tier Sprite Rule

- James has uploaded all tier sprites. VBv2 should use the uploaded `S_tier.png`, `A_tier.png`, `B_tier.png`, `C_tier.png`, and `D_tier.png` sprites for all current and future food entries, selected from each food's tier.
- The tier sprites live in `app/sprites/ui/intro_&_outro/` and are mirrored to `docs/app/sprites/ui/intro_&_outro/` for published VBv2 use.
- S-tier outro stamps should feel more premium than the other tiers. Current VBv2 implementation adds brighter gold/mint glow and pixel glimmers around the S-tier stamp.
- Commit `ac690e25` applied tier sprites and S-tier premium VFX to VBv2 only; keep the old original builder untouched unless James explicitly asks for it.
