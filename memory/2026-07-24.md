## 2026-07-24 04:00 UTC

- VBv2 tier outro sprites are now tier-specific across food entries: use the uploaded `S_tier.png`, `A_tier.png`, `B_tier.png`, `C_tier.png`, and `D_tier.png` sprites according to the food's tier. S-tier entries get the extra premium VFX, but keep it restrained: soft glow and dimmer pixel sparkles, not an over-bright flash.
- VBv2 has an S-tier-only stamp SFX: `audio/sfx/stamps/s_tier_stamp_level_up.mp3`, mirrored at `docs/audio/sfx/stamps/s_tier_stamp_level_up.mp3`. It should play only when the actual `S_tier.png` stamp reveals, never for A/B/C/D tier stamps. Current S-tier level-up volume is intentionally quiet at about `0.22`.
- VBv2 intro/outro normal stamp hit should be loud and low-pitched. Current stamp hit setting is gain/volume `1.5` with playback rate around `0.56` to `0.68`; values above browser volume `1` are handled through Web Audio gain. This applies to the regular stamp hit for intro and outro stamp moments.

## 2026-07-24 09:16 UTC

- VBv2 audio note superseding the 04:00 stamp-hit volume note: after today's SFX tuning, do not use the old `1.5` regular stamp-hit setting as the desired state.
- Treat the intro/outro explosion/ranked impact cue and the food stamp/traditional stamp cue as separate audio controls. The explosion/ranked impact SFX was considered perfect before this tuning pass; do not change its timing or volume unless James explicitly asks.
- Explosion/ranked impact SFX rule: intro and outro should use the same default timing/volume path, and the two intro explosion hits must stay the same volume as each other and the outro equivalent for the same audio file. Current locked baseline in `docs/video-builder-v2/video-builder-v2.js`: `STAMP_SFX_VOLUME = 0.18`, `STAMP_SFX_VOLUME_VARIATION = 0`, `STAMP_SFX_LEAD_SECONDS = 0.1`.
- Stamp SFX rule: do not create separate volumes for the same stamp audio file. The traditional stamp file is intentionally louder everywhere it is used: `traditional_stamp_hit.mp3` has fixed volume `0.24`. The default `impact_stamp_hit.mp3` baseline remains `0.18`.
- Traditional stamp sync: `traditional_stamp_hit.mp3` has a fixed `0.54s` start offset to trim baked-in silence, and intro food-stamp timing is the only special stamp timing path: `INTRO_FOOD_STAMP_SFX_LEAD_SECONDS = 0.2`; negative intro stamp events are triggered immediately at playback start.
- S-tier outro stays separate: suppress the regular stampImpact SFX on the S-tier tier reveal and use only `audio/sfx/stamps/s_tier_stamp_level_up.mp3`. Current S-tier special SFX settings are `S_TIER_STAMP_SFX_VOLUME = 0.36` and `S_TIER_STAMP_SFX_LEAD_SECONDS = 0.16`.
- VBv2 playback-lag trims made today: cache SFX event schedules, avoid rebuilding scene lists/control panels every frame, and stop recalculating the audio status label every frame.

## 2026-07-24 10:07 UTC

- VBv2 audio note superseding the 09:16 traditional-stamp volume: `traditional_stamp_hit.mp3` is now fixed volume `0.35` wherever used. Keep the `0.54s` trim and intro food-stamp early/immediate playback path. Default `impact_stamp_hit.mp3` / explosion-ranked impact stays locked at `0.18`, no variation, and normal default/outro timing.
- Narration volumes were checked in VBv2 for Bacon and Kale: playback volume is `1` and there are no per-food narration volume fields in `foods-index`.
- Highlight-glow SFX are now intentionally food-specific: Bacon uses `audio/sfx/ui/highlight_glow_loop.mp3`; Kale uses `audio/sfx/ui/freesound_community_magical_background_6892.mp3`. VBv2 includes a per-food `highlightGlow` reset so Kale cannot reuse Bacon's previous UI loop.
- Latest pushed VBv2 audio commits from this pass: `89383651`, `c8d212cd`, `470af0fe`.

## 2026-07-24 11:36 UTC

- VBv2 audio note superseding the 10:07 traditional-stamp volume: `audio/sfx/stamps/traditional_stamp_hit.mp3` is now fixed volume `0.45` wherever used. Keep its `0.54s` source start offset and file-level stamp timing settings. `audio/sfx/stamps/impact_stamp_hit.mp3` remains the default stamp/explosion-ranked impact asset at `0.18`.
- Adam narration is now a voice-label-level setting, not a one-food tweak. `ADAM_NARRATION_VOLUME = 0.7` in `docs/video-builder-v2/video-builder-v2.js`, and generators stamp Adam entries with `narrationVolume: 0.7`; non-Adam narration remains `1.0` unless explicitly configured.
- Reusable SFX settings now live by audio file path in `SFX_ASSET_SETTINGS` in `docs/video-builder-v2/video-builder-v2.js`. Future foods should assign SFX file paths and inherit file-level settings for stamp impact, section transition, and UI glow rather than duplicating volume/timing values per food.
- Current reusable SFX pools for new/other foods are stable-random per food entry: stamp impact chooses between `audio/sfx/stamps/impact_stamp_hit.mp3` and `audio/sfx/stamps/traditional_stamp_hit.mp3`; section transition chooses between `audio/sfx/transitions/section_transition_whoosh.mp3` and `audio/sfx/transitions/freesound_community_retro_spell_sfx_85574.mp3`; highlight glow chooses between `audio/sfx/ui/highlight_glow_loop.mp3` and `audio/sfx/ui/freesound_community_magical_background_6892.mp3`.
- Bacon and Kale remain explicit/manual SFX profiles after the auto-assignment pass: Bacon keeps `highlight_glow_loop.mp3`; Kale keeps `freesound_community_magical_background_6892.mp3`. Stable-random SFX assignment applies to the other episode-backed foods, with paths generated by `scripts/generate-dashboard-data.js` and `scripts/foodranked-generate-episode.js`.
- Transition SFX have file-level scheduled offsets now: `timeOffsetSeconds: 0` for both transition files. UI glow files have file-level fade/playback-rate settings, but no fixed scheduled play time because they follow highlight cue windows.
- Latest pushed VBv2 audio/profile commits after 10:07: `86719304`, `d244fb7e`, `a84ead4b`, `89938acb`, `fa9aa403`, `67cd763d`.

## 2026-07-24 11:50 UTC

- Memory correction for VBv2 audio: the 04:00 note saying regular stamp hit volume `1.5` is obsolete; the current source of truth is the later file-level asset settings. Treat old volume notes as superseded when they conflict with the latest VBv2 code/memory.
- Current VBv2 reusable audio profile rules remain: `traditional_stamp_hit.mp3` volume `0.45` with `0.54s` source trim; `impact_stamp_hit.mp3` / explosion-ranked impact volume `0.18`; `ADAM_NARRATION_VOLUME = 0.7`; transition file offsets `timeOffsetSeconds: 0`; UI glows follow highlight cue windows.
- James approved turning the reusable SFX/Adam audio-profile workflow into a skill. A draft/update now exists in `skills/nutrition-workflow-automator/SKILL.md`, but it is a local dirty change and replaced older workflow/ElevenLabs guidance in that file; review/merge it carefully before committing so useful prior automation guidance is not lost.
- Current local dirty files after the audio-profile work/memory fix: `memory/2026-07-23.md`, `memory/2026-07-24.md`, and `skills/nutrition-workflow-automator/SKILL.md`. During memory flushes, only append to `memory/2026-07-24.md` and leave bootstrap/reference files untouched.

## 2026-07-24 12:41 UTC

- OpenClaw memory was fixed after the 11:50 flush by restarting the gateway; CLI memory search and chat-level `memory_search` both worked afterward against `memory/2026-07-24.md`.
- The reusable FoodRanked audio-profile skill proposal was saved and left pending with id `nutrition-workflow-automator-20260724-0bb0753fed`.
- VBv2 SFX profile persistence fix: commit `bb59bad2` (`Persist randomized SFX profiles per food`) added explicit `sfxProfile` assignments to every food entry, not just generated indexes. Coverage after the fix: `foods/` 274/274, `docs/data/foods/` 274/274, existing episode manifests 158/158, generated episode index 156/156. Bacon and Kale remain manually pinned; other foods use stable-random stamp/UI/transition choices from the two-file pools.
- Stable SFX randomization was strengthened to use all 8 combinations across stamp impact, section transition, and highlight glow instead of collapsing most foods to the same bundle. Future foods should persist a concrete `sfxProfile` path selection per entry while inheriting sound settings from file-level `SFX_ASSET_SETTINGS`.
- D-tier outro special SFX were added in commit `65f9b338` (`Add D-tier outro special SFX`). New renamed stamp files: `audio/sfx/stamps/d_tier_game_lose.mp3` and `audio/sfx/stamps/d_tier_death_collapse.mp3`, mirrored under `docs/audio/sfx/stamps/`.
- D-tier outro rule: treat these like the S-tier special outro path. Suppress normal stamp-impact SFX on D-tier tier reveal, play `d_tier_game_lose.mp3`, and start `d_tier_death_collapse.mp3` halfway through the game-lose sound. Measured game-lose duration was `4.284s`, so the death-collapse start is `2.142s`.
- S-tier special SFX remains file-level and reusable: all S-tier reveals should share Kale-configured `audio/sfx/stamps/s_tier_stamp_level_up.mp3` settings from `SFX_ASSET_SETTINGS`.
- The Google Doc `FoodRanked schema` was updated after the D-tier/SFX work with the current VBv2 audio schema rules: file-level SFX settings, `sfxProfile v1`, S-tier and D-tier special outro SFX behavior, and Adam narration volume locked at `0.7`.

## 2026-07-24 13:32 UTC

- D-tier special outro SFX current source of truth: `d_tier_game_lose.mp3` and `d_tier_death_collapse.mp3` are configured file-level in `docs/video-builder-v2/video-builder-v2.js` via `SFX_ASSET_SETTINGS`, both at volume `0.28`. The game-lose cue uses `leadSeconds: 0.5`; the death-collapse cue is scheduled halfway through the game-lose audio.
- D-tier death SFX reliability fix: commit `ba6f033b` (`Ensure D-tier death SFX plays`) added a fallback D-tier SFX event so D-tier foods without DBv2 placement data, including Baked Beans, still play the special D-tier outro pair. The death-collapse sound is now scheduled from the game-lose audio itself rather than depending on a visible tier stamp playhead event.
- D-tier timing/volume tuning commits after initial add: `b73dd6e8` tightened the D-tier game-lose lead to `0.26s`; `3b34c3af` tightened it to `0.5s` and primed/preloaded both D-tier files; `94f3564b` lowered both D-tier SFX volumes to `0.28`.
- Baked Beans display fix: commit `7675beea` (`Fit Baked Beans header name`) caps the `BAKED BEANS` header name at `7` layout pixels so it stays on one line in Display Builder v2. The app, DBv2, and VBv2 loaders were cache-busted for the change.

## 2026-07-24 14:16 UTC

- Baked Beans header memory correction: the 13:32 note with a `7` layout-pixel cap was not enough in VBv2. Commit `586918ed` later lowered the `BAKED BEANS` header cap to `6.2`, made header food-name layers no-wrap in app/VBv2, restored VBv2 header syncing, and browser-verified Baked Beans as one line.
- Background music loop beds were added from five uploaded `audio/music` files in commit `586918ed` and tightened in commit `31e1fa7b`. Current loop files are `audio/music/*_loop_240s.mp3` with byte-matching mirrors in `docs/audio/music/`; each output is slightly over `240s`, decodes cleanly, starts/ends with active audio, and was rebuilt from boundary-cleaned sources with wraparound crossfading to remove silent/dead loop seams.
- Current background music pool in `scripts/lib/music-profiles.js`: `freesound_community_8bit_sample_69080_loop_240s.mp3`, `hauntsync_retro_chiptune_adventure_318059_loop_240s.mp3`, `lucadialessandro_arcade_melody_295434_loop_240s.mp3`, `retro_bgm_chan_low_level_enemy_534609_loop_240s.mp3`, and `retro_bgm_chan_vs_robbot_vs_534622_loop_240s.mp3`.
- Commit `e1a7140d` (`Assign background music profiles`) added stable-random `musicProfile` assignment per food from the five background music loop files. Coverage verified at the time: 274 source foods, 274 published foods, 158 existing episode manifests, and 156 foods-index episode entries. New episode generation and dashboard generation call `completeMusicProfile`.
- VBv2 background music playback source of truth: `docs/video-builder-v2/video-builder-v2.js` resolves `food.episode.musicProfile || food.musicProfile`, loads the `backgroundMusic` role, loops it under playback, and uses `BACKGROUND_MUSIC_VOLUME = 0.14` unless a role-specific volume is configured.

## 2026-07-24 14:43 UTC

- Finalised background-music calibration plan from James: the five finalised foods should cover the five music loop files once each before music-volume tuning. Bacon keeps `audio/music/retro_bgm_chan_low_level_enemy_534609_loop_240s.mp3`; Kale keeps `audio/music/hauntsync_retro_chiptune_adventure_318059_loop_240s.mp3`; Raspberries is being pinned to `audio/music/freesound_community_8bit_sample_69080_loop_240s.mp3`; Buckwheat should keep/use `audio/music/lucadialessandro_arcade_melody_295434_loop_240s.mp3`; Black Beans should use the remaining `audio/music/retro_bgm_chan_vs_robbot_vs_534622_loop_240s.mp3`.

## 2026-07-24 20:40 UTC

- Raspberries was generated/finalised as a compact A-tier episode in commit `9ced4b9d` (`Generate Raspberries compact script`). It has 11 narration blocks, exactly 3 pros and 3 cons, and is manually pinned to `audio/music/freesound_community_8bit_sample_69080_loop_240s.mp3` as part of the five-finalised-food music calibration plan.
- Raspberries script note: closing strengths were corrected to fibre, vitamin C, and anthocyanin-rich berry pigments. Do not promote tiny protein/fat signals as "big strengths" in fruit/low-protein closings.
- Fruit starch narration rule from commit `345302da` (`Avoid low fruit starch narration weakness`): low starch is normal for many fruits, especially berries, and should not be spoken as a meaningful weakness. Keep the visible starch row/arrow if generated by the locked display rules, but fruit narration should focus on sugar moderation, fibre, glycemic impact, vitamins, and minerals instead of calling low starch a downside.
- Next finalised scripts in the current plan are Buckwheat, then Black Beans. Maintain unique music coverage across the five finalised foods: Bacon, Kale, Raspberries, Buckwheat, and Black Beans should each use a different one of the five looped background music files before volume calibration.
