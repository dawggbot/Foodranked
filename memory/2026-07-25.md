# 2026-07-25

## FoodRanked Memory Flush - 03:22 UTC

- Latest pushed repo state in this flush sequence is commit `9c2ad618` (`Add targeted narration exception guards`), after a long VBv2/audio/profile generation pass. Existing unrelated local dirty files seen repeatedly were memory files and `skills/nutrition-workflow-automator/SKILL.md`; avoid touching/reverting them unless James asks.
- Reusable audio-profile skill proposal was created and left pending as `nutrition-workflow-automator-20260724-0bb0753fed`. It captures the SFX/audio-lock/browser-probe workflow.
- VBv2 reusable audio settings are now file-level, mainly in `docs/video-builder-v2/video-builder-v2.js` via asset settings. Future foods should point at audio file paths and inherit settings from the file, not duplicate volume/timing per food unless James is calibrating a new file.
- Current stamp/S-tier/D-tier core audio settings: default `impact_stamp_hit.mp3` remains `0.18`; `traditional_stamp_hit.mp3` is `0.45` with its `0.54s` source trim; Adam narration is voice-level `0.7`; S-tier uses `s_tier_stamp_level_up.mp3` with file-level config and no normal stamp underneath; D-tier uses `d_tier_game_lose.mp3` and `d_tier_death_collapse.mp3`, both volume `0.28`, game-lose lead `0.5s`, and death-collapse starts halfway through game-lose (`2.142s` after game-lose start). Normal stamp SFX is suppressed on S-tier and D-tier special outros.
- D-tier special SFX fallback was fixed for foods without DBv2 placement, including Baked Beans. Baked Beans browser smoke confirmed both `d_tier_game_lose.mp3` and `d_tier_death_collapse.mp3` play at `0.28`.
- Bacon/Kale SFX calibrations are intentional anchors: Bacon uses UI glow `audio/sfx/ui/highlight_glow_loop.mp3`; Kale uses `audio/sfx/ui/freesound_community_magical_background_6892.mp3`; Kale traditional stamp stays the calibrated loud stamp. The UI glow reset bug was fixed so one food cannot carry a previous food's UI loop into the next.
- Stable profile assignment exists for current and future foods: `sfxProfile` picks stamp/transition/UI glow from the pools, `musicProfile` picks one of five music loops, and `voiceProfile` stores stable `random_suitable` narrator selection. New generation paths should call the completion helpers and preserve explicit manual pins.
- Five background music beds were created as seamless-ish 240s loop files in `audio/music/*_loop_240s.mp3` with matching mirrors in `docs/audio/music/`. Each was boundary-cleaned and wraparound-crossfaded so starts/ends have active audio. VBv2 plays assigned background music looped at file-level music volume, currently `0.14` for all five. The toolbar status line was moved below the scrubber so the progress slider remains visible.
- Current music pool: `freesound_community_8bit_sample_69080_loop_240s.mp3`, `hauntsync_retro_chiptune_adventure_318059_loop_240s.mp3`, `lucadialessandro_arcade_melody_295434_loop_240s.mp3`, `retro_bgm_chan_low_level_enemy_534609_loop_240s.mp3`, and `retro_bgm_chan_vs_robbot_vs_534622_loop_240s.mp3`.
- Five-food music calibration plan from James: Bacon uses `retro_bgm_chan_low_level_enemy_534609_loop_240s.mp3`; Kale uses `hauntsync_retro_chiptune_adventure_318059_loop_240s.mp3`; Raspberries is pinned to `freesound_community_8bit_sample_69080_loop_240s.mp3`; Buckwheat should use `lucadialessandro_arcade_melody_295434_loop_240s.mp3`; Black Beans should use `retro_bgm_chan_vs_robbot_vs_534622_loop_240s.mp3`. Keep these five unique before music-volume calibration.
- Raspberries was generated/finalised as compact A-tier in commit `9ced4b9d`; it has exactly 3 pros and 3 cons, 11 narration blocks, and corrected strengths of fibre, vitamin C, and anthocyanin-rich berry pigments. Do not promote tiny protein/fat signals as major fruit strengths.
- Food-type narration exceptions are now narrow and narration-only. Current targeted weak-callout guards: fruit and dairy low starch should not be spoken as a weakness; plant foods should not use low Vitamin B12 as a weak vitamin callout; fruit/veg/tubers below useful protein threshold avoid weak amino-acid/bioavailability callouts; low-fat fruit/veg/tubers/grains avoid weak omega-3/polyunsaturated-fat callouts; `0mg` cholesterol is not used as a spoken fat standout outside meats/dairy. Visible rows/arrows stay unchanged.
- James explicitly corrected that broad cross-food-type exceptions should not be added casually. Add future exceptions only when a submacro or micro is unfair/awkward for a specific food type, like starch for fruits, and prefer narration guardrails before score changes.
- Baked Beans header was adjusted so the food name stays on one line: font cap lowered to `6.2`, header name layers made no-wrap, and VBv2 header syncing restored/cache-busted.
- Google Doc `FoodRanked schema` was updated during the audio pass with VBv2 audio schema rules, file-level SFX settings, `sfxProfile v1`, S-tier/D-tier special outro behavior, Adam narration `0.7`, and final D-tier audio settings.
