# 2026-07-28

## FoodRanked Session Notes

- Music profiles were applied across the rest of the food entries and exposed through the public index: 274 source foods, 274 published foods, 158 episode manifests, and 274 public index entries. Commit: `a1cc9900` (`Apply music profiles to public index`).
- Final tier narration display/subtitles should remain `A tier.`, but TTS/cache validation should use pronunciation overrides rather than trusting stale audio. Black Beans specifically needed `ttsText: "Ay tier!"` and `voice-v2` to avoid sounding like lowercase "a tier". Commit: `52eeb050`.
- VBv2 now has a video `Download` button. It records from the start, hides builder UI during recording, restores the previous preview state afterward, prefers MP4 when supported, and falls back to WebM. Initial implementation commit: `04a8013a`.
- VBv2 download recording was changed from browser/window capture to direct recording of the stage canvas at `1080x1920` 9:16 so exported videos match the preview canvas and do not include extra browser/window space. It no longer relies on `getDisplayMedia` for the main recording path. Commit: `ff7445c4` (`Record VBv2 downloads from stage canvas`).
- VBv2 download export was refined again to crop from the same phone-shell preview window rather than the full hidden stage, removing extra right/bottom canvas space. Test export crop observed as `409x732` source scaled into a `1080x1920` MP4 stream. Commit: `5fb3f883` (`Crop VBv2 downloads to preview frame`).
- DBv2 and Layout Builder were checked for the same oversized export/canvas issue; no matching download/export path needed changes. DBv2 has no video/image recorder path, and Layout Builder's iframe canvas/shell already measured as the same visible rectangle after zoom/crop.
- VBv2 download was later changed to MP4-only. The button is now `Download MP4`, no WebM fallback, and export preloads stage sprites, tier stamps, macro GIF assets, and fonts from frame 0 before encoding. If a sprite fails and has a valid fallback, the fallback is used before the MP4 starts. Commit: `c11c7c98` (`Make VBv2 downloads MP4-only`).
- Video DownloadHelper on Firefox is not a good fit for VBv2 because VBv2 generates video from canvas rather than serving an existing MP4/HLS stream. For direct MP4 exports, James should use Windows Chrome or Edge and wait for the MP4-ready state before clicking `Download MP4`; browsers without `MediaRecorder` MP4 support cannot make the direct MP4.
- VBv2 intro now applies a focus blur to the stat sheet/background during the stamp intro while keeping the food image, `RANKED` sprite, glow, and glimmers crisp. The blur fades out during the intro and is gone by the next section. Commit: `33fb0b02` (`Add VBv2 intro focus blur`).
- VBv2 intro focus blur was increased after review; the blur strength was bumped from `1.16` to `2` while keeping the food image and `RANKED` stamp sprites sharp. Commit: `8bc17a94` (`Increase VBv2 intro focus blur`).
- DBv2 now shows the intro/outro stamp sprites statically, without animations, and keeps them at VBv2-style upscaled sizes. Intro shows `RANKED` at `80x80` and the food image at `48x24`; outro shows the tier stamp at `78x78` plus like/follow/share stamps. Commit: `7348f8a9` (`Show static intro outro stamps in DBv2`).
- Bacon's collagen wording was first patched to remove the repeated connective-tissue-protein phrasing and generated as `voice-v11`, but the current source of truth is the later full rewrite below. Earlier patch commit: `c7594377` (`Fix Bacon collagen narration repeat`).
- DBv2 also received the intro focus blur treatment: background/stat sheet blur during intro while the `RANKED` sprite and food image stay sharp. This landed with the Bacon rewrite. Commit: `67cc41e1` (`Refresh Bacon narration and DBv2 intro blur`).
- Bacon's full script/narration was rewritten with a durable `script-overrides.json`, regenerated as `voice-v12`, force-aligned, and refreshed into `docs/data/foods-index.*`. Targeted checks confirmed no connective-tissue repeat, one collagen mention, exactly 3 pros and 3 cons. Commit: `67cc41e1`.
- During the Bacon/DBv2 push, the push helper also auto-created/pushed `8203eee3` from pre-existing dirty memory/skill files; afterward the repo was clean and `origin/main` was up to date.
- VBv2's download button was changed again to a real direct-file MP4 download rather than any screen/canvas recording. It now checks for a published file at `docs/video/episodes/<food-id>/<food-id>-vbv2.mp4`, downloads it as `<food-id>-vbv2.mp4` when present, and disables with a missing-MP4 message when absent. Commit: `d38feac1` (`Make VBv2 download use published MP4 files`).
- Added the offline VBv2 MP4 renderer. Run `node scripts/render-vbv2-mp4.js bacon` to generate `docs/video/episodes/bacon/bacon-vbv2.mp4`; replace `bacon` with any food id. The renderer captures the exact VBv2 9:16 stage frame-by-frame, encodes H.264/AAC MP4 at `1080x1920`, and mixes split narration, background music, and VBv2 SFX. Commit: `c79e3dc4` (`Add offline VBv2 MP4 renderer`).
- `ffmpeg` was installed in the workspace for the offline renderer. Smoke verification created `/tmp/bacon-vbv2-sfx-smoke.mp4`, and `ffprobe` confirmed H.264 video, AAC audio, and `1080x1920`.
- Added a local VBv2 render helper so the `Download MP4` button can run the offline renderer and then download the finished file when VBv2 is opened through the helper server. It checks whether the MP4 already exists, starts a render job if missing, waits for completion, then downloads the generated `<food-id>-vbv2.mp4`. Commit: `6ee4cd85` (`Add VBv2 local MP4 render helper`).
- Static GitHub/normal website pages still cannot run local commands; the helper path requires `node scripts/serve-vbv2-render-helper.js`. If the helper URL is blocked, fallback remains running `node scripts/render-vbv2-mp4.js <food-id>` manually and then using the direct MP4 download button.
- Helper access was improved after James could not access the first local URL. Port `4173` was busy, so the helper used port `4174`; the shared access URL given was `http://187.124.112.243:4174/docs/video-builder-v2/index.html`. Raw workspace ports may still be blocked by the webchat/network. Commit: `7b68b504` (`Improve VBv2 render helper access`).
