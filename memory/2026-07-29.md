## 2026-07-29 08:40 UTC

- VBv2 MP4 downloads are now direct file downloads only: the button checks for `docs/video/episodes/<food-id>/<food-id>-vbv2.mp4` and downloads it like a hosted video file. Browser-side canvas/screen recording was removed because James explicitly does not want screenrecord-style output.
- Offline MP4 renderer exists at `scripts/render-vbv2-mp4.js <food-id>`. It renders the VBv2 stage frame-by-frame, mixes split narration, background music, and VBv2 SFX, and encodes H.264/AAC `1080x1920` MP4 output to `docs/video/episodes/<food-id>/<food-id>-vbv2.mp4`.
- Local helper server exists at `scripts/serve-vbv2-render-helper.js` so a local VBv2 page can trigger the renderer then download the MP4. Remote/raw workspace ports may be inaccessible to James; the likely long-term answer is a Windows desktop app bundling Node/ffmpeg.
- DBv2 now has per-section carousel still downloads for Intro, Fats, Carbs, Protein, Vitamins, Minerals, Pros, Cons, and Outro. Export format is PNG to preserve pixel art and text sharpness.
- DBv2 section PNG exports should render macro GIF sprites at the start of their animation for cleaner stills. Arrow indicators must stay pixel-perfect and unstretched; exporter uses aspect-preserving sprite drawing and whole native-pixel multiples, with whole-scale output currently around `1125x2000` rather than forced `1080x1920`.
- Intro focus blur is applied in both VBv2 and DBv2. It blurs the background/stat sheet while keeping the food image and `RANKED` stamp sprites sharp; VBv2 blur intensity was increased to `2` and fades out after the intro.
- DBv2 includes static intro/outro sprites from VBv2 at their upscaled sizes, without adding their animations.
- Generation/verification now enforces videos at `180s` max and uses simpler, more direct narration. After commit `12a92d98`, duration audit had `0` videos over `180s`; Kale was longest public video at `157.572s`.
- Bacon has a refreshed simple script/narration with `voice-v13`, no connective-tissue/collagen repeat, and duration around `94.933s`; narration/subtitle audits were passing.

## 2026-07-29 11:00 UTC

- DBv2 section PNG exports now intentionally use mixed GIF still-frame modes: Vitamins/Minerals micro icons freeze at the first frame, macro section GIFs freeze at the first frame, but macro bar fill sprites freeze at their final/completed frame. Relevant pushed commits: `bdc9a95c` and `de84ed92`.
- White Potato became the current approved narration style benchmark. James said it is "the best so far"; learn from its voice: direct factual blocks, everyday connective phrasing, precise nutrition claims, quick food-category summaries, and practical pros/cons that match the script without just repeating the scoring sections.
- Avoid the rejected wording `fat barely matters unless it has been added`. For tubers, the approved replacement idea is: `fat is mostly a preparation check, not the main job.`
- Pros/cons across food entries were realigned to the updated rule/style: exactly 3 pros and 3 cons, with practical/context copy matching each script and avoiding recap-style repeats of the macro/micro facts. All 156 compact episode outputs and the dashboard index were regenerated in commit `de187082`.
- White Potato final script/audio/video was generated and synced in commit `ce49ac5c`. Audio manifest: `docs/audio/episodes/white-potato/voice-v1-blocks.json`; rendered video: `docs/video/episodes/white-potato/white-potato-vbv2.mp4`; duration about `133.77s`; verification passed with narration/subtitle checks and ffprobe `1080x1920` AAC video output.
- A pending Skill Workshop update was created from the White Potato style: `nutrition-content-studio-20260729-b55bdce0b7`.

## 2026-07-29 11:26 UTC

- James said not to bother with MP4 download work yet because the MP4 output is still extremely broken. Treat VBv2 MP4/download as unresolved despite prior renderer/audio work; do not present it as done until separately fixed.
- Display pros/cons were fixed in pushed commit `6181726f` so generated display data matches the updated context/script pros and cons. `batch-results.json`, `docs/data/batch-results.json`, and leaderboards were regenerated.
- Video Builder v2 now rebinds pro/con text from `food.contextItems` when a layout loads, so stale/manual DBv2 placement exports do not override the canonical source pros/cons. White Potato display should show: `very practical staple food`, `can be fairly filling`, `flexible meal base`, `needs cooking before it works`, `prep method changes it fast`, and `plain flavour often needs help`.
- The broken White Potato MP4 artifact was removed from published docs output for now.

## 2026-07-29 12:26 UTC

- Hazelnuts is an approved script style example. James said it is exactly how he wants scripts to be. Keep the same shape: compact factual scoring blocks, food-category summaries, and practical pros/cons that add real-world context without simply restating macro/micro section facts.
- Hazelnuts audio was generated and synced in pushed commit `95f235e3` with updated narration, `voice-v1` split ElevenLabs audio, 11 blocks, forced-aligned subtitles/timings, refreshed VBv2/dashboard index, and no MP4 render/download work. The S-tier TTS override keeps display text as `S tier.` but speaks `Ess tier!`
- Hazelnuts protein narration was adjusted from the chat draft to mention `Essential amino acids score is 5 out of 9`, because that submetric is on the display and the narration verifier requires shown protein submetrics to be spoken.
- S tier was made strictly the rarest generated tier in pushed commit `b9bded32`. The generated-data audit now enforces this. Current tier counts at that point were `S 36`, `B 37`, `A 47`, `C 69`, `D 85`.
- Chia Seeds script was provided in chat and currently ends as S tier. Key copy: huge omega 3 at `17830 milligrams`, fibre `34.4 grams`, glycemic index `1`, protein `16.5 grams` with useful-protein gate caveat, vitamins low overall, minerals led by magnesium `80%` DV, calcium `49%`, iron `43%`, zinc `42%`; pros are fuller texture helps real meals, ALA omega-3 headline fat, easy to boost meals invisibly; cons are needs enough fluid, phytate absorption caveat, gel texture/digestion can be divisive.

## 2026-07-29 12:56 UTC

- Cashews and Lima Beans were checked after the S-rarity boundary change and still align as B tier: Cashews ranking `60.7209`, display score `60`, reveal `B tier.`; Lima Beans ranking `60.5841`, display score `60`, reveal `B tier.` A now starts at `61`, so both remain correctly just below the boundary.
- New display rule from James: if any milligram-based display score/value, currently omega 3 and cholesterol, reaches 5 digits, convert the display text to grams so it fits. Source/scoring values stay in milligrams. Pushed commit `17ef61a2` implemented this for long mg display values: Chia Seeds `17830mg -> 17.83g`, Flax Seeds `22813mg -> 22.81g`, Flaxseed Oil `53300mg -> 53.3g`, and Ground Flaxseed `22800mg -> 22.8g`.
- Chia Seeds narration was generated and synced in pushed commit `66f73c40`, with `voice-v1` split narration using `Bill - Wise, Mature, Balanced`, 11 blocks, 72 subtitle cues, duration about `159.77s`, and no MP4 export/download work. Chia now shows/speaks `17.83g` for omega 3, while the final reveal displays `S tier.` and uses `Ess tier!` for TTS clarity.

## 2026-07-29 13:38 UTC

- Greek Yogurt became another approved script/style example; James said the scripts are now "so good". Greek Yogurt narration/display was synced in pushed commit `f41e5810` with the approved script, updated displayed pros/cons, `voice-v1` using `Lily - Velvety Actress`, 11 split blocks, 80 aligned subtitle cues, duration about `161.77s`, and no MP4 export/download work.
- Greek Yogurt cleanup also refreshed affected food entries so displayed pros/cons match the newer practical/context style, including removing older `not universal` wording where it was still appearing.
- Extra Virgin Olive Oil script was approved except for two display/script tweaks. The display/header name is intentionally shortened to `xtra vrgn olive oil`, rendering as `XTRA VRGN OLIVE OIL` and fitting the actual header layer at auto font size `7`.
- EVOO's polyphenol pro was simplified to `brings polyphenols when fresh` on both display and script. Pushed commit `a285d67a` refreshed the EVOO generated episode/script outputs and checks passed. Likely next step after James reviews is to generate/sync EVOO narration, still with no MP4 export/download work.

## 2026-07-29 14:26 UTC

- EVOO header fitting was corrected after James pointed out unused space before `PER 100G`. The old fitter only used the food-name textbox width; DBv2 and VBv2 now let EVOO use the actual available gap up to the per-100g label. It now renders as `XTRA VIRGIN OLIVE OIL`, not the shortened `XTRA VRGN OLIVE OIL`.
- EVOO narration was generated and synced in pushed commit `8e7156b5` (`Sync EVOO narration and header fit`): `voice-v1`, `Eric - Smooth, Trustworthy`, 11 split blocks, 77 subtitle cues, synced duration about `136.5s`, and no MP4 export/download work.
- EVOO checks passed after sync: JS syntax, narration/subtitles, generated data audit, pros/cons title fit, VBv2 smoke test, and diff whitespace. MP4 export/download remains intentionally out of scope until the broken MP4 workflow is fixed separately.

## 2026-07-29 15:39 UTC

- James decided it was unfair for Regular Cola and Banana to share D tier, especially with Cola scoring better than Banana. A new `Slop tier` now exists below D for foods that are straight-up negative to consume, such as regular cola, ultra-processed packaged snacks, and similar cases. Slop tier uses negative scoring/display values.
- Slop tier implementation was pushed; latest pushed head was `e933fc7f`, with project work commit `d17aad90` and final Regular Cola sync included. Regular Cola is now `Slop tier.` with public tier score `-20` and displayed rounded ranking score `-11`; Banana remains `D tier` with display score `17`.
- New D tier sprite and new Slop sprite are wired into the display/video builders. D tier now uses the normal intro stamp SFX; Slop tier uses the previous D fail/death SFX.
- Tier counts after adding Slop were `Slop 33`, `S 36`, `B 37`, `A 47`, `C 64`, `D 57`. S remains rarest among normal tiers, while Slop is a smaller negative bucket.
- Regular Cola narration was updated/synced for the new Slop ending: `voice-v1`, 11 split blocks, 85 subtitle cues, about `151.13s` audio plus dwell, no MP4 export/download. Checks passed: syntax, generated-data audit, Cola narration/subtitles, pro/con title fit, whitespace, and Playwright smoke for Cola/Banana sprites and SFX.

## 2026-07-29 16:18 UTC

- James asked to remember carotenoids as worth mentioning in pros/cons when source-backed. A pending Skill Workshop update was created: `nutrition-content-studio-20260729-f6c128c670`. It adds named carotenoid context such as beta-carotene, lutein, zeaxanthin, and lycopene where relevant.
- Slop tier presentation was expanded in pushed commit `216b68fe`: the final script/narration ending should say exactly `SLOP` rather than `Slop tier.` or `SLOP TIER`.
- The updated uploaded Slop sprite is mirrored into docs and measured `62x31`; Slop stamps render much larger, with a `128` grid-pixel target width, about `512px` wide at `pixelUnit=4` and roughly 95% of the screen grid.
- Slop tier now has negative-only visual effects: sickly vignette, corruption bars, drips, and harsher stamp glow. Slop keeps the old D fail/death SFX, while normal D stays on the regular stamp route.
- Regular Cola was regenerated/resynced after the `SLOP` ending and larger Slop stamp changes: `voice-v1`, 11 split blocks, 85 subtitle cues, about `152.731s`; no MP4 export/download.
- A reusable tier-migration workflow skill proposal was created in Skill Workshop: `foodranked-tier-migration-workflow-20260729-ebf32c9fc1`. It captures the checklist for adding/changing a tier across scorer rules, generated data, display/video builders, SFX routing, narration sync, audit guards, and Playwright smoke checks.

## 2026-07-29 16:58 UTC

- James reuploaded the Slop sprite again, but the source and docs copies were already identical: `62x31` with matching hashes, so no sprite content change was needed.
- Some of the 11 approved sample foods had broken narration after regenerated text drifted away from already-approved split audio takes. Fixed foods: Black Beans, Buckwheat, Kale, Raspberries, plus Bacon because its latest take matched text but had no active alignment pointer.
- Script overrides were added for the four drifted foods so future regeneration keeps their approved narration text stable. Bacon's active alignment pointer was restored.
- Subtitle normalization was patched so spoken `9 out of 9` can remain audio-only while subtitles/display show `9/9`.
- Verification after the narration repair: all 11 sample foods had active audio blocks matching `narration.txt`, all 11 had positive synced split-audio durations, VBv2 smoke loaded Bacon, Black Beans, Buckwheat, Kale, and Raspberries as `split ready · synced`, narration/subtitle verifier passed, generated data audit had `0` errors, pro/con title fit passed, and `git diff --check` passed.
- Narration fix commit was `094caa2e`; latest pushed head reported as `efc0e667` because the push helper also picked up pre-existing local skill/memory changes.

## 2026-07-29 17:14 UTC

- FoodRanked display/video canvas size is `135 x 240` author grid units, exported as `1080 x 1920px`; `1` grid unit equals `8px` in final render.
- Slop outro stamp bounds were fixed in pushed commit `a265aeb0` (`Tune Slop outro stamp and score glow`). The Slop sprite source remains `62 x 31`; the outro stamp renders at `128` grid units wide, about `1024px` on export, leaving about `3.5` grid units margin per side at rest.
- The Slop out-of-bounds issue came from stamp animation scaling the large stamp to about `1.62x`. Slop now has its own tighter stamp motion and is forced to stay centered on the visible canvas.
- Negative outro scores now use a brown/dirty amber glow instead of the normal positive-score glow.
- Checks after the Slop centering/glow change passed: JS syntax, `git diff --check`, and Playwright bounds/glow verification. No MP4 export/download work was done.

## 2026-07-29 18:08 UTC

- Slop stamp still overflowed the phone border because the old author canvas was wider than the visible phone frame. Pushed commit `f97fef3a` (`Keep Slop stamp inside outro frame`) sized Slop against the actual visible frame with side padding and tightened its glow. At that point the visible phone frame measured about `101.36` grid units wide, Slop rendered around `83.36` grid units wide, with about `9` grid units of box margin per side and about `4.5` grid units after glow/shadow.
- James then asked to remove the extra hidden canvas space entirely because it was the biggest cause of broken video downloads. Pushed commit `a7285b9f` changed the Layout Builder, DBv2, and VBv2 to use the exact visible phone frame canvas instead of the old oversized `135 x 240` author canvas.
- The new canonical builder canvas is `105 x 186.667` visible-frame grid units and exports/renders as exact `1080 x 1920`. DBv2 no longer exports from the oversized hidden canvas; VBv2 no longer relies on the `7/9` crop wrapper.
- Verification after the exact-canvas migration passed: JS syntax, generated data audit, Regular Cola narration/subtitles, pro/con title fit, DBv2 export dimensions, VBv2/layout-builder browser measurements, MP4 probe at `1080x1920`, and `git diff --check`.
- Latest pushed head after the canvas migration was reported as `81130abc`, because the push helper also committed the existing memory note after the builder fix.

## 2026-07-29 18:29 UTC

- James wanted more space between the large Slop outro stamp and the share/like/follow icon sprite. Pushed commit `be75b9db` moved only the Slop stamp up by `3` grid units, leaving the icon sprite position unchanged.
- After the spacing change, both VBv2 and DBv2 Regular Cola outro layouts measured about `7` grid units of gap between the Slop stamp and the share/like/follow icons. DBv2 was also fixed so the Slop stamp uses the same visible-frame width rule as VBv2 and stays inside the exact canvas.
- Verification after the Slop spacing change: VBv2 gap about `7` grid units, DBv2 gap about `7.002` grid units, DBv2 export still `1080x1920`, JS syntax passed, `git diff --check` passed, and Regular Cola narration/subtitle check passed. No MP4 export/download was done.
- James reuploaded the Slop sprite again afterward, but no new sprite content had landed locally: `sprites/ui/intro_&_outro/slop.png` and `docs/app/sprites/ui/intro_&_outro/slop.png` were still byte-identical, both `62x31`, with matching hashes. No sprite wiring change was needed.
