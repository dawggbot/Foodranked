# 2026-07-30

- Fixed VBv2/webpage lag and pushed `c67d71ac` (`Fix web builder lag`). Main causes were repeated deep cloning of the shared DB/asset manifest during render and macro-bar GIF frames being generated in one burst. Changes included lazy cached DB reads in `docs/app/foodranked-database.js`, avoiding default DB clones for normal sprite/audio paths, batching macro-bar GIF frame generation and coalescing render refreshes in `docs/video-builder-v2/video-builder-v2.js`, plus cache-busting affected HTML script URLs. Verification passed with `node --check`, `git diff --check`, and Playwright smoke/perf checks; local VBv2 load improved from about 29s with a 24.5s main-thread freeze to about 0.88s load, max long task about 189ms, and search input about 2.6ms.
- Fixed VBv2 MP4 parity and pushed `172d4956` (`Fix VBv2 MP4 parity`) plus `294842f2` (`Force fresh VBv2 helper renders`). Root causes: MP4 renderer was not receiving the live VBv2/DBv2 layout and fell back to default layout, causing missing section indicators/bar fill sprite and misplaced sprites; GIF/CSS animations advanced on browser wall-clock time during capture, causing macro icons and dynamic backgrounds to play too fast; dynamic background used preview-sized CSS pixels and shrank in 1080x1920 export; audio mixing used raw narration offsets instead of the VBv2 scene/subtitle timeline; existing MP4s could be served stale instead of regenerated. Changed `docs/video-builder-v2/video-builder-v2.js`, `scripts/render-vbv2-mp4.js`, `scripts/serve-vbv2-render-helper.js`, and `docs/video-builder-v2/index.html`. Verification passed with `node --check`, `git diff --check`, seeded visual smoke render, audio-enabled smoke render, and helper POST smoke. Next local VBv2 "Download MP4" forces a fresh file from the current VBv2/DBv2 layout.
- After James reported "0 change to the mp4s", confirmed the code fixes were present but the checked-in MP4 asset itself was stale. Regenerated and pushed `docs/video/episodes/bacon/bacon-vbv2.mp4` in `da6ac906` (`Regenerate VBv2 MP4 with renderer parity fixes`); file size changed from `20,182,362` bytes to `23,360,117` bytes. Finished MP4 frame inspection confirmed section indicators, macro bar fill sprite, corrected GIF/background timing, full-size dynamic background, and narration/subtitle sync at the `92.494s` closing-summary start. Verification included `node --check`, `git diff --check`, full bacon MP4 render, `ffprobe`, and extracted-frame inspection from the rendered MP4. Push helper also added memory-only commit `742a03d2`, separate from code/MP4 changes.
- James confirmed the regenerated MP4 fixed background, macro icon GIF timing, subtitles, and narration, but layout and Bacon pro/con behavior still needed work. Fixed and pushed `98488784`: MP4 rendering now requires live DBv2/VBv2 `layoutPlacement` and refuses to render with the repo-default fallback layout; the helper API returns HTTP 400 when `layoutPlacement` is missing; VBv2 no-layout smoke shows no DBv2 placement rather than silently rendering a wrong layout; stale tracked Bacon MP4 was removed so downloads cannot keep serving an old asset. Bacon was regenerated with `voice-v14-blocks` so each displayed pro/con title is spoken word-for-word first, then followed by its detail sentence, restoring pro/con highlight timing because VFX matching can find the exact display phrases. Bacon pros are still `minor` row highlights, while cons are `major`. Verification included `node --check` for DBv2/VBv2/renderer/helper, `git diff --check`, Bacon episode regeneration, dashboard data, app asset manifest, new Bacon voice generation and forced alignment, no-layout CLI failure, helper 400 rejection, and clean VBv2 load checks.
- Fixed James's follow-up issue where Bacon MP4 download was blocked after requiring live layout placement, and pushed `089d8c0e` (`Fix Bacon VBv2 download and take`). VBv2 now accepts valid DBv2 placement exports even when generated under an older build id, and the MP4 button can retry/fetch DBv2 placement instead of staying disabled. Bacon now uses `voice-v15`: intro/outro restored from the earlier good take, pros/cons retain corrected word-for-word displayed-title narration followed by detail sentences. Verification confirmed 3 `major-con-siren` SFX events; Bacon still has 0 major-pro sparkles because its pros remain `minor` in the data. Full Bacon MP4 was not re-committed from a temporary/default layout to avoid reintroducing layout drift; next local VBv2 download should render from the browser's DBv2/VBv2 placement.
- After James reported no visible change and no major pro/con SFX across the sample foods, fixed and pushed `b33462f5` (`Fix VBv2 pro/con sfx and Bacon intro`). Root cause: the MP4 renderer only fired major pro/con SFX/glow cues when narration matched the exact on-screen pro/con phrase; restored a fallback row-timing window so major pro/con SFX and glow cues return without changing any food entry files. Bacon's rushed intro was MP4-side audio clipping caused by render metadata shorter than the actual `Bacon!` audio file; the renderer now checks real MP3 duration and keeps the full intro take. Generated review MP4 `docs/video/episodes/bacon/bacon-vbv2-check.mp4` for James to inspect. Verification included `node --check`, `git diff --check`, MP4 probe, and an event smoke across the 11 sample foods. Food entries were not changed.
- Fixed the live VBv2 path after James confirmed major pro/con SFX were still missing there, and pushed `e975c1f4` (`Fix live VBv2 pro con SFX playback`). The prior work had improved the render/MP4 path but live VBv2 still skipped SFX when reset/seek failed before `Audio.play()`. `docs/video-builder-v2/video-builder-v2.js` now preloads major pro/con SFX and still calls `Audio.play()` even if the reset/seek step throws. Bacon split-audio timing also warms real MP3 metadata so the live intro hook does not trust stale short durations such as `0.529s`, and `docs/video-builder-v2/index.html` was cache-busted to `20260730-vbv2-live-sfx-v1`. Playwright live VBv2 verification confirmed Bacon major cons call `major_con_siren_buzzer.mp3`, Kale major pros call `major_pro_sparkle_shine.mp3`, all 11 sample foods expose exact major pro/con event counts, and Bacon intro timing uses real metadata. No food entries changed.
- Synced stale generated pro/con narration with current visible pro/con titles and pushed `05e62877` (`Sync pro con narration with current titles`). Confirmed the major pro/con SFX fix is global across the current index: 274 foods total, 152 foods with major pro/con items, 82 major pros, 159 major cons, 241 expected major SFX cues. Kale and Raspberries were the only generated episodes with pro narration no longer matching visible pro titles; updated their script overrides and regenerated split audio (`kale` now `voice-v7`, `raspberries` now `voice-v4`). Refreshed subtitles, alignments, `foods-index.*`, and the app asset manifest. Added media-duration recording to the voice generator so future short blocks are less likely to be clipped by alignment timing. Verification found 0 pro/con title mismatches across 156 generated episode scripts and passed VBv2 load checks for Bacon/Kale/Raspberries plus `node --check`, `git diff --check`, and narration/subtitle verification. No `foods/` entries changed.
- Restored Bacon's live VBv2 intro take and pushed `3873c908` (`Restore Bacon VBv2 intro take`). James still heard no improvement because Bacon's active `voice-v15` intro used the same `Bacon!`/`Ranked!` audio hashes as the older bad `voice-v13` take. Created fresh Bacon-only `voice-v16`, restored the intro hook audio from `voice-v14`, kept the corrected current body narration, rebuilt alignment/index data, and cache-busted VBv2. Verification confirmed VBv2 loads `voice-v16`, `Bacon!` uses `audio/episodes/bacon/voice-v16-blocks/01-hook_food.mp3`, the `Bacon!` duration is the full `0.784s`, `Ranked!` follows after it instead of clipping into it, `foods-index.json` changed for Bacon only, no `foods/` or `rulesets/` entries changed, and `verify-narration-subtitles.js bacon`, JS syntax checks, and `git diff --check` passed.
