## 2026-07-31 14:09 UTC - VBv2 Proofing And Narration Notes

- James wants FoodRanked video/layout inspection delivered as web pages first, not MP4s. Use VBv2 proof pages for layout/framing checks, then only generate/download MP4 once the page looks right. The bacon proof page pattern is `docs/video-builder-v2/index.html?food=bacon&proof=mp4&render=mp4`; published page link used was `https://dawggbot.github.io/Foodranked/video-builder-v2/index.html?food=bacon&v=4d6a4a56`. DBv2 placement page pattern is `docs/display-builder-v2/index.html?videoBuilderExportFood=bacon`.
- User feedback: the VBv2 proof page looked accurate, but downloaded/rendered MP4s still appeared unchanged or mismatched compared with VBv2. Treat MP4 export/capture as unresolved until proven from the actual downloaded MP4, and avoid telling James it is fixed based only on smoke checks.
- Durable narration guidance from James: for submacro/micro callouts, prioritize nutrients that matter most to that food type, the outstanding highest and lowest values, and exceptions that are unusually high or low for that food type. After each highlighted mention, include a very brief explanation of what the nutrient is good for and whether the amount is expected for that food.
- Example context: in bacon, collagen can deserve mention if it is the lowest submacro, but the narration should briefly say what collagen is useful for and whether bacon being low in it is expected.
- Cleanup direction under consideration: deleting/archive old original Video Builder, old Display Builder/test page, Studio dashboard, and visual-template page would reduce confusion more than improve performance. Do not blindly delete shared dependencies. Keep `docs/app/` for now because Layout Builder still iframe-loads `docs/app/index.html?mode=layout-builder` and v2 still uses shared assets/code from `docs/app`. Keep `templates/visual-template.v1.json` until generator/schema references are migrated. A useful guard would redirect direct `docs/app/index.html` opens to DBv2 unless opened with `mode=layout-builder`.

## 2026-07-31 15:13 UTC - VBv2 MP4 Path And Cleanup

- Cleanup is now done and pushed in commit `3b001a44` (`Clean stale builders and guard VBv2 exports`): removed stale original Video Builder, Studio dashboard, Display Builder test, visual-template HTML, mockup visual template, and stale Bacon proof MP4. `docs/index.html` now links only Database, Layout Builder, DBv2, and VBv2.
- `docs/app/index.html` direct opens now redirect to DBv2, while `docs/app/index.html?mode=layout-builder` remains available for Layout Builder. Continue treating `docs/app/` as shared infrastructure, not dead code, until Layout Builder/v2 dependencies are migrated.
- VBv2 hosted pages no longer download stale published MP4s. MP4 export should go through the local helper path that receives the live VBv2/DBv2 placement payload; the helper was guarded to reject renders without placement payload.
- The prior MP4 mismatch was caused by rendering/serving a path that could not see VBv2's browser-local DBv2/Layout Builder placement state. Future MP4 checks should compare actual sampled frames from the generated MP4, not only the web proof.
- The micro icon invisibility bug in proof/render mode was fixed by not canvas-swapping `micros_section` GIF icons, so vitamin/mineral icons stay as normal images.
- Bacon was rendered and pushed in commit `0035eb6a`; output path is `docs/video/episodes/bacon/bacon-vbv2.mp4`, with published raw URL `https://github.com/dawggbot/Foodranked/raw/main/docs/video/episodes/bacon/bacon-vbv2.mp4`. It was rendered through the VBv2 placement path and checked with sampled MP4 frames for fats, protein, vitamins, minerals, pros/cons, and final D-tier reveal. `ffprobe` showed `1080x1920`, `30fps`, `133.644s`, about `27MB`.
