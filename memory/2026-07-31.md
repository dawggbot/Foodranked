# 2026-07-31 Durable Memory

- James now prefers FoodRanked video inspection as web proof pages first, not MP4s. Use VBv2 proof URLs such as `docs/video-builder-v2/index.html?food=<id>&proof=mp4&render=mp4`; only render/download MP4 after the proof page is confirmed.
- VBv2/DBv2 layout work: `7407de27` made VBv2 preview fit screen height and changed MP4 render scaling to use active DBv2/VBv2 grid width. `2b45a60f` added VBv2 `MP4 proof` mode, stripping side panels and using render-mode scaling for fast browser checks.
- Stale builder cleanup: `3b001a44` removed public/stale original Video Builder, Studio dashboard, Display Builder test, visual-template HTML/mockups, and stale Bacon proof MP4; `docs/index.html` now exposes the active tools only. Keep `docs/app/` for Layout Builder use; direct `docs/app/index.html` redirects to DBv2 unless opened with `mode=layout-builder`.
- VBv2 MP4 debugging conclusion: the hosted proof page was accurate but terminal/static MP4 renders kept using stale placement/layout payloads. A bad placement had section indicators around `y: 213.75` while the MP4 canvas grid is `105 x 186.667`, so validation saw indicators but they rendered wrong/off-canvas. `f5a16b6e` deleted the bad published Bacon MP4 and added stricter validation/bounds checks. Do not trust old Bacon MP4s as layout evidence.
- Future MP4 direction: build a small local app, likely Electron, with VBv2 as the single source of truth. It should open the trusted VBv2 proof, hold the live DBv2/Layout Builder placement state, and render/download `*-vbv2.mp4` from that exact DOM/state with one button, avoiding GitHub Pages/static JSON/helper confusion.
- Bacon VBv2 vitamin highlight bug fixed: `468abd35` tightened micron/vitamin glow styling so B12 no longer bleeds into vitamin K and vitamin D no longer bleeds into vitamin C. Final pushed main head after that work was `4e5ff651`. Proof URL pattern remains `https://dawggbot.github.io/Foodranked/video-builder-v2/index.html?food=bacon&proof=mp4&render=mp4&v=<commit>`.
- Future narration guidance from James: submacro/micro callouts should prioritize nutrients that matter most to the food type, outstanding highs/lows, and unusual exceptions for that food type. After mentioning one, briefly say what it is good for and whether the amount is expected or unusual. Do not add generic explanations everywhere.
- Skill/workflow to-do from James: use lessons from the 11 approved samples to update skills/workflows for filling every nutrition section. Create/use a `nutrition-geek` skill to fill food entries from USDA API first, outside research where appropriate, and clearly labeled educated estimates only when defensible. Then redo other food entries, especially pros/cons, without changing the 11 approved samples unless James explicitly unlocks them. Script writing should be based on the actual food nutritional profile.
- Pending Skill Workshop proposals were created for `nutrition-geek`, `nutrition-scoring-engineer`, `nutrition-workflow-automator`, and `foodranked-script-writer`; they encode USDA-first sourcing, complete section coverage, estimate labeling, `N/A` when not defensible, sample-lock protection, and script/pro-con alignment with the nutrition profile. Apply/finish these before batch redoing entries.
- Correction to Bacon VBv2 vitamin highlight memory: James rejected the tighter micron glow class and asked for it reversed. `ced4cd39` fixed the Bacon vitamin bleed by clipping the existing micron glow and removing micron handoff overlap, without reintroducing the tighter glow class and without changing food entries. Do not use the earlier `micron-narration-highlight` approach.
- Database update: `ced4cd39` added a `Finalised Entries` section to the Database with all 274 foods as checkboxes. The 11 sample/finalised foods are ticked and locked; other foods are toggleable locally.
- Dynamic background check at `ced4cd39`: VBv2/Layout Builder food-type sprite cascade worked in tested cases, with Bacon using bacon sprites, Kale using kale sprites, and Oats falling back to the grain plate.
- Correction to Database finalised entries: as of `8e190810`, the Database `Finalised Entries` section should show only the 11 finalised/sample foods, all checked. Do not show the unfinalised foods in that list.
- Bacon VBv2 vitamin highlight constraint from James: do not change the actual highlighted glow style, strength, or class aesthetics. He rejected tighter/contained/flattened glow changes. Fix only the bleed where the glow appears over the wrong neighboring vitamins/minerals. Latest attempted fix is `d7753336`, using clipped micron highlight clones/lanes so active B12/D glow does not exist over K/C; if bleed remains, adjust target isolation/clip geometry only, not the glow itself.
- Current local worktree context after `d7753336`: main is pushed, but skill proposal files remain dirty/uncommitted (`skills/foodranked-script-writer/SKILL.md`, `skills/nutrition-scoring-engineer/SKILL.md`, `skills/nutrition-workflow-automator/SKILL.md`, and new `skills/nutrition-geek/`) plus the untracked daily memory file. Do not commit those skill proposals unless continuing the skill pass intentionally.
- James confirmed the latest Bacon vitamin highlight isolation was "so much better" after `d7753336`; keep improving bleed, if needed, by isolating/clipping the glow target only. Do not change the glow look, strength, or class aesthetics.
- Current tool-state model explained to James: Layout Builder saves layout/placement in browser `localStorage`; DBv2 reads that layout and applies selected food sprites/text/nutrition; VBv2 reads DBv2 rendered placement and adds timing/subtitles/narration/VFX/highlights/proof/export behavior. This is linked but fragile because it is same-browser/localStorage based; the app should replace it with one shared backend source of truth.
- App target from James: build local-only `FoodRanked Studio` combining Database, Layout Builder, DBv2, and VBv2 into one app, eventually containing social media automation project state so GitHub is not essential to project integrity. James will download MP4s from the app and upload manually for now.
- `368d4abf` added `APP-BUILD-READINESS.md` and README link. It documents needed app pieces: backend API, SQLite/Postgres path, media store, render-job queue, proof/render parity, secrets/settings, validation gates, backups, audit log, publishing packs, social post status, and analytics import.
- `b7252919` started local `FoodRanked Studio`: `npm run studio`, one shell wrapping Database/Layout/DBv2/VBv2, APIs for health/foods/tools/app state/backups/VBv2 render jobs, ignored `studio-data/`, backup button including app state plus browser-local builder state, and local renderer path for MP4 downloads. ElevenLabs key was found in `.env.local`; USDA key was not found under common env names, so `.env.example` now documents `USDA_API_KEY=`.
- James could not access `http://127.0.0.1:4787/` because it was running on the workspace machine, not his laptop. He wants a Steam-like downloadable Windows app.
- `595c88a1` added Windows desktop packaging: Electron shell (`npm run desktop`), Windows zip build (`npm run dist:win`), GitHub Actions workflow `Build FoodRanked Studio Windows`, bundled ffmpeg/ffprobe support, and MP4 downloads saving through the app. Windows user path: `git pull`, `npm ci`, `npm run desktop:install-browsers`, `npm run desktop`; build zip with `npm run dist:win`. Zip built successfully here as `dist/desktop/FoodRanked Studio-0.1.0-win.zip` but was not committed because it is generated and around 465MB. Could not GUI-smoke Electron in Linux workspace due to no display server.
- Current worktree after `595c88a1`: main is pushed and synced; dirty/uncommitted skill proposal files still exist (`skills/foodranked-script-writer/SKILL.md`, `skills/nutrition-scoring-engineer/SKILL.md`, `skills/nutrition-workflow-automator/SKILL.md`, and new `skills/nutrition-geek/`) plus `memory/2026-07-31.md`. Do not commit the skill proposals unless intentionally continuing the nutrition skill pass.
