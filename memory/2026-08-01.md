# 2026-08-01

- Layout builder: James is actively redoing the canonical FoodRanked layout. Do not delete old layouts yet. When James says the new layout is saved and exactly how he wants it, treat that exact layout as canonical, point generation/workflows at only it, and delete other existing layout files to avoid confusion.
- Recent layout-builder rule: the intro section is the source for header and UI sprite placement across all nine sections.
- Section indicators: each section should get its corresponding highlighted section indicator centered on the exact center of the normal section indicator slot. The highlighted sprite must keep its original/native size, render foremost on the layer, and be saved last/highest-z so it appears above the normal indicator.
- Sprite sizing rule from James: layout-builder automation must not scale authored sprites. Preserve user-authored `width`/`height` unless James explicitly asks for scaling. This especially applies to highlighted section indicators and the macro bar frame/fill sprites; macro bar frame/fill drops use their native `104x17` size rather than a global drop scale.
- James preference for layout-builder fixes: if he placed or sized a sprite manually, preserve that placement/size exactly while fixing the surrounding automation.

## 12:14 UTC Layout Builder Updates

- Latest macro bar sizing override: James approved the fat-icon-scale trial. Macro bar frame/fill sprites should render at the fat icon's scale ratio (`24/32 = 0.75`), so the `104x17` bar sprites trial at `78x13`. This supersedes the earlier "bar drops stay native 104x17" note unless James asks to revert.
- The fat-icon-scale macro bar change was made to be easy to reverse: previous bar dimensions are backed up in layout meta under `macroBarFatIconScaleTrialBackupV1`, and the behavior is isolated behind `MACRO_BAR_FAT_ICON_SCALE_MODE`.
- Section indicators were moved down by 1 nudge across all sections after James confirmed the fat-icon-scale bars looked good.
- Arrow indicator placements from the Fats section were synced into the Carbs and Protein sections. Keep only submacro arrow indicators, per durable FoodRanked rule.
- Relevant pushed commits from the layout-builder session: `107eac24` for fat-icon-scale macro bars and `57e5b3dd` for the section-indicator nudge plus Carbs/Protein arrow sync.
