# 2026-08-01

- Layout builder: James is actively redoing the canonical FoodRanked layout. Do not delete old layouts yet. When James says the new layout is saved and exactly how he wants it, treat that exact layout as canonical, point generation/workflows at only it, and delete other existing layout files to avoid confusion.
- Recent layout-builder rule: the intro section is the source for header and UI sprite placement across all nine sections.
- Section indicators: each section should get its corresponding highlighted section indicator centered on the exact center of the normal section indicator slot. The highlighted sprite must keep its original/native size, render foremost on the layer, and be saved last/highest-z so it appears above the normal indicator.
- Sprite sizing rule from James: layout-builder automation must not scale authored sprites. Preserve user-authored `width`/`height` unless James explicitly asks for scaling. This especially applies to highlighted section indicators and the macro bar frame/fill sprites; macro bar frame/fill drops use their native `104x17` size rather than a global drop scale.
- James preference for layout-builder fixes: if he placed or sized a sprite manually, preserve that placement/size exactly while fixing the surrounding automation.

## 12:14 UTC Layout Builder Updates

- Latest macro bar sizing override: James approved the fat-icon-scale trial. Macro bar frame/fill sprites should render at the fat icon's scale ratio (`24/32 = 0.75`), so the `104x17` bar sprites trial at `78x13`. This supersedes the earlier "bar drops stay native 104x17" note unless James asks to revert.
- The fat-icon-scale macro bar change was made to be easy to reverse: previous bar dimensions are backed up in layout meta under `macroBarFatIconScaleTrialBackupV1`, and the behavior is isolated behind `MACRO_BAR_FAT_ICON_SCALE_MODE`.
- Section indicators were moved down by 1 nudge across all sections after James confirmed the fat-icon-scale bars looked good.
- Arrow indicator placements from the Fats section were synced into the Carbs and Protein sections. Keep only submacro arrow indicators, per durable FoodRanked rule.
- Relevant pushed commits from the layout-builder session: `107eac24` for fat-icon-scale macro bars and `57e5b3dd` for the section-indicator nudge plus Carbs/Protein arrow sync.

## 17:06 UTC Studio App/Layout Reset

- Latest app/layout instruction from James supersedes the earlier "do not delete old layouts yet" note: delete old wrong app/layout paths and rebuild around only the current FoodRanked tools/layout chain.
- Current Studio app should contain the current Layout Builder, Database, DBv2, VBv2, and the Input panel/API from the recent good app for adding/filling food entries plus PNG and narration audio uploads.
- Canonical render chain is now: Layout Builder saved layout named `test` -> fresh DBv2 placement export -> VBv2 preview/render. Do not render from old builder layouts, stale placement caches, restore-test-layout seeds, or ad-hoc placement files.
- Studio/startup/render behavior from the latest rebuild: clear stale old builder/placement caches, preserve current Layout Builder keys, refuse MP4 rendering unless the current Layout Builder has a saved `test` layout, and send fresh Layout Builder state plus fresh DBv2 placement into the VBv2 renderer.
- MP4 proof is removed from both webpage and app. Do not restore proof mode, proof shortcuts, `proof=mp4`, or MP4 proof UI. Normal VBv2 MP4 render/download remains acceptable.
- Latest pushed app release at compaction time: commit `15d56d9f`, tag `studio-windows-v0.1.5-20260801`, download `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.5-20260801/FoodRanked-Studio-Windows.zip`.

## 17:59 UTC App Layout Canonicalization

- James showed that the app Layout Builder was still using the wrong old layout despite recent rebuilds. Treat that old app layout as permanently unwanted; do not reintroduce it, its fallback file, or its cached seeded placement path.
- The wrong fallback layout file `docs/app/default-layout.js` was deleted. Future Studio/app work should not recreate a default layout fallback; import/use the current canonical Layout Builder saved `test` layout instead.
- Current canonical app seeding path after the fix: one current Layout Builder `test` layout is imported into the app and used to seed Studio, Layout Builder, and DBv2. The app clears old food-scoped layout caches and old DBv2 placement exports.
- Layout verification detail from the latest fix: the correct Layout Builder app view has section dots at approximately `y=173`, not the bad old row around `y=139`.
- Latest pushed app release after this correction: commit `5acc73de`, tag `studio-windows-v0.1.6-20260801`, download `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.6-20260801/FoodRanked-Studio-Windows.zip`.

## 19:13 UTC Layout Builder Recovery/Lock Rules

- Supersedes the 17:59 app canonicalization note: the v0.1.6 canonical seed was the bad old layout and overwrote James's correct Layout Builder state. Do not use that seed or recreate any auto-import/forced seed behavior from it.
- App work is paused unless James explicitly asks for it. He said to forget about the app after the layout loss. Do not rebuild or retarget the app while the Layout Builder is being recovered.
- The hand-made morning layout was lost from available committed/local sources. James redid the Intro header manually, then asked to copy that Intro header to all other sections.
- Commit `4413bae8` applied the Intro section header as the source for header sprites and header text placement across all sections, with timestamped localStorage backups before syncing.
- Commit `5a2d056a` unlocked Layout Builder editing/saving again. Current public cache-busted link: `https://dawggbot.github.io/Foodranked/layout-builder/index.html?v=20260801-unlock-layout-save-v1`.
- Current Layout Builder behavior after `5a2d056a`: layout name input is editable, saving `test` is allowed, `test` is protected from deletion, old food-scoped layout cache is cleared in Layout Builder mode, and saving over `test` first creates backup copies.
- Critical durable rule from James: when he finishes reconfiguring and says to lock/save it, make multiple non-overwriting copies before any future automation: saved browser preset `test`, timestamped backup presets, exported JSON in repo, committed git snapshot, and pushed GitHub backup. Never overwrite the locked layout or seed/import over it without explicit instruction and fresh backups.

## 19:59 UTC Layout Builder Macro/Micro Completion

- James has resumed manual Layout Builder work after the lost layout incident. App work remains out of scope unless he explicitly asks for it.
- Commit `4eb0b4b3` synced the ready Fats-section arrow indicators into the other two macro sections, Carbs and Protein, with localStorage backups before migration. Public link from that step: `https://dawggbot.github.io/Foodranked/layout-builder/index.html?v=20260801-macro-arrow-sync-v1`.
- James said every section other than Vitamins and Minerals is complete. Do not touch completed non-vitamin/mineral sections unless James explicitly asks.
- Commit `051b1a03` removed the `Apply fats layout to macros` button from Layout Builder and completed the Vitamins/Minerals micro-bar layout. Public link: `https://dawggbot.github.io/Foodranked/layout-builder/index.html?v=20260801-micro-bars-complete-v1`.
- Current Vitamins/Minerals migration behavior: copy the fully filled first Vitamins micro-bar column pattern across all Vitamins columns (`1..6`) and Minerals columns (`1..5`), then add a centered `??%` textbox to each micro bar sprite using existing Vitamins textboxes as placement reference.
- Keep normal `vitamins_percent_1..6` and `minerals_percent_1..5` labels untouched. Only micro bar sprite percentage textboxes should use the placeholder `??%` until food data/rendering fills them.
- Layout Builder should create localStorage backups before any automatic macro/micro layout migration and must not overwrite the locked `test` layout without explicit instruction and backups.

## 21:04 UTC Layout Builder Lockdown

- James confirmed the Layout Builder is complete, but app work remains out of scope unless he explicitly asks for it.
- Commit `1915a21d` unlocked manual macro arrow editing after James said the previous macro arrow sync was wrong. Layout Builder no longer auto-copies Fats arrows into Carbs/Protein, broader macro sync ignores arrow sprites, and Layout Builder no longer food-syncs macro arrow visibility/color while editing. DBv2/VBv2 production rendering behavior was left unchanged.
- Durable rule from James: Layout Builder should never be food-synced under any circumstances; DBv2 owns food fill-in/sync. Do not add Layout Builder food-sync behavior back.
- Commit `13345c0a` locked down the completed Layout Builder presets. Public cache-busted link from that step: `https://dawggbot.github.io/Foodranked/layout-builder/index.html?v=20260801-layout-lockdown-v1`.
- Current Layout Builder lockdown behavior: on reload in James's editing browser, copy the current working layout into `test 1`, `test 2`, `test 3`, `test 4`, and `test 5`; lock those copies so they cannot be overwritten or deleted; remove non-matching saved layouts from builder storage; and keep DBv2 pointed at the locked copies while ignoring old builder food-layout caches.
- The `Restore test from VBv2` feature was removed because it is now useless/confusing and can reintroduce stale layouts. Do not restore it unless James explicitly asks.
- Treat `test 1` through `test 5` as protected master layout backups. Never overwrite, import over, seed over, auto-migrate, delete, or otherwise replace them without explicit instruction from James plus fresh backups/exported JSON/Git commit first.
