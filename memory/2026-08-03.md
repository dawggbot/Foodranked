## 2026-08-03 - Plums entry note

- James confirmed Plums is present. Before treating Plums as final, round macro gram amounts to the nearest decimal place and fill the currently blank pros/cons with exactly 3 practical, food-specific pros and exactly 3 practical, food-specific cons.
- James is going to rewrite the Plums script and send it back as a style reference. When he does, use the rewrite to learn the desired future narration style, generate/get the narration audio files for the revised Plums script, and push the audio/script update to the app through Agent Sync.

## 2026-08-03 - Plums and Buckwheat app sync

- Plums pros/cons were filled and pushed. The app needed an Agent Sync job with rich `displayItems`/`contextItems`; otherwise DBv2/VBv2 can show `N/A` for pros/cons. The final fix was pushed as commit `014f47d2` with the job title `Plums Pros/Cons DBv2 fix`.
- Buckwheat sprite was pushed for app sync in commit `4e2dcb3b` with the Agent Sync job title `Buckwheat sprite update`.
- Keep using Agent Sync jobs for app-facing FoodRanked updates that James needs to pull into the app, and verify DBv2/VBv2 display paths when pros/cons or context sections are involved.

## 2026-08-03 - Buckwheat VBv2 plate sprite fix

- James confirmed the Buckwheat sprite itself looked good, but it was missing from the VBv2 food plate. The fix was pushed as commit `0d3c6aad` with the Agent Sync job title `Buckwheat sprite plate fix`.
- For app sprite pushes, include the food in the app food-image bundle plus DBv2/VBv2 sprite allowlists and size tables; updating only the upload/manifest path can leave the VBv2 header food plate blank.
- The Buckwheat plate fix intentionally did not change plate/canvas layer positions. Verification showed the VBv2 `Header food image` layer loading `/studio-data/uploads/images/buckwheat/buckwheat.png` as a `30x30` image.

## 2026-08-03 - Buckwheat nameplate and Plums rewrite/audio

- James reported Buckwheat was visible in DBv2 and the VBv2 plate, but still missing from the VBv2 nameplate. The pushed fix is commit `2659f1c1` with Agent Sync job title `Buckwheat VBv2 nameplate fix`.
- VBv2 nameplate sprite handling must catch the `Library: selected food image` layer as well as `Header food image`; fixing only the header/plate path can leave the nameplate blank.
- James rewrote the Plums narration and wants that style learned for future scripts. Keep exact nutrition facts precise, but use plainer category context: explain why the number matters for that food type, say when a section is not important for fruit, and use practical summaries like quick on-the-go snacks, low-calorie-to-volume ratio, and ability to complement other foods.
- The Plums rewrite audio was generated as `voice-v1` split narration, subtitles were aligned/verified, and the app sync was pushed in commit `2659f1c1` with Agent Sync job title `Plums rewritten narration audio`.
- A pending Skill Workshop proposal captured the Plums rewrite style: `foodranked-script-writer-20260803-df1fa239cb`. Review/apply it before future FoodRanked script generation.

## 2026-08-03 - Plums playback/display and Buckwheat plate rebuild

- James later reported the app still froze when pressing play on Plums, Buckwheat still was not sitting over the VBv2 food plate in the installed app, Plums nutrition values displayed badly because tiny values were being rounded away, and Plums arrow indicators looked wrong.
- The pushed fix is commit `a99046dd`. It also built and released a new Windows Studio app: `studio-windows-v0.1.24-20260803`, download `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.24-20260803/FoodRanked-Studio-Windows.zip`.
- New Agent Sync jobs to run after installing that build: `Plums playback and display fix` and `Buckwheat VBv2 plate sprite fix v2`.
- Buckwheat VBv2 plate placement should match DBv2: food image sprite exported/rendered as `23x23` at `x: 8`, `y: 5`, over the `29x29` food plate.
- Plums playback was switched to local `/studio-data/uploads/...` split-audio paths and verified in live VBv2 playback to avoid app freezes from incompatible audio paths.
- Permanent ElevenLabs narration speed is now `1.12` in `config/elevenlabs-voice-settings.v1.json`.
- Macro display rule nuance from the Plums fix: keep main macro gram displays rounded to nearest 0.1g, but preserve very small submacro gram values with two decimals when needed so values like `0.02g` and `0.04g` do not display as zero. Only submacros should use arrow indicators.
- Plums arrow bands were normalized so cholesterol is green while low omega/poly fat values are red; continue checking arrow color semantics when regenerating FoodRanked display data.

## 2026-08-03 - Submacro display precision rule

- James clarified the display precision rule with examples: `sugar_g: 9.92` should display as `9.9g` because values starting at 1 or above use one decimal; `saturated_fat_g: 0.017` should display as `0.02g`, and `polyunsaturated_fat_g: 0.044` should display as `0.04g`, because nonzero submacro values below 1 keep two decimals. This is display-only and should not change source values, scoring, arrow bands, or narration unless separately requested.

## 2026-08-03 - Plums preview performance and Buckwheat Raspberries-pattern sync

- The latest pushed fix is commit `07c901cc` with Windows Studio release `studio-windows-v0.1.25-20260803`: `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.25-20260803/FoodRanked-Studio-Windows.zip`.
- After installing that app build, run Agent Sync jobs `Plums display precision refresh` and `Buckwheat sprite and MP4`.
- Plums display precision is now permanent in generator, DBv2, and VBv2: values >= 1 display to one decimal, while nonzero submacro values below 1 display to two decimals. Verified examples: `9.92g` -> `9.9g`, `0.017g` -> `0.02g`, `0.044g` -> `0.04g`.
- To reduce lag in VBv2 live preview, playback redraw is capped around 24fps and preview background sprites are capped at 8. If James still reports lag, inspect preview rendering cost before changing nutrition data.
- Buckwheat sprite sync was changed to follow the working Raspberries pattern and include `renderMp4`, because repeated image-only jobs still left the VBv2 plate blank in the installed app.
- A pending Skill Workshop proposal for the display precision rule is `nutrition-scoring-engineer-20260803-38440cf565`; review/apply it before future scoring/display work.

## 2026-08-03 - VBv2 food image placement cache

- James reported Buckwheat and Raspberries food image sprites were still missing from the VBv2 nameplate/food plate while appearing correctly in DBv2, and asked whether VBv2 draws from DBv2. Answer: VBv2 reads DBv2's saved placement export, not the live DBv2 screen, so stale placement cache can make DBv2 look correct while VBv2 stays blank.
- The latest pushed fix is commit `d07dfcee` with Windows Studio release `studio-windows-v0.1.27-20260803`: `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.27-20260803/FoodRanked-Studio-Windows.zip`.
- That fix forces a fresh DBv2 placement export for VBv2 food-image placement and keeps packaged sprite fallback paths for Buckwheat/Raspberries like Kale. It should not need an Agent Sync job; install the new app build and reopen VBv2.

## 2026-08-03 - Header plate render stack lesson

- James confirmed the correct app fix is commit `18eae587` (`Fix header plate render stack`) with Windows Studio release `studio-windows-v0.1.28-20260803`: `https://github.com/dawggbot/Foodranked/releases/download/studio-windows-v0.1.28-20260803/FoodRanked-Studio-Windows.zip`.
- The final issue was not food-specific Buckwheat/Raspberries data. DBv2 and VBv2 share DBv2's placement export, but VBv2 draws its own stage. The renderer draw order must enforce the header stack: food plate behind calorie bubble, and food plate behind selected food image.
- Future food image/plate fixes should start by checking the webpage/app render comparator stack before adding food-specific Agent Sync patches. Preserve coordinates; fix order at renderer level across DBv2 webpage, VBv2 webpage, and bundled app page when the layer stack is the problem.

## 2026-08-03 - Black Beans sprite and add-food workflow

- James confirmed the Black Beans sprite works in the app. The sprite upload arrived as `black beans.png`; normalize uploaded food sprite filenames to app ids such as `black-beans.png` before bundling.
- The Black Beans app sprite import was pushed as commit `d72df0ed` with Agent Sync job title `Black Beans sprite update`. The import path updated the app sprite bundle, DBv2/VBv2/bundled app allowlists and size tables, `foods-index`, and `app-assets`; verification checked DBv2 resolution and VBv2 rendering over the legume plate with the correct header stack.
- For a finalized food entry ready for app download prep, gather food identity/category, sprite, audio preference, and style notes; build `foods/<food-id>.sample.json` with per-100g sourced nutrition, scoring fields, exactly 3 pros and exactly 3 cons; run scorer/proof checks; generate script, subtitles, manifest, app/dashboard data, and docs data; add sprite to source/app paths and verify DBv2/VBv2 header stack; generate/align split narration audio if needed; create a top Agent Sync job; verify DBv2/VBv2 display, arrows, pros/cons, subtitles, playback, and sprite placement; then commit and push. "Finalized" means complete, pushed, and app-syncable, not that the actual video download was triggered.
