# 2026-08-05

- James confirmed Cranberries is now perfect in the app after the fixed Windows release. Cranberries should be treated as the current gold-standard food entry proof for DBv2/VBv2/app parity: complete data, sprite placement under the calorie bubble, split narration audio present, Agent Sync refresh path working, and installed Windows app verification.
- A Skill Workshop proposal based on the Cranberries workflow was approved and applied as `foodranked-entry-finalizer`. Use it for future single-food or batch food entry work. It covers source data, scoring, exactly 3 pros/3 cons, narration/script, split audio, sprite/app assets, Agent Sync, web proof, Windows release, and installed-app verification.
- Added White Potato and Hazelnuts uploaded sprites to the app and web asset flow. `white potato.png` was normalized to `white-potato.png`; both sprites were added under `docs/app/sprites/header/food_images/`, DBv2/VBv2/app sprite allowlists and sizes were updated, `foods-index` and `app-assets` were regenerated, and Agent Sync job `White Potato and Hazelnuts sprite update` was added. Pushed commit `eadec45e`; Windows release `studio-windows-v0.1.31-20260805` contains the sprite update.
- Fixed macro bar SFX being audible in VBv2 but too quiet in exported MP4s. MP4 exporter now boosts quiet single macro-bar SFX events while capping stacked macro fills so foods like Cranberries do not get harsh or clipped. Pushed commit `cb199b90 Boost macro bar SFX in MP4 exports`; Windows release `studio-windows-v0.1.32-20260805` contains the fix.
- Macro SFX verification from the fix: Bacon macro SFX improved from about `-21.7 dBFS` to `-15.9 dBFS`; old bundled Bacon MP4 was about `-27.3 dBFS`; Cranberries stayed controlled at about `-13.7 dBFS`. Checks passed: `node --check`, `npm run studio:check`, and `git diff --check`.
- Macro bar SFX design decision: VBv2 remains the creative/source-of-truth for event timing, playback rate, source slice, fades, lowpass, and volume. The MP4 path then applies a macro-only export gain for perceptual parity because identical numeric VBv2 volumes sounded quieter after narration/music mixing and ffmpeg/AAC export.
- Added a DBv2 webpage `Thumbnail` section before `Intro`. It reuses the intro ranked/food lockup, removes background/phone elements, and uses the food-type hue as the blurred backdrop. The normal VBv2 placement payload and 9 video section indicators remain unchanged. Pushed commit `6de3ad91 Add DBv2 thumbnail section`.
- Wired the DBv2 `Thumbnail` section into the installed Windows app by packaging the same `docs/` source into Windows release `studio-windows-v0.1.33-20260805`. James needs that app zip or newer to see `Thumbnail` inside the app.
- Added Chia Seeds and Greek Yoghurt uploaded sprites to the app/web asset flow. Pushed commit `95b9a116`; Windows release `studio-windows-v0.1.34-20260805` contains the sprites. Verified both sprites load in DBv2/app data, app asset manifest includes them, and a short Chia Seeds MP4 smoke render succeeded with macro-bar fill SFX mixed at `volume=1`.
- James clarified that food sprites in the ranked/thumbnail lockup must be scaled like the ranked sprite. Treat square 30x30 food sprites as 48x48 in the ranked lockup, matching the ranked sprite's 1.6x display scale from 50x50 to 80x80. Future food sprite entries should carry natural width/height metadata so DBv2/VBv2/app do not fall back to the short Bacon-shaped box.
- Implemented the food sprite scale fix by carrying natural sprite dimensions through the app asset data. Chia Seeds and Greek Yogurt now carry 30x30 metadata, VBv2 intro lockup displays them at 48x48, and the header plate remains 23x23 so it stays below the calorie bubble. Pushed commit `30de3d98`; Windows release `studio-windows-v0.1.35-20260805` contains the scale fix.
- James tested Chia Seeds MP4 from v0.1.35 and still could not hear the macro bar SFX. The exporter was including the events, but Chia's short fills were still below practical audibility. Updated `scripts/render-vbv2-mp4.js` so short single macro fills receive a stronger MP4 export boost while stacked fills stay capped. Pushed commit `da739192`; Windows release `studio-windows-v0.1.36-20260805` contains the louder Chia macro SFX fix.
- Macro SFX verification after `da739192`: new Chia MP4 export had noticeably louder fats and audible protein macro fills, while the stacked carb fill stayed controlled. For future macro SFX work, test with Chia Seeds because it exposes the short-fill audibility problem better than Cranberries/Bacon.
