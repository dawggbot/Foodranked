# 2026-08-09

- Macadamia Nuts is now wired into the FoodRanked app and pushed to `main`. Relevant commits from this run: `b55e6977` (`Wire macadamia episode into app`), `913e23a4` (`Add macadamia Agent Sync job`), `4557bc92` (`Add macadamia narration handoff files`), and `3eae8b7e` (`Add macadamia ElevenLabs split narration`).
- Macadamia food id is `macadamia`, display name is `Macadamia Nuts`, and the episode is `C tier`. James supplied and approved the narration script used for the override/handoff.
- App sprite path for Macadamia is `docs/app/sprites/header/food_images/macadamia.png`.
- Macadamia ElevenLabs narration take is `voice-v1` with split audio enabled. There are 11 MP3 narration blocks under `production/episodes/macadamia/voice/`, mirrored for VBv2 under `docs/audio/episodes/macadamia/`, and `docs/data/foods-index.*` points Macadamia at `voice-v1` split audio.
- Verification after Macadamia narration wiring included `node scripts/verify-narration-subtitles.js`, `npm run studio:check`, and a VBv2 browser probe that saw Macadamia using `voice-v1`, `split-blocks`, and 11 narration events.
- The current ElevenLabs credential has been restored only in ignored local `.env.local`; do not commit or store the key value. This key can generate TTS but does not have `voices_read` or `forced_alignment`, so Macadamia used the pinned Eryn voice and local MP3-duration timing instead of random voice selection or ElevenLabs word alignment.
- Agent Sync has a separate job index that must be updated for Studio sync visibility. Macadamia's Agent Sync job pulls the sprite, final narration text, 11 MP3 blocks, and split-audio manifest, then selects Macadamia in Studio.
- Macadamia VBv2 MP4 download initially had narration that was present but too quiet. Fixed by loudness-normalizing the existing `voice-v1` split MP3 blocks and pushing commit `e04cfd75` (`Normalize macadamia narration audio`). Updated VBv2/Agent Sync manifests so the Macadamia sync job is now named `Macadamia full entry, sprite, and loud split narration update`.
- After the Macadamia loudness fix, verification included `node scripts/verify-narration-subtitles.js`, `npm run studio:check`, an Agent Sync shape check, and a staged secret scan. Measured voice-only render improved from about `-27.5 dB` mean to `-17.15 LUFS`; default mixed MP4 measured around `-13.42 LUFS`.
- Macadamia web VBv2 had narration but the installed app MP4 was silent because the app-synced entry kept stale inline block paths pointing at `docs/audio/...` instead of app-local downloaded `/studio-data/uploads/...` paths. Fixed in commit `f95a34bc` (`Fix Macadamia app split narration sync`) by using app-local split-audio manifest paths in the Agent Sync flow. Clean app sync downloaded all 11 MP3s and an app-path VBv2 MP4 rendered with audible narration around `-17.16 LUFS`.
- Future VBv2 app narration sync must verify both surfaces: the web/docs VBv2 path and a clean app Agent Sync install. App jobs should download split MP3 blocks plus the local split-audio manifest and avoid stale inline `docs/audio` block paths.
- James said the approved Tater Tots script is the strongest model yet for future scripts: keep the information dense but casual, use direct everyday phrasing, allow informal connective lines like `And that's all that really needs to be said about this section`, include practical prep/cooking caveats when they affect the score, and make the final summary conversational without losing nutrition precision.
- Tater Tots is now wired into VBv2/app and pushed to `main` in commit `c1815729`. Food id is `tater-tots`, display name is `Tater Tots`, tier is `Slop tier`, and the Agent Sync job is `Tater Tots full entry, sprite, and app-local split narration update`.
- Tater Tots work included the approved script override, uploaded sprite, generated ElevenLabs `voice-v1` split narration, loudness-normalized audio, updated subtitles/timing, refreshed VBv2/app asset manifests, and app-local split narration sync. Local smoke test installed the sprite plus 11 narration MP3 blocks through Agent Sync and rendered a VBv2 MP4 with audible audio.
- The Tater Tots style lesson was also captured as pending Skill Workshop update `foodranked-script-writer-20260809-71ba7a03e3`; apply that writing guidance to future FoodRanked script generation.
